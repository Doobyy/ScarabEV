// Auto-generated from admin-ui-src/scripts/documentation.js. Do not edit this file directly.
export const ADMIN_DOCUMENTATION_SCRIPT = String.raw`
const BIBLE_MARKDOWN = "# SCARABEV PROJECT BIBLE\n\nVersion: authoritative rebuild (generated from repository inspection)\n\nAudience: beginner/hobbyist maintainer\n\nScope: this document is the primary operating and maintenance manual for ScarabEV.\n\nRule: if a statement is not directly verifiable from repository code/config, it is labeled **NEEDS OWNER CONFIRMATION**.\n\n---\n\n## 1. EXECUTIVE OVERVIEW (PLAIN ENGLISH)\n\nScarabEV is a Path of Exile utility site that helps players decide which scarabs to vendor in the 3-for-1 recipe and which to keep/sell. It combines:\n\n- live market prices\n- observed community session data\n- EV (expected value) math\n- regex/token tooling\n- admin operations for health, failure logs, backups, and data management\n\nWhy users rely on it:\n\n- It turns large scarab inventories into actionable decisions.\n- It provides threshold-driven recommendations (vendor vs keep).\n- It reduces manual price checking.\n- It helps operate on probability instead of guesswork.\n\nMost important systems:\n\n- Frontend app (\`/index.html\` + \`js/*\`) for user-facing analysis.\n- Market worker (\`workers/market-worker/worker.js\`) as market/cache/snapshot authority.\n- Backend admin worker (\`backend/src/*\`) for secure operations, token/scarab lifecycle, and admin UI.\n- Session/community API worker (\`workers/scarabev-api/index.js\`) for observed weights/session aggregation.\n\nWhat must never casually change:\n\n- Worker-first market data trust model.\n- EV formulas without explicit validation.\n- Auth/session/CSRF/security checks in backend.\n- Token generation/profile constraints.\n- Storage schemas and backup/restore flow.\n\nFull ecosystem in plain terms:\n\n- Frontend shows recommendations and tools.\n- Frontend pulls market and history from market worker.\n- Frontend pulls observed weights/session aggregates from session API.\n- Frontend pulls token sets/scarab metadata from backend public endpoints.\n- Backend admin controls protected operations and writes backend D1 + R2 backups.\n- Market worker stores caches/history/failure state in KV.\n- Session API stores session and aggregate data in D1.\n\n---\n\n## 2. COMPLETE ARCHITECTURE MAP\n\n> **Beginner note:** Think of ScarabEV as four cooperating systems: **Frontend**, **Backend**, **Market Worker**, and **Session API**.\n\n### 2.1 Text Diagram\n\nUser Browser  \n-> **Frontend** (\`index.html\`, \`js/app.js\`)  \n-> **Market Worker** (\`workers/market-worker/worker.js\`) for prices, history, snapshot status, retries, failure logs, backup import/export, bulk name map  \n-> Session API (\`workers/scarabev-api/index.js\`) for \`/api/aggregate\`, \`/api/sessions\`, admin session review endpoints  \n-> **Backend** Admin Worker (\`backend/src/index.ts\`) for **auth**/admin UI/scarab+token management/ops routes  \n-> Storage:\n- Backend **D1** (\`scarabev-backend-*\`) + Backend **R2** (\`scarabev-backups-*\`)\n- Market **KV** (\`EV_HISTORY\` namespace)\n- Session **D1** (\`poepool-db\`/staging)\n\n### 2.2 Fast Ownership Matrix\n\n| System | Primary role | Main files | Storage |\n|---|---|---|---|\n| **Frontend** | UI, events, \`state\` reads, \`render\` | \`index.html\`, \`js/app.js\`, \`js/state.js\` | Browser memory + localStorage |\n| **Market Worker** | **worker-first** market cache/snapshot authority | \`workers/market-worker/worker.js\` | **KV** |\n| Session API | community session intake + aggregate weights | \`workers/scarabev-api/index.js\` | **D1** |\n| **Backend** Admin | private admin UI, secure mutations, token/scarab ops | \`backend/src/**\` | **D1** + **R2** |\n\n### 2.3 Trust Boundaries\n\n- Browser is untrusted input boundary.\n- **Backend** admin routes require authenticated session + role + **CSRF** for mutations.\n- **Market Worker** does controlled upstream \`fetch\` and cache guard logic.\n- Session API accepts public submissions and applies intake/scoring/moderation layers.\n\n### 2.4 Data Authority\n\n- **Market price authority for app behavior**: market worker cached responses (**worker-first** model).\n- **Observed weight authority**: session API aggregate output.\n- **Token authority**: backend published token set (\`/public/token-set/latest\`).\n- **Frontend state authority**: in-memory \`state\` object (\`js/state.js\`) plus specific localStorage caches.\n\n### 2.5 Cache vs Source of Truth vs Temporary\n\n- **source of truth** (operational):\n  - Backend **D1** for auth/scarabs/tokens/audit/ops metadata\n  - Session API **D1** for session/aggregate data\n- Cache/history stores:\n  - Market **KV** keys (\`market-cache-v2:*\`, history and incident/failure keys)\n- Temporary runtime:\n  - Browser \`state\` object\n  - Some localStorage caches (token cache, session history, UI preferences)\n\n> **Owner reminder:** Do not treat cache as canonical data. Cache helps speed and resilience; it is not the same as **source of truth**.\n\n---\n## 3. FULL REPO MAP / FILE OWNERSHIP\n\n### 3.1 Top-level map\n\n- \`index.html\`: main frontend shell.\n- \`js/\`: frontend modules.\n- \`backend/\`: Cloudflare worker TypeScript backend/admin.\n- \`workers/market-worker/\`: market/cache/snapshot worker.\n- \`workers/scarabev-api/\`: session/community API worker.\n- \`scripts/\`: utility checks/smoke scripts.\n- \`tests/frontend-e2e/\`: Playwright E2E tests.\n- \`.github/workflows/\`: CI/deploy workflows.\n- \`AGENTS.md\`: operational guardrails.\n\n### 3.2 Ownership (must-follow)\n\n| Area | Primary file(s) | Why this is the source of truth | Early edit safety |\n|---|---|---|---|\n| UI/events/\`render\` | \`js/app.js\` | UI orchestration and event wiring live here | **safe to edit** for text/layout wiring |\n| Runtime \`state\` | \`js/state.js\` | Shared mutable runtime values | medium caution |\n| Constants/endpoints | \`js/config.js\` | Static datasets and endpoint config | **safe to edit** when changing copy/lists |\n| EV math | \`js/scarabEngine.js\` | Formula ownership is centralized | **do not touch** casually |\n| Regex/token parsing | \`js/regexEngine.js\` | Regex transform and parser behavior | medium-high caution |\n| Market parsing/lookups | \`js/market.js\` | Worker payload normalization lives here | medium-high caution |\n| Token source fallback | \`js/tokenSource.js\` | Source selection and fallback logic | medium caution |\n| Admin/security/routes | \`backend/src/**\` | Auth/session/route authority | **do not touch** casually |\n| Market cache/snapshots | \`workers/market-worker/worker.js\` | Price cache/history/incident authority | **do not touch** casually |\n\n> **Safe first step:** If you are unsure, change copy/labels first, then test, then inspect \`git diff\`.\n### 3.3 What does NOT belong where\n\n- Do not put EV formula changes in \`js/app.js\` (belongs in \`js/scarabEngine.js\`).\n- Do not put mutable runtime flags into \`js/config.js\` (belongs in \`js/state.js\`).\n- Do not bypass market worker by adding direct third-party price source in frontend.\n- Do not merge admin modules into one giant file; keep \`backend/src/admin/*\` split.\n\n---\n\n## 4. FRONTEND COMPLETE GUIDE\n\n### 4.1 Boot flow\n\nFrom \`js/app.js\` startup section:\n\n1. \`atlasLoad()\` restores Atlas tab settings.\n2. \`updateDailySnapshotCopy()\` updates snapshot schedule text.\n3. \`Promise.allSettled([...])\` for:\n   - \`initializeBackendTokenSource(...)\`\n   - \`fetchScarabMetadata()\`\n   - \`fetchCurrentLeague()\`\n4. Then kicks off:\n   - \`fetchMarketScarabPrices()\`\n   - \`fetchPriceHistory()\`\n   - \`fetchAndRenderEVChart()\`\n   - \`fetchAndRenderAtlasTrendPreview()\`\n5. \`initSlider()\`\n6. \`initHashRouting(switchTab)\`\n7. Global/window bindings exposed via \`exposeGlobals\`.\n\n### 4.2 Fetch order and async dependencies\n\n- Prices from market worker are required before full vendor table usefulness.\n- Observed weights are pulled from session API (\`fetchObservedWeights\`) and combined with prices for weighted EV.\n- If weighted data unavailable/immature, app can fall back to harmonic mode.\n\n### 4.3 Market data path to UI\n\n- \`fetchMarketScarabPrices()\` calls worker \`?type=Scarab\`.\n- Response parsed via \`parseWorkerResponse\` (\`js/market.js\`).\n- Prices/images/divine rate placed into \`state\`.\n- \`renderVendorTable()\` and dependent charts/components rerender.\n\n### 4.4 Observed weights path to UI\n\n- \`fetchObservedWeights()\` fetches \`\${POOL_API_URL}/api/aggregate?league=...\`.\n- Reads \`weights\` and \`weightMeta\`.\n- Stores into \`state._observedWeights\`, \`_weightMeta\`, \`_weightSessionCount\`, \`_weightTradeCount\`.\n- Recomputes calibrated weighted rate if market prices already loaded.\n\n### 4.5 EV calculations path\n\n- \`js/scarabEngine.js\`:\n  - \`calcEV(entries)\` harmonic mean logic.\n  - \`computeWeightBasedRate()\` weighted mean = \`sum(weight * price) / 3\`.\n  - \`calcAutoEV()\` chooses weighted or harmonic based on mode + readiness.\n\n### 4.6 Harmonic vs Weighted logic\n\n- Harmonic: conservative baseline when weight maturity is low.\n- Weighted: uses observed output frequencies + market prices.\n- App has recommendation warning text linked to current-league share progress.\n\n### 4.7 Threshold slider effect path\n\n- Slider init: \`initSlider()\`.\n- Threshold conversion helpers: \`sliderValueToThreshold\`, \`thresholdToSliderValue\`.\n- On slider change, \`state.ninjaEvOverride\` updates and calls recalculation/render path.\n- \`recalculateVendorTargets()\` updates threshold and table/regex.\n\n### 4.8 CSV import quantity path\n\n- Wealthy Exile CSV path functions in \`js/app.js\`:\n  - \`importWealthyCSV\`, \`parseWealthyCSV\`, \`renderEstimator\`, related breakdown renderers.\n- Imported quantities affect estimator and vendor/keep counts.\n\n### 4.9 Regex generation path\n\n- \`updateRegexUI(prefix, vendorNames, keepNames)\` builds regex via \`buildRegex\` (\`js/regexEngine.js\`).\n- Over-limit behavior can switch to inverse regex (\`\"! ...\"\`) when needed.\n- Copy path: \`copyRegex(type)\`.\n\n### 4.10 Render/update lifecycle\n\nCore pattern:\n\n- Fetch -> normalize -> mutate \`state\` -> render function(s).\n- Main renderers:\n  - \`renderVendorTable()\`\n  - \`renderAtlas()\`\n  - \`renderAnalysis()\`\n  - \`renderSessionHistory()\`\n  - chart renderers\n\n### 4.11 Tab system\n\n- \`switchTab(tab, skipHash)\` toggles active pane/nav state.\n- Hash routing via \`initHashRouting\` controls deep link/back-forward behavior.\n\n### 4.12 Event binding model\n\n- Most events are wired in \`js/app.js\`.\n- Includes pointer events for slider, tooltip listeners, tab actions, bulk upload handlers, etc.\n- Some handlers are global (window/document), so regression testing must include cross-tab interactions.\n\n### 4.13 State mutation flow\n\n- All shared mutable runtime values in \`js/state.js\`.\n- Write in feature functions, then rerender dependent components.\n- Avoid adding hidden ad-hoc globals.\n\n### 4.14 Common UI components\n\n- Vendor table rows and grouped sections.\n- Threshold card + slider.\n- Regex display/copy controls.\n- Atlas cards and recommendation blocks.\n- Analysis stat cards and weight table.\n- Session logger tables/history details.\n- Bulk analyzer input + parse results.\n\n### 4.15 HOW TO CHANGE (frontend)\n\n#### Labels/text\n\n- FAQ and many static text blocks live in \`js/config.js\` (\`FAQ_SECTIONS\`) and \`index.html\`.\n- Dynamic status labels often in \`js/app.js\`.\n\n#### Cards/stat blocks/tables\n\n- Card/table markup generation is mostly string-template render in \`js/app.js\`.\n- Keep rendering changes in \`js/app.js\`; do not move business logic into templates.\n\n#### Sliders\n\n- Slider behavior belongs to \`initSlider\` + helper functions in \`js/app.js\`.\n- EV math still belongs in \`js/scarabEngine.js\`.\n\n#### Tabs\n\n- Add tab shell in \`index.html\` and wiring in \`switchTab\`/related render functions in \`js/app.js\`.\n\n#### FAQ\n\n- Edit \`FAQ_SECTIONS\` in \`js/config.js\`.\n\n#### Styling\n\n- Frontend CSS variables and classes are in \`index.html\` style blocks.\n- Admin styling is separate (\`backend/src/admin/styles.ts\`).\n#### Mobile layout\n\n- Adjust media queries and responsive structures in \`index.html\` styles + any JS layout assumptions in \`js/app.js\`.\n\n#### Sorting/filters\n\n- Vendor sort/filter logic in \`js/app.js\` (\`setNinjaSort\`, \`setNinjaView\`, analysis filters, bulk filters).\n\n### 4.16 Frontend troubleshooting\n\n- Blank tables:\n  - Check browser console and network for worker request failure.\n  - Confirm \`WORKER_URL\` resolved from \`js/config.js\` for current host environment.\n- Wrong prices:\n  - Check worker response meta (stale/age/source).\n  - Check worker cache freshness in admin health/failure logs.\n- Weighted EV blank:\n  - Check aggregate endpoint response and \`weights\` availability.\n  - Check \`state._weightUnavailableReason\` and current-league maturity.\n- Regex broken:\n  - Verify backend token set endpoint returns \`tokensByName\`.\n  - Check fallback source state (\`backend\`, \`backend-cache\`, \`backend-unavailable\`).\n- Copy button broken:\n  - Inspect \`copyRegex\` path and clipboard permissions in browser.\n- Stale UI:\n  - Hard refresh + confirm worker cache age metadata.\n- Slider not updating:\n  - Validate pointer events not blocked and \`onSliderChange\` path still wired.\n- Mobile broken:\n  - Verify responsive CSS rules and any hardcoded widths in generated table markup.\n- Console errors:\n  - Prioritize first error by stack trace; many downstream errors cascade.\n\n### 4.17 Frontend local testing checklist\n\n- Open each tab once and confirm no runtime exception.\n- Refresh prices and verify vendor list repaints.\n- Toggle harmonic/weighted and observe threshold change.\n- Move slider and ensure ROI/threshold updates.\n- Generate regex and test copy button.\n- Import sample CSV and validate estimator changes.\n- Open analysis tab and verify cards/tables render.\n- Test mobile width and no horizontal overflow where not intended.\n\n### 4.18 Frontend pre-deploy checklist\n\n- Run frontend smoke (\`node scripts/frontend-smoke.mjs\`) if available in environment.\n- Run Playwright E2E (\`npm test\` in \`tests/frontend-e2e\`) if available.\n- Confirm environment guard does not point production frontend to staging endpoints.\n- Confirm theme (light/dark) both render correctly after style edits.\n\n---\n\n## 5. EV / BUSINESS LOGIC GUIDE\n\n### 5.1 EV concept\n\nExpected Value (EV) here means average return per scarab input over many trades.\n\n### 5.2 Harmonic model\n\n- Formula implemented in \`calcEV(entries)\`.\n- Harmonic mean penalizes cheap-common outcomes strongly.\n- Useful conservative baseline when distribution confidence is low.\n\n### 5.3 Weighted model\n\n- Implemented in \`computeWeightBasedRate\`.\n- Uses observed frequency weights and current prices.\n- Core formula: \`sum(weight_i * price_i) / 3\`.\n\n### 5.4 Why both models exist\n\n- Harmonic protects against immature or noisy frequency data.\n- Weighted represents current empirical drop mix better once data matures.\n\n### 5.5 Threshold economics and 3-for-1\n\n- You consume 3 scarabs per trade.\n- Weighted EV threshold is per input scarab after dividing pooled expected return by 3.\n- Feeding higher-cost inputs can reduce or erase edge even when recipe can high-roll.\n\n### 5.6 Volatility / sample size\n\n- Small sessions can deviate heavily from expectation.\n- Long-run behavior matters more than short streaks.\n\n### 5.7 Where formulas live\n\n- \`js/scarabEngine.js\` for frontend runtime EV decisions.\n- Market worker also computes weighted snapshots for historical records (\`workers/market-worker/worker.js\`).\n\n### 5.8 Safe formula modification workflow\n\n1. Change only one formula area at a time.\n2. Add temporary debug outputs for before/after comparison.\n3. Validate with fixed sample data.\n4. Check all downstream screens (vendor table, charts, analysis).\n5. Remove debug output after verification.\n\n### 5.9 Math change validation checklist\n\n- Known sample input produces expected harmonic output.\n- Known weights + prices produce expected weighted output.\n- Threshold slider baseline aligns with selected mode.\n- Session logger threshold mode stores expected value.\n- EV chart values remain sane and monotonic where expected.\n\n---\n\n## 6. REGEX / TOKEN SYSTEM GUIDE\n\n### 6.1 Why regex exists\n\nRegex lets users quickly filter/target scarabs in PoE trade/search workflows.\n\n### 6.2 Constraints and limits\n\n- Token/profile constraints validated in backend (\`poeRegexProfile.ts\`).\n- Frontend handles over-limit fallback/inversion display behavior.\n\n### 6.3 Token generation lifecycle\n\n- Draft generation endpoint: \`/admin/token-drafts/generate\`.\n- Draft report/violations produced in backend token generator.\n- Publish endpoint: \`/admin/token-sets/publish\`.\n- Activate old set endpoint: \`/admin/token-sets/:id/activate\`.\n- Public consumption endpoint: \`/public/token-set/latest\`.\n\n### 6.4 Parser lifecycle in frontend\n\n- Token map loaded from backend in \`initializeBackendTokenSource\`.\n- Regex build/parse via \`js/regexEngine.js\`.\n- Session logger uses parse results to classify matched scarabs.\n\n### 6.5 What breaks when token sets are wrong\n\n- Regex can fail to match intended scarabs.\n- False positives can lead to accidental vendoring.\n- Session logger regex guidance becomes misleading.\n\n### 6.6 Safe modify checklist\n\n- Generate draft and inspect validation report.\n- Check for collisions and missing scarabs.\n- Verify sample regex parse on frontend after publish.\n- Keep rollback token set ready via activate endpoint.\n\n---\n\n## 7. BACKEND ADMIN GUIDE\n\n### 7.1 Route map (verified)\n\n> **Beginner note:** Read routes as \`METHOD + PATH + permission level\`.\n\n#### Route families at a glance\n\n| Family | Typical paths | Purpose | Risk level |\n|---|---|---|---|\n| **Auth/owner** | \`/admin/auth/*\`, \`/admin/owner/*\` | login/session/owner controls | high |\n| Scarab | \`/admin/scarabs*\`, \`/public/scarabs/metadata\` | scarab lifecycle + public metadata | medium-high |\n| Token | \`/admin/token-*\`, \`/public/token-set/latest\` | token draft/publish/activate | high |\n| Ops/audit | \`/admin/ops/*\`, \`/admin/audit-logs\` | backups/retry/failures/environment | high |\n\nAuth/owner:\n\n- \`POST /admin/auth/login\`\n- \`POST /admin/auth/logout\`\n- \`GET /admin/auth/session\`\n- \`GET /admin/healthz\`\n- \`POST /admin/owner/ping\`\n- \`POST /admin/owner/change-password\`\n\nScarab:\n\n- \`GET /public/scarabs/metadata\`\n- \`GET /admin/scarabs/token-inputs\`\n- \`GET /admin/scarabs\`\n- \`POST /admin/scarabs\`\n- \`GET /admin/scarabs/:id\`\n- \`PUT /admin/scarabs/:id\`\n- \`DELETE /admin/scarabs/:id\`\n- \`POST /admin/scarabs/:id/retire\`\n- \`POST /admin/scarabs/:id/reactivate\`\n- \`GET /admin/scarabs/:id/versions\`\n\nToken:\n\n- \`GET /admin/token-drafts/latest\`\n- \`POST /admin/token-drafts/generate\`\n- \`POST /admin/token-sets/publish\`\n- \`POST /admin/token-sets/import-legacy\`\n- \`POST /admin/token-sets/:id/activate\`\n- \`DELETE /admin/token-sets/:id\`\n- \`GET /admin/token-sets\`\n- \`GET /admin/token-sets/:id\`\n- \`GET /public/token-set/latest\`\n\nOps/audit:\n\n- \`GET /admin/audit-logs\`\n- \`GET /admin/ops/backups\`\n- \`POST /admin/ops/backups/run\`\n- \`POST /admin/ops/backups/restore\`\n- \`GET /admin/ops/environment\`\n- \`GET /admin/ops/cloudflare-usage\`\n- \`GET /admin/ops/failure-logs\`\n- \`POST /admin/ops/retry\`\n- \`GET /admin/ops/bulk-name-map\`\n- \`POST /admin/ops/bulk-name-map\`\n- \`GET /admin/ops/bulk-mismatch-log\`\n- \`DELETE /admin/ops/bulk-mismatch-log\`\n### 7.2 Auth/session/CSRF model\n\n- Session cookie and CSRF cookie are issued at login.\n- Mutation methods require CSRF header check (\`x-csrf-token\` matching cookie model).\n- Session expiration and rotation enforced via security helpers.\n- Rate limiting uses \`rate_limits\` table.\n\n### 7.3 Permissions\n\n- Role checks through \`hasRequiredRole\` and route role requirements.\n- Owner-only endpoints exist (\`/admin/owner/*\`).\n\n### 7.4 Admin UI composition\n\n- Markup: \`backend/src/admin/view.ts\`\n- Styles: \`backend/src/admin/styles.ts\`\n- Script modules: \`backend/src/admin/scripts/*.ts\`\n- Composition entry: \`backend/src/admin/ui.ts\`\n\n### 7.5 Audit and mutation safety\n\n- Audit records in \`audit_logs\` table.\n- Sensitive state changes should route through audited handlers.\n\n### 7.6 How to add an endpoint safely\n\n1. Add route handling in correct \`backend/src/routes/*\` file.\n2. Add auth/role/CSRF checks for mutation routes.\n3. Add repository method if new DB access is required.\n4. Add admin UI wiring only if needed.\n5. Add tests/typecheck.\n\n### 7.7 How to debug route issues\n\n- 404: check route path and method mismatch.\n- 401: missing/expired session.\n- 403: role failure or CSRF invalid.\n- 429: rate limit hit.\n- 500: inspect worker logs and DB binding configuration.\n---\n\n## 8. SCARAB MANAGER GUIDE\n\n### 8.1 Lifecycle\n\n- Create scarab (\`POST /admin/scarabs\`)\n- Edit (\`PUT /admin/scarabs/:id\`)\n- Retire (\`POST /admin/scarabs/:id/retire\`)\n- Reactivate (\`POST /admin/scarabs/:id/reactivate\`)\n- Delete (guarded; avoid deleting historical identities casually)\n\n### 8.2 Canonical naming importance\n\n- Name mismatches break market mapping, weight mapping, token entries, and UI grouping.\n- Prefer explicit versioned text updates instead of ad-hoc rename hacks.\n\n### 8.3 Season transition safety\n\n- Validate active scarab set per league before launch.\n- Retire obsolete items intentionally.\n- Regenerate token draft and validate collisions/coverage.\n\n### 8.4 Safe update workflow\n\n1. Draft list changes in admin.\n2. Validate impact in token input preview.\n3. Regenerate token draft.\n4. Verify frontend metadata endpoint output.\n5. Publish with rollback candidate documented.\n\n---\n\n## 9. COMMUNITY DATA / SESSION INTAKE GUIDE\n\n### 9.1 What it does\n\n- Collects session submissions.\n- Aggregates observed outputs into weights.\n- Supplies weights/metadata to frontend and market worker.\n\n### 9.2 Endpoint families (verified in \`workers/scarabev-api/index.js\`)\n\n- Public:\n  - \`GET /api/aggregate\`\n  - \`POST /api/sessions\`\n  - \`GET /health\`\n- Admin/session moderation:\n  - \`/admin/sessions/*\` routes (review queues, analytics, reruns, approve/reject/delete)\n- Maintenance:\n  - \`POST /api/sync-repair\`\n  - \`POST /api/recalculate-globals\`\n  - \`GET /api/admin/weights-debug\`\n\n### 9.3 Intake logic\n\n- L1/L2 scoring constants and thresholds are defined in worker source.\n- States include auto-approve, review pending, research, reject classes.\n- Fingerprint logic exists for dedupe/anomaly controls.\n\n### 9.4 Storage and trust model\n\n- Sessions are stored in D1.\n- Aggregate totals/received-by-scarab persisted in D1.\n- Intake event log table \`session_intake_events\` created/used.\n- Base table creation for \`sessions\`, \`aggregate\`, \`rate_limit\` not found in this repository as SQL migration files.\n- **NEEDS OWNER CONFIRMATION**: canonical schema bootstrap path for session API DB.\n\n### 9.5 Outlier/moderation philosophy\n\n- High-risk/abnormal submissions can be held for review.\n- Not all accepted data has equal confidence early in a league.\n\n### 9.6 How to inspect/fix bad data\n\n1. Use admin session review endpoints/UI.\n2. Identify suspicious fingerprints or drift reasons.\n3. Move state (approve/reject/research) with admin note.\n4. Recalculate globals if needed.\n5. Verify aggregate endpoint changed as expected.\n\n---\n\n## 10. MARKET WORKER COMPLETE GUIDE\n\n### 10.1 Responsibilities\n\n- Pull upstream market prices (poe.ninja).\n- Cache and serve scarab/currency/current-league data.\n- Produce EV/price/atlas history snapshots.\n- Run retry queues and snapshot backstop.\n- Track failures/incidents.\n- Provide backup export/import/smoke test endpoints.\n\n### 10.2 Fetch/parse/normalize flow\n\n1. Request comes to \`handleRequest\` with \`type\` query param.\n2. For market types, fetch/refresh logic uses cached state and staleness policy.\n3. Upstream fetch via timeout wrapper.\n4. Parse item lines and map to scarab names.\n5. Store/return payload with metadata.\n\n### 10.3 Freshness and stale windows\n\nPolicy constants:\n\n- \`currentLeague\`: fresh 10m, max stale 24h\n- \`marketCurrent\`: fresh 5m, max stale 30m\n- \`marketStandard\`: fresh 60m, max stale 6h\n\n### 10.4 Cron lifecycle\n\nFrom \`wrangler.toml\` and worker code:\n\n- \`*/5 * * * *\`:\n  - refresh current league bundle\n  - scheduled standard/current refresh substeps by minute\n  - run pending snapshot retry\n  - run snapshot kickoff backstop\n- \`0 18 * * *\`:\n  - daily snapshot and failure log pruning (if enabled)\n\n### 10.5 Snapshot lifecycle\n\n- Writes required sets: EV, Atlas EV, Price history (\`REQUIRED_SNAPSHOT_WRITES\`).\n- Tracks status and retry keys in KV.\n- Inline retry attempts per run + deferred retry queue.\n- Max retry attempts before unresolved closure.\n\n### 10.6 Failure log families\n\n- Failure logs are KV entries under \`market-cache-v2:failure-log:*\`.\n- Incident state keys under \`market-cache-v2:incident-state:*\`.\n\n### 10.7 Manual retry system\n\n- \`type=ManualRetry\` supports actions including snapshot retry/run and failure-log maintenance paths.\n- Uses admin token headers (\`x-admin-token\` / \`x-manual-retry-token\`) for protected actions.\n\n### 10.8 Backup export/import/smoke\n\n- \`type=BackupExport\` summary/full modes.\n- \`type=BackupImport\` writes scoped key sets.\n- \`type=BackupSmokeStatus\` and \`type=BackupSmokeTest\` verify recoverability.\n\n### 10.9 Bulk map support\n\n- \`type=BulkNameMap\` get/set canonical name mapping.\n- \`type=BulkMismatchLog\` collect and clear unresolved OCR/CSV mismatches.\n\n### 10.10 How frontend/admin consume outputs\n\n- Frontend uses worker for:\n  - current prices/currency/current league\n  - price history and EV history chart feeds\n  - bulk name map/mismatch logging\n- Admin backend ops routes proxy/selectively consume worker endpoints for failure logs/retries/backup operations.\n\n### 10.11 Market worker troubleshooting\n\n- Stale market data:\n  - Check runtime config endpoint (\`type=RuntimeConfig\`) and cron enable flags.\n  - Check cache metadata age/dataState.\n- Failed fetches:\n  - Inspect failure logs endpoint and upstream timeout errors.\n- Corrupted cache:\n  - Use backup import or clear specific keys cautiously.\n- Retry loops:\n  - Inspect \`snapshot-retry\` key state and max attempt exhaustion.\n- Bad snapshots:\n  - Check required write completeness and guard errors.\n- Name mismatches:\n  - Use BulkNameMap and mismatch logs to align parser names.\n\n---\n\n## 11. DATA / STORAGE GUIDE\n\n### 11.1 Backend D1 tables (from \`backend/migrations/*.sql\`)\n\n| Table | Purpose | Authority | Safe to delete? | Recovery |\n|---|---|---|---|---|\n| \`migration_meta\` | migration tracking | operational metadata | no | rerun migrations + restore backup |\n| \`admin_users\` | admin credentials | authoritative | no | restore from backup/manual bootstrap |\n| \`auth_sessions\` | active sessions | temporary/auth | yes (forces relogin) | recreate by logging in |\n| \`rate_limits\` | per-scope rate counters | operational | yes (temporary) | auto rebuild |\n| \`audit_logs\` | admin action history | authoritative audit | no (unless accepted loss) | restore backup |\n| \`leagues\` | league records | authoritative | no | restore/migrate |\n| \`seasons\` | season records | authoritative | no | restore/migrate |\n| \`scarabs\` | scarab lifecycle | authoritative | no | restore/migrate |\n| \`scarab_text_versions\` | version history | authoritative history | no | restore/migrate |\n| \`draft_token_sets\` | draft token generations | operational | usually no | regenerate possible but history lost |\n| \`draft_token_entries\` | draft token rows | operational | usually no | regenerate possible but history lost |\n| \`draft_token_reports\` | generation reports | operational/debug | yes with caution | regenerate drafts |\n| \`token_sets\` | published token set history | authoritative | no | restore backup |\n| \`token_set_entries\` | published token rows | authoritative | no | restore backup |\n| \`backup_snapshots\` | backup run metadata | operational history | no (recommended keep) | rebuild partially from new runs |\n\n### 11.2 Session API D1 tables\n\nVerified in code:\n\n- \`session_intake_events\` table is created by worker boot path.\n- \`sessions\`, \`aggregate\`, \`rate_limit\` are used heavily.\n- **NEEDS OWNER CONFIRMATION**: exact bootstrap migration files for these base tables (not present in repo).\n\n### 11.3 KV key families (market worker)\n\n- \`market-cache-v2:current-league\`\n- \`market-cache-v2:snapshot-*\`\n- \`market-cache-v2:failure-log:*\`\n- \`ev-history-*\`, \`atlas-ev-history-*\`, \`price-history-*\`, \`price-history-backup-*\`\n- \`market-cache-v2:backup-smoke-*\`\n- \`market-cache-v2:bulk-name-map\`\n- \`market-cache-v2:bulk-mismatch-log\`\n\nDeletion safety:\n\n- Deleting cache/history keys can break charts and operational visibility.\n- Deleting snapshot/failure keys can hide incidents.\n- Delete only with explicit reason and backup export first.\n\n### 11.4 R2 backup objects (backend)\n\n- Backend writes backup snapshots to R2 bucket configured by env.\n- Object prefix controlled by \`BACKUP_OBJECT_PREFIX\`.\n- Deleting objects without replacement removes restore capability.\n---\n\n## 12. INFRASTRUCTURE / HOSTING GUIDE\n\n### 12.1 Verified topology\n\n- Frontend static site: appears GitHub Pages based on \`robots.txt\` and \`sitemap.xml\` pointing to \`https://doobyy.github.io/ScarabEV/\`.\n- Backend admin/API worker: Cloudflare Workers (\`backend/wrangler.*.toml\`).\n- Market worker: Cloudflare Worker (\`workers/market-worker/wrangler*.toml\`).\n- Session/community API: Cloudflare Worker (\`workers/scarabev-api/wrangler*.toml\`).\n- Data stores: Cloudflare D1, KV, R2.\n\n### 12.2 Environment bindings (verified)\n\nBackend production/staging:\n\n- D1 binding \`DB\`\n- R2 binding \`BACKUP_R2\`\n- env vars include \`MARKET_WORKER_URL\`, \`SESSION_API_BASE_URL\`, backup controls, session TTL/rotation\n\nMarket worker:\n\n- KV binding \`EV_HISTORY\`\n- service binding \`SCARABEV_API\`\n- \`AGGREGATE_API_URL\` env var with production/staging guard checks\n\nSession API:\n\n- D1 binding \`DB\`\n- ADMIN_KEY secret required for admin endpoints\n\n### 12.3 Important infra warnings\n\n- \`workers/scarabev-api/wrangler.staging.toml\` has placeholder D1 id (\`REPLACE_WITH_STAGING_D1_DATABASE_ID\`).\n- \`workers/market-worker/wrangler.staging.toml\` has placeholder KV namespace id and notes for service binding.\n- **NEEDS OWNER CONFIRMATION**: actual current staging resources and whether staging deployment is currently active.\n\n---\n\n## 13. LOCAL DEVELOPMENT GUIDE\n\n### 13.1 Requirements\n\n- Node.js + npm\n- Wrangler CLI (or \`npx wrangler\`)\n- Cloudflare account access for real worker dev with bindings\n\n### 13.2 Clean machine setup\n\n1. Clone repo.\n2. \`cd backend\`\n3. \`npm install\`\n4. Typecheck backend: \`npm run typecheck\`\n\n### 13.3 Run backend worker locally\n\nFrom \`backend/\`:\n\n- \`npm run dev\`\n\n### 13.4 Run frontend locally\n\nNo root package.json is present.\n\nOptions:\n\n- Use a static server (for example VSCode Live Server or \`python -m http.server\`) from repo root.\n- Ensure frontend endpoint environment detection behaves as expected for local host.\n\n### 13.5 Run market worker locally\n\n- Worker file exists but no dedicated package scripts in \`workers/market-worker\`.\n- Use wrangler directly from that folder with appropriate config and bindings.\n- **NEEDS OWNER CONFIRMATION**: preferred local command set and local binding strategy.\n\n### 13.6 Run session API locally\n\n- \`workers/scarabev-api/index.js\` with wrangler config.\n- Requires D1 binding and admin secret for admin endpoints.\n- **NEEDS OWNER CONFIRMATION**: canonical local DB bootstrap for base tables.\n\n### 13.7 Tests/build/typecheck\n\n- Backend tests: \`cd backend && npm test\`\n- Backend typecheck: \`cd backend && npm run typecheck\`\n- Frontend smoke: \`node scripts/frontend-smoke.mjs\`\n- E2E: \`cd tests/frontend-e2e && npm install && npm test\`\n\n### 13.8 Common setup issues\n\n- Missing D1/KV/R2 bindings cause runtime failures.\n- Staging placeholder IDs not replaced => deploy/dev failures.\n- Session API base tables missing => submission/aggregate endpoint errors.\n\n---\n\n## 14. DEPLOYMENT GUIDE\n\n### 14.1 Staging deploy\n\nBackend (verified script):\n\n- \`cd backend\`\n- \`npm run deploy:staging\`\n\nMarket/session worker staging:\n\n- wrangler deploy with staging configs after replacing placeholder IDs.\n- **NEEDS OWNER CONFIRMATION**: exact staging deploy commands currently used for these two workers.\n\n### 14.2 Production deploy\n\nBackend:\n\n- \`cd backend\`\n- \`npm run deploy:production\`\n\nMarket/session worker production:\n\n- wrangler deploy from each worker folder using production config.\n- **NEEDS OWNER CONFIRMATION**: exact operational scripts used by owner.\n\nFrontend:\n\n- repo implies GitHub Pages source-based deployment.\n- **NEEDS OWNER CONFIRMATION**: exact production publish trigger (branch/action/manual).\n\n### 14.3 Verification immediately after deploy\n\n- Backend \`/admin/healthz\` returns healthy.\n- Admin login works and session persists.\n- Frontend loads and tabs switch cleanly.\n- Market prices refresh and not stale beyond policy.\n- Aggregate endpoint returns data.\n- Regex token source status is backend/backend-cache.\n- Backup status endpoint and failure logs render.\n\n### 14.4 Rollback workflow\n\n- Re-deploy last known good worker build/config.\n- For token regressions: reactivate previous token set in admin.\n- For data corruption: restore from backup snapshot.\n- Keep incident note in audit/history after rollback.\n\n---\n\n## 15. OPERATIONS MANUAL\n\n### 15.1 Daily\n\n- Open admin Health.\n- Confirm:\n  - Admin API healthy\n  - Public token endpoint healthy\n  - League cache fresh\n  - Snapshot status healthy\n  - Backup smoke status healthy\n- Check failure logs for new unresolved incidents.\n\n### 15.2 Weekly\n\n- Run backup snapshot and verify external key present.\n- Review recent failure logs trends.\n- Review audit logs for unexpected mutation actions.\n- Review bulk mismatch log growth.\n\n### 15.3 Monthly\n\n- Perform restore drill in non-production environment.\n- Confirm secrets and critical IDs are still documented and accessible.\n- Prune outdated operational notes.\n\n### 15.4 League launch checklist\n\n- Verify current league detection.\n- Review scarab list lifecycle (retire/reactivate/new).\n- Generate and validate token draft, then publish.\n- Confirm aggregate/weights maturity behavior (harmonic recommendation expected early).\n- Run end-to-end sanity across vendor/atlas/logger/analysis.\n\n---\n\n## 16. TROUBLESHOOTING ENCYCLOPEDIA\n\nFormat: IF X -> CHECK Y -> FIX Z\n\n| Symptom | First checks | Typical fix direction |\n|---|---|---|\n| Login fails | \`/admin/auth/login\` response, user exists, password hash | reset via owner change-password route |\n| 401 in admin UI | session cookie expiration | re-login and inspect session TTL config |\n| 403 on mutation | **CSRF** header/cookie pairing | refresh session and ensure \`x-csrf-token\` sent |\n| Blank frontend vendor table | worker request + console first error | restore **Market Worker** health and refresh |\n| Wrong EV values | mode (harmonic/weighted), weights payload, price inputs | correct data path before formula changes |\n| Stale cache warnings | worker policy age + cron enabled flags | re-enable cron or run manual retry |\n| Token 404/unavailable | \`/public/token-set/latest\` + publish status | publish or activate valid set |\n| Backup restore fails | backup payload scope + **R2** key availability | run smaller scoped restore then full |\n| Snapshot failures repeat | failure logs + retry state | resolve root cause, then manual retry |\n| Button no-op in frontend | exposed binding + first runtime error | fix first thrown exception |\n| Rate limit blocks | rate limit tables/config windows | adjust only if abuse-safe |\n| Data drift/weird weights | intake states/outliers + review queue | moderate suspicious sessions + recalc globals |\n| League transition weirdness | league cache + scarab/token coverage | sync lifecycle + republish token |\n\n> **Do not do this:** Do not patch symptoms by adding a new market source. Preserve **worker-first** architecture.\n\n---\n## 17. DISASTER RECOVERY MANUAL\n\nAssumption: original maintainer unavailable.\n\n> **Warning:** This section is for urgent recovery. Move slowly, preserve evidence, and keep a clear **rollback** path.\n\n### 17.1 Critical credentials/info to preserve\n\n- Cloudflare account access.\n- Worker names and wrangler configs.\n- **D1** database IDs.\n- **KV** namespace IDs.\n- **R2** bucket names.\n- Secrets (\`ADMIN_KEY\`, auth/token salts/secrets used by backend).\n- Git repository access.\n\n### 17.2 Recover from zero (high-level)\n\n| Phase | Action | Evidence to capture |\n|---|---|---|\n| 1 | Get repo access and clone | commit hash and branch |\n| 2 | Recreate workers from wrangler configs | worker URLs and version IDs |\n| 3 | Recreate missing **D1/KV/R2** resources | new resource IDs |\n| 4 | Update wrangler files with new IDs | diff + config snapshot |\n| 5 | Restore D1 and KV/R2 backup payloads | restore logs/output |\n| 6 | Redeploy backend, market worker, session API, frontend | deploy command outputs |\n| 7 | Run smoke tests on major tabs and admin health | pass/fail checklist |\n\n### 17.3 Recreate components\n\n- Backend worker + **D1** + **R2** from \`backend/wrangler.*.toml\`.\n- **Market Worker** + **KV** + service binding from \`workers/market-worker/wrangler*.toml\`.\n- Session API + **D1** from \`workers/scarabev-api/wrangler*.toml\`.\n\n### 17.4 Restore data\n\n- Backend **D1**: restore from SQL backup/export.\n- Session **D1**: restore from historical export.\n- Market **KV**: use backup import endpoint payloads where available.\n- **R2**: ensure snapshot objects restored and accessible.\n\n### 17.5 Final smoke test\n\n- Admin **auth**/**session** works.\n- Health all green except known tolerated warnings.\n- Frontend vendor recommendations \`render\` and update.\n- Session submit and aggregate update path works.\n- Token latest endpoint non-empty and regex generation works.\n\n> **Owner reminder:** If any high-risk step is uncertain, mark **NEEDS OWNER CONFIRMATION** before proceeding.\n\n---\n## 18. KNOWN QUIRKS / TRIBAL KNOWLEDGE REPLACEMENT\n\n- Early league weighted guidance can look unstable; harmonic is intended default until maturity improves.\n- Some warnings are informational (stale but still served) vs hard failures; read status metadata not just color badge.\n- Clearing caches blindly can remove useful forensic context.\n- Token changes can appear frontend-side only after cache refresh or reload.\n- Bulk OCR path can produce partial CSV; app includes partial-parse detection and mismatch logging.\n- Dark/light theme regressions can happen if shared styles are unintentionally changed; keep mode-specific tokens separated.\n\n---\n\n## 19. SAFE CHANGE PLAYBOOKS\n\n### 19.1 Add UI feature\n\n- Touch: \`index.html\`, \`js/app.js\`, optionally \`js/state.js\`/\`js/config.js\`.\n- Avoid: EV logic files unless truly needed.\n- Validate: tab load, no console errors, mobile behavior.\n- Rollback: revert frontend commit.\n\n### 19.2 Change EV formula\n\n- Touch: \`js/scarabEngine.js\` (and worker snapshot math only if required parity).\n- Avoid: direct formula logic in render code.\n- Validate: known sample cases + chart + threshold + analysis.\n- Rollback: restore previous formula commit.\n\n### 19.3 Add new scarab\n\n- Touch: scarab lifecycle in backend (\`/admin/scarabs\`), plus canonical list locations.\n- Avoid: random rename hacks in frontend only.\n- Validate: appears in metadata, market mapping, token draft coverage.\n- Rollback: retire/reactivate/restore previous text version.\n\n### 19.4 Modify token behavior\n\n- Touch: \`backend/src/tokens/*\`, token routes, possibly \`js/regexEngine.js\` for client parser changes.\n- Validate: draft report clean, publish success, frontend regex parse/copy.\n- Rollback: activate prior token set.\n\n### 19.5 Add admin route\n\n- Touch: \`backend/src/routes/*\` + repository/security where required.\n- Validate: auth, role, CSRF, audit logs.\n- Rollback: remove route and redeploy.\n\n### 19.6 Add worker query type\n\n- Touch: \`workers/market-worker/worker.js\` routing + logic function.\n- Validate: response schema, auth if mutation, failure logging.\n- Rollback: redeploy previous worker.\n\n### 19.7 Change schema\n\n- Touch: backend migration SQL or session API schema path.\n- Validate: migration forward on staging, no production data loss.\n- Rollback: planned reverse migration or restore backup.\n\n### 19.8 Update docs\n\n- Touch: admin documentation source content and this bible.\n- Validate: search/table-of-contents anchors and readability in both themes.\n- Rollback: revert doc-only commit.\n\n---\n\n## 20. AI CONTINUITY / FUTURE ASSISTANT HANDOFF\n\n### 20.A Plain-English project identity\n\nScarabEV is a market-informed EV decision system for PoE scarab vendor strategy, with admin operations and community-data weighting.\n\n### 20.B Architecture truths future AI must obey\n\n- Market data path is worker-first.\n- Formula ownership is explicit (\`js/scarabEngine.js\`).\n- Security boundaries in backend are non-negotiable.\n- Storage changes require migration and rollback planning.\n\n### 20.C File ownership routing map\n\n- UI/events: \`js/app.js\`\n- state: \`js/state.js\`\n- constants/endpoints: \`js/config.js\`\n- EV: \`js/scarabEngine.js\`\n- regex: \`js/regexEngine.js\`\n- market parse: \`js/market.js\`\n- token source fallback: \`js/tokenSource.js\`\n- backend/admin/auth/routes: \`backend/src/**\`\n- market cache/snapshot/failure/backup: \`workers/market-worker/worker.js\`\n\n### 20.D Guardrails for any future AI\n\nNEVER:\n\n- broad-refactor for small request\n- bypass worker trust path casually\n- weaken security checks\n- silently alter EV formulas\n- pretend to verify without reading files\n\nALWAYS:\n\n- read \`AGENTS.md\` first\n- inspect real files before proposing edits\n- make surgical patches\n- report exactly what changed\n- give validation steps\n- mark uncertainty as \`NEEDS OWNER CONFIRMATION\`\n\n### 20.E UNIVERSAL FUTURE AI PROMPT (copy/paste)\n\n\`\`\`text\nYou are maintaining ScarabEV. Read AGENTS.md first and treat it as law.\nBefore editing, inspect relevant files and summarize current behavior from code.\nMake the smallest surgical change possible.\nDo not refactor unrelated code.\nDo not bypass market worker trust model.\nDo not change EV math unless explicitly requested.\nDo not weaken auth/session/CSRF/security controls.\nAfter edits, list changed files, why each was changed, and exact validation steps.\nIf any detail is not verifiable from repo/config, label it: NEEDS OWNER CONFIRMATION.\n\`\`\`\n\n### 20.F AGENTS.md MIRROR (recommended copyable content)\n\n\`\`\`markdown\n# AGENTS.md (Recommended Mirror)\n\n1. Read this file before any edits.\n2. Route changes by ownership:\n- UI/events -> js/app.js\n- state -> js/state.js\n- constants/endpoints -> js/config.js\n- EV math -> js/scarabEngine.js\n- regex -> js/regexEngine.js\n- market parsing -> js/market.js\n- token source fallback -> js/tokenSource.js\n- backend/admin/security/routes -> backend/src/**\n- market cache/snapshot/failure/backups -> workers/market-worker/worker.js\n3. Keep patches surgical.\n4. No broad refactors unless explicitly requested.\n5. Preserve behavior unless change requested.\n6. Do not bypass worker-first market architecture.\n7. Do not weaken auth/session/CSRF/rate limits.\n8. For UI iteration, revert bad edit first; do not stack hacks.\n9. State uncertainty honestly as NEEDS OWNER CONFIRMATION.\n10. Always provide validation steps after changes.\n\`\`\`\n\n### 20.G How humans judge AI output safely\n\n- Did it read relevant files first?\n- Did it touch only expected files?\n- Did it explain impact and validation clearly?\n- Did it avoid architecture/security/math drift?\n- Did it admit uncertainty instead of guessing?\n\n### 20.H Warning signs unsafe AI is freelancing\n\n- Suggests major refactor for tiny UI fix.\n- - Adds new data source to \"quickly fix\" worker issues.\n- Changes formulas without explicit request.\n- - Says \"done\" with no verification steps.\n- Modifies security code while working on styling.\n\n### 20.I First-contact workflow for onboarding new AI\n\n1. Read-only architecture summary request first.\n2. No-edit code walkthrough second.\n3. Tiny non-critical patch third.\n4. Only then production-impacting changes.\n\n### 20.J Core principle\n\nThe AI serves the system.  \nThe system does not bend to the AI.\n\n---\n\n## 21. BEGINNER GLOSSARY\n\n### Core math and recommendation terms\n\n- **EV** - Expected value (average long-run return).\n- **Harmonic EV** - Conservative EV using harmonic mean.\n- **Weighted EV** - EV using observed output frequencies and prices.\n- **Threshold** - Max cost per scarab to vendor.\n\n### Runtime and architecture terms\n\n- **Frontend** - Browser app (\`index.html\`, \`js/*\`) where UI is shown.\n- **state** - In-memory runtime values that \`render\` functions read.\n- **render** - Turning current \`state\` into visible UI output.\n- **fetch** - Browser/worker network request used to load data.\n- **Market Worker** - Cloudflare worker for market cache/snapshots/history.\n- **Backend** - Admin/auth/token/scarab operations worker.\n\n### Storage terms\n\n- **D1** - Cloudflare SQLite-like database.\n- **KV** - Key-value storage for caches/history/state.\n- **R2** - Object storage for backup files.\n\n### Security and operations terms\n\n- **auth** - Identity/session controls for who can do protected actions.\n- **CSRF** - Forgery protection token check for mutations.\n- **Session** - Authenticated login state.\n- **rollback** - Returning to known-good code/data after a bad change.\n- **source of truth** - Canonical data authority for a decision.\n- **Stale data** - Cached data older than freshness policy.\n- **Retry queue** - Deferred reattempt state for failed snapshot writes.\n\n---\n## 22. OWNER CONFIRMATION CHECKLIST\n\nItems not fully verifiable from repository alone:\n\n1. **Session API base schema bootstrap path** for \`sessions\`, \`aggregate\`, \`rate_limit\` tables.\n2. **Current real staging resource IDs** for session API D1 and market worker KV/service in staging configs.\n3. **Canonical production deploy command(s)** for market worker and session API (if wrappers/scripts are used outside repo).\n4. **Canonical frontend deployment trigger** (exact GitHub Pages workflow/branch/manual process).\n5. **Secret inventory and rotation policy** beyond visible wrangler config placeholders/comments.\n6. **Disaster recovery source of truth for off-repo credential vault and backup retention SOP**.\n\n---\n\n## 23. RECOMMENDED FUTURE DOC SPLITS\n\nAfter this bible is accepted, split into maintainable docs:\n\n1. \`docs/frontend-manual.md\`\n2. \`docs/backend-admin-ops-manual.md\`\n3. \`docs/market-worker-runbook.md\`\n4. \`docs/session-intake-moderation-manual.md\`\n5. \`docs/ev-formula-spec.md\`\n6. \`docs/disaster-recovery.md\`\n7. \`docs/ai-handoff-guardrails.md\`\n8. \`docs/deployment-checklists.md\`\n\nThis file should remain the top-level index and emergency fallback reference.\n";

function docsEscHtml(value){
  return String(value||'')
    .replace(/&/g,'&amp;')
    .replace(/</g,'&lt;')
    .replace(/>/g,'&gt;')
    .replace(/\"/g,'&quot;')
    .replace(/'/g,'&#39;');
}

function docsStripHtml(value){
  return String(value||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();
}

function docsSlug(title){
  return String(title||'')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g,'-')
    .replace(/^-+|-+$/g,'') || 'section';
}

function docsInline(md){
  let out = docsEscHtml(md || '');
  out = out.replace(/\[([^\]]+)\]\((https?:[^)]+)\)/g, '<a href="$2" target="_blank" rel="noopener noreferrer">$1</a>');
  out = out.replace(/\*\*([^*]+)\*\*/g, '<b>$1</b>');
  out = out.replace(/\`([^\`]+)\`/g, '<code>$1</code>');
  return out;
}

function markdownToHtml(md){
  const lines = String(md||'').replace(/\r\n/g,'\n').split('\n');
  const html = [];
  let inCode = false;
  let codeLines = [];
  let listType = null;
  let listItems = [];
  let para = [];
  let quoteLines = [];
  let tableLines = [];

  const flushPara = ()=>{
    if (!para.length) return;
    html.push('<p>'+docsInline(para.join(' '))+'</p>');
    para = [];
  };
  const flushList = ()=>{
    if (!listItems.length) return;
    const tag = listType === 'ol' ? 'ol' : 'ul';
    html.push('<'+tag+'>'+listItems.map((item)=>'<li>'+docsInline(item)+'</li>').join('')+'</'+tag+'>');
    listType = null;
    listItems = [];
  };
  const flushCode = ()=>{
    if (!inCode) return;
    html.push('<pre class="docs-code"><code>'+docsEscHtml(codeLines.join('\n'))+'</code></pre>');
    inCode = false;
    codeLines = [];
  };
  const flushQuote = ()=>{
    if (!quoteLines.length) return;
    html.push('<blockquote><p>'+docsInline(quoteLines.join(' '))+'</p></blockquote>');
    quoteLines = [];
  };
  const flushTable = ()=>{
    if (!tableLines.length) return;
    const splitRow = (row)=>row.trim().replace(/^\||\|$/g,'').split('|').map((cell)=>cell.trim());
    const isDivider = (row)=>/^(\|\s*:?-{3,}:?\s*)+\|?$/.test(row.trim());
    if (tableLines.length >= 2 && isDivider(tableLines[1])) {
      const headerCells = splitRow(tableLines[0]);
      const bodyRows = tableLines.slice(2).map(splitRow).filter((cells)=>cells.length);
      const thead = '<thead><tr>'+headerCells.map((cell)=>'<th>'+docsInline(cell)+'</th>').join('')+'</tr></thead>';
      const tbody = bodyRows.length
        ? '<tbody>'+bodyRows.map((row)=>'<tr>'+row.map((cell)=>'<td>'+docsInline(cell)+'</td>').join('')+'</tr>').join('')+'</tbody>'
        : '';
      html.push('<table class="docs-table">'+thead+tbody+'</table>');
    } else {
      html.push('<pre class="docs-code"><code>'+docsEscHtml(tableLines.join('\n'))+'</code></pre>');
    }
    tableLines = [];
  };

  for (const raw of lines) {
    const line = raw || '';

    if (line.trim().startsWith('\`\`\`')) {
      flushPara();
      flushList();
      flushQuote();
      flushTable();
      if (inCode) flushCode();
      else { inCode = true; codeLines = []; }
      continue;
    }

    if (inCode) {
      codeLines.push(line);
      continue;
    }

    const h4 = line.match(/^####\s+(.+)$/);
    if (h4) {
      flushPara(); flushList(); flushQuote(); flushTable();
      html.push('<h4>'+docsInline(h4[1])+'</h4>');
      continue;
    }

    const h3 = line.match(/^###\s+(.+)$/);
    if (h3) {
      flushPara(); flushList(); flushQuote(); flushTable();
      html.push('<h3>'+docsInline(h3[1])+'</h3>');
      continue;
    }

    const ol = line.match(/^\s*\d+\.\s+(.+)$/);
    if (ol) {
      flushPara(); flushQuote(); flushTable();
      if (listType && listType !== 'ol') flushList();
      listType = 'ol';
      listItems.push(ol[1]);
      continue;
    }

    const ul = line.match(/^\s*-\s+(.+)$/);
    if (ul) {
      flushPara(); flushQuote(); flushTable();
      if (listType && listType !== 'ul') flushList();
      listType = 'ul';
      listItems.push(ul[1]);
      continue;
    }

    const bq = line.match(/^\s*>\s?(.*)$/);
    if (bq) {
      flushPara(); flushList(); flushTable();
      quoteLines.push(bq[1]);
      continue;
    }

    if (/^\|.*\|\s*$/.test(line.trim())) {
      flushPara(); flushList(); flushQuote();
      tableLines.push(line.trim());
      continue;
    }

    if (!line.trim()) {
      flushPara(); flushList(); flushQuote(); flushTable();
      continue;
    }

    flushQuote();
    flushTable();
    para.push(line.trim());
  }

  flushPara();
  flushList();
  flushQuote();
  flushTable();
  flushCode();
  return html.join('');
}

function buildSectionsFromMarkdown(markdown, prefix){
  const full = String(markdown||'').replace(/\r\n/g,'\n').trim();
  const sections = [];
  const rx = /^##\s+(.+)$/gm;
  const hits = [];
  let m;
  while ((m = rx.exec(full)) !== null) {
    hits.push({ index: m.index, title: m[1].trim() });
  }
  if(!hits.length){
    return [{
      id: prefix + '-section',
      title: 'Overview',
      html: markdownToHtml(full),
      searchText: docsStripHtml(full).toLowerCase()
    }];
  }
  for (let i = 0; i < hits.length; i++) {
    const start = hits[i].index;
    const end = i + 1 < hits.length ? hits[i + 1].index : full.length;
    const chunk = full.slice(start, end).trim();
    const lines = chunk.split('\n');
    const title = lines.shift().replace(/^##\s+/, '').trim();
    const body = lines.join('\n').trim();
    sections.push({
      id: prefix + '-' + String(i + 1).padStart(2, '0') + '-' + docsSlug(title),
      title,
      html: markdownToHtml(body),
      searchText: docsStripHtml(title + ' ' + body).toLowerCase()
    });
  }
  return sections;
}

const OWNER_CURRICULUM_MARKDOWN = "# Owner Curriculum: Learning to Maintain ScarabEV\n\nCurriculum = teacher and training path.\nProject Bible = encyclopedia and operating reference.\nPractice Lab = reps and drills.\nMentored Workshops = guided change sessions.\n\n## 1. Start Here: Owner Mindset Reset\n\nYou are not \"behind.\" You are at the normal point where product ownership grows into technical ownership.\n\nMost founders who direct a product through AI-assisted building eventually hit the same wall:\n- they know exactly what the app should do,\n- they can describe bugs clearly,\n- but the code feels like a foreign city.\n\nThis curriculum treats that stage as expected, not as failure.\n\nWhat matters now is not becoming a brilliant programmer overnight. What matters is becoming a calm, practical maintainer who can:\n1. inspect a real issue,\n2. make safe, scoped edits,\n3. test and rollback confidently,\n4. decide when to escalate.\n\nPlain English translation:\n- You already own ScarabEV strategically.\n- This course helps you own it operationally.\n\n## 2. How To Use This Curriculum With The Bible\n\nRead the lesson first, then open files immediately. Learning stays shallow when reading is disconnected from code.\n\nWorking loop:\n1. Read one section here.\n2. Open the referenced file and locate the relevant function.\n3. Write one plain-English note about what the function does.\n4. Make one tiny edit when the lesson asks.\n5. Test immediately.\n6. Record what changed.\n\nUse the Project Bible when you need exact route maps, operational rules, or recovery references. The Bible tells you what exists. This curriculum teaches how to think while using it.\n\n## 3. How To Study The Codebase Without Panic\n\n### 3.1 Read in slices, not floods\n\nLarge files feel impossible when read top-to-bottom. Instead, read by behavior slice:\n- one button,\n- one slider,\n- one table sort,\n- one fetch path.\n\nEach slice should answer:\n- what triggers this behavior,\n- where state changes,\n- where output is rendered.\n\n### 3.2 Start from visible behavior\n\nBegin with the screen, not the backend.\n\nExample:\nIf the threshold slider appears wrong, start at the slider listener in js/app.js, then move to threshold conversion helpers, then render logic, then EV functions if needed.\n\n### 3.3 Convert code to plain English notes\n\nEvery 15 minutes, stop and write:\n- \"This function runs when...\"\n- \"It reads these values...\"\n- \"It updates these values...\"\n- \"User sees this result...\"\n\nThis practice transforms passive scrolling into real understanding.\n\n### 3.4 Study modes\n\n#### 30-minute session\n- 5 minutes: pick one visible behavior.\n- 15 minutes: trace trigger to output.\n- 10 minutes: write notes and one follow-up question.\n\n#### 60-minute session\n- 10 minutes: recap previous notes.\n- 30 minutes: trace one full fetch -> state -> render chain.\n- 20 minutes: one safe edit and test.\n\n#### Weekend deep-dive\n- choose one subsystem,\n- map its boundaries,\n- run one controlled change with rollback ready,\n- write a one-page summary for future you.\n\n## 4. 90-Day Roadmap (Operational, Not Academic)\n\n### Month 1: Frontend comfort and core JavaScript literacy\n\nOutcome target:\nYou can read js/app.js without freezing, trace event flow, and ship low-risk copy/layout changes safely.\n\n### Month 2: Real feature editing and debugging\n\nOutcome target:\nYou can diagnose whether a problem is render logic, state wiring, parse shape, or request failure.\n\n### Month 3: Self-maintenance and operational calmness\n\nOutcome target:\nYou can run small end-to-end maintenance tasks, deploy with a checklist, and recover from mistakes quickly.\n\nWeekly milestone spine:\n- Week 1: UI structure and source mapping.\n- Week 2: event -> state -> render tracing.\n- Week 3: safe visible edits.\n- Week 4: slider/button behavior literacy.\n- Week 5: fetch and network debugging.\n- Week 6: parse and data-shape confidence.\n- Week 7: table/card behavior edits.\n- Week 8: regex/token UI ownership basics.\n- Week 9: controlled logic edits.\n- Week 10: backend route awareness.\n- Week 11: deployment and rollback rehearsal.\n- Week 12: incident calmness and escalation discipline.\n\n## 5. Beginner JavaScript Through ScarabEV\n\nThis is not generic JavaScript trivia. Each concept is taught through ScarabEV behavior.\n\n### Variables and constants\n\nWhat it is:\nA variable is a named value that can change. A constant is a named value you intend to keep stable.\n\nHow ScarabEV uses it:\n- js/state.js stores runtime values that change while the app runs.\n- js/config.js stores stable constants, text blocks, and endpoint settings.\n\nWhat breaks when misunderstood:\nIf you put changing runtime data into config, you create hidden state bugs. If you treat endpoint config casually, you can point the app to the wrong environment.\n\nCommon beginner mistake:\nEditing a constant to fix a runtime issue that should have been fixed in state or render logic.\n\n### Functions and parameters\n\nWhat it is:\nA function is a named behavior with expected inputs and outcomes.\n\nHow ScarabEV uses it:\n- event handlers in js/app.js,\n- calculators in js/scarabEngine.js,\n- parsing helpers in js/market.js.\n\nWhy this matters:\nMost regressions happen when parameter assumptions are changed without checking callers.\n\nPlain English:\nA parameter is a promise about shape and meaning. Break that promise and downstream code becomes unreliable.\n\n### Objects, arrays, loops, and conditions\n\nObjects hold one structured thing. Arrays hold many structured things. Loops process the list. Conditions choose the correct branch.\n\nScarabEV example:\nVendor rows are built from arrays of parsed entries. Sorting, filtering, and recommendation labeling depend on loop + condition pipelines.\n\nWhat breaks when misunderstood:\n- wrong object key names cause undefined UI values,\n- over-strict filter conditions empty a table,\n- branch conditions can hide the right output.\n\n### Events, state, and render\n\nCore model:\n1. User event fires.\n2. Handler executes.\n3. State updates.\n4. Render function repaints affected UI.\n\nIf step 3 happens without step 4, users see stale output.\nIf step 4 runs against wrong state shape, users see wrong output.\n\n### Imports and exports\n\nModules exist so ScarabEV does not become one giant file.\n\nWhy this matters:\nWhen logic is duplicated instead of imported, drift starts. A bug fixed in one copy survives in another.\n\nOwnership reminder:\n- UI wiring belongs mostly in js/app.js.\n- EV math belongs in js/scarabEngine.js.\n- parsing belongs in js/market.js.\n\n### Async, await, and fetch\n\nFetch paths are asynchronous because data arrives later.\n\nNormal pattern:\n- request data,\n- check response status,\n- parse payload,\n- normalize shape,\n- update state,\n- render.\n\nCommon mistake:\nTreating every fetch response as valid JSON success. Real systems fail. Guard branches are part of feature quality.\n\n## 6. File-by-File Beginner Tour (How To Read, Not Just What Exists)\n\n### index.html (easy)\n\nStart here when learning visible structure.\n\nRead order:\n1. tab containers,\n2. section ids,\n3. major card blocks.\n\nIgnore first:\ndense styling details unrelated to your current behavior slice.\n\nSafe edits:\ntext and non-functional layout adjustments.\n\n### js/app.js (medium)\n\nThis is the orchestration hub.\n\nHow to read safely:\n- pick one feature path,\n- find trigger handler,\n- follow called helpers,\n- identify final render call.\n\nIgnore first:\nunrelated tabs and long utility branches.\n\n### js/state.js (easy)\n\nTreat this as runtime memory map.\n\nFocus on:\nkeys touched by your active feature.\n\nCommon confusion:\nexpecting state changes to auto-render.\n\n### js/config.js (easy)\n\nTreat this as stable definitions and static content.\n\nGood beginner wins:\nFAQ copy, helper text, endpoint label review.\n\n### js/scarabEngine.js (later)\n\nRead before editing.\n\nFocus on:\nfunction signatures and required input shape.\n\nDo not touch casually:\nformula behavior that affects recommendations.\n\n### js/regexEngine.js (medium)\n\nFocus on:\nbuild paths, fallback/warning branches, and output constraints.\n\nCommon mistake:\nassuming parser math is wrong when token source is actually invalid.\n\n### js/market.js (medium)\n\nFocus on:\nhow worker payloads are normalized before UI usage.\n\nDebugging payoff:\nmany visible UI issues are parse-shape issues here, not render-only issues.\n\n### js/tokenSource.js (medium)\n\nFocus on:\nsource priority and fallback behavior.\n\nWhy this file matters:\nregex output quality depends on token source reliability.\n\n## 7. Month 1 Curriculum: Frontend Comfort\n\n### Week 1: Structure literacy\n\nLearning target:\nmap visible UI text to source locations reliably.\n\nExercises:\n- change one FAQ line,\n- update one helper label,\n- verify no behavior changes.\n\n### Week 2: app.js confidence\n\nLearning target:\ntrace one event path without reading the whole file.\n\nExercises:\n- trace one button click path,\n- trace one tab switch path,\n- summarize both in plain English.\n\n### Week 3: Safe visible edits\n\nLearning target:\nship low-risk UI improvements with discipline.\n\nExercises:\n- improve tooltip wording,\n- adjust spacing in one region,\n- verify both themes.\n\n### Week 4: Interactivity basics\n\nLearning target:\nunderstand slider/button event flow and output changes.\n\nExercises:\n- trace threshold slider values,\n- change one non-risk interaction label,\n- test rerender behavior.\n\n## 8. Month 2 Curriculum: Real Ownership\n\n### Week 1: Console-first debugging\n\nLearn to prioritize the first error, not the loudest error.\n\nDeliverable:\none documented debug case with first error, root cause, and fix.\n\n### Week 2: Data-flow tracing\n\nTrace one path completely:\nrequest -> parse -> state -> render.\n\nDeliverable:\nclear map showing where each value changes.\n\n### Week 3: Render and behavior edits\n\nMake one logic-adjacent UI change with explicit before/after checks.\n\nDeliverable:\nsmall diff, test notes, rollback note.\n\n### Week 4: Network diagnosis\n\nRead failed requests by class:\n401, 403, 404, 500, malformed payload.\n\nDeliverable:\none short runbook mapping status code to first checks.\n\n## 9. Month 3 Curriculum: Self-Maintenance\n\n### Backend awareness\n\nUnderstand route families, auth guards, and mutation risk boundaries.\n\n### Deployment confidence\n\nAlways run a predictable pre-deploy checklist:\n- scoped diff,\n- type/build checks,\n- smoke checks,\n- rollback plan.\n\n### Rollback fluency\n\nPractice rollback when calm, not only during incidents.\n\n### Incident calmness\n\nUse sequence:\nsymptom -> evidence -> hypothesis -> tiny test -> decision.\n\nNever panic-edit across multiple layers at once.\n\n## 10. Safe Change Rules For Owner Operations\n\n1. One small change per step.\n2. Test after every step.\n3. No broad refactors while learning.\n4. Treat formulas, auth, and security as high risk.\n5. Keep rollback path ready before risky edits.\n6. Review git diff before and after.\n\n## 11. Fear Index and Readiness Gates\n\n### GREEN (safe learning zone)\n\nExamples:\ncopy, labels, docs text, minor layout.\n\nMove up when:\nyou can ship multiple clean edits with no unintended behavior changes.\n\n### YELLOW (medium coupling)\n\nExamples:\nrender logic, sort/filter behavior, slider labeling, event wiring.\n\nMove up when:\nyou can trace cause and effect reliably and debug from evidence.\n\n### RED (high-risk zones)\n\nExamples:\nauth/session/security, formula logic, worker cache policy, schema migrations.\n\nEntry rule:\nexplicit owner confirmation, written test plan, and rollback plan.\n\n## 12. How To Use AI Less Over Time\n\nStage 1: AI explains unfamiliar code.\nStage 2: owner predicts change; AI validates risks.\nStage 3: owner edits low-risk tasks; AI reviews diff.\nStage 4: owner debugs first; AI assists edge cases.\nStage 5: AI is consultant, not crutch.\n\nPrompt templates:\n- Explain this function in plain English and list caller assumptions.\n- Review this intended change and name top 3 regression risks.\n- Help debug this error using first-evidence workflow only.\n- Review this diff and flag high-risk side effects.\n\n## 13. Milestone Checklist\n\nEnd of Month 1:\n- locate UI text quickly,\n- trace basic event paths,\n- run safe copy/layout edits,\n- use console calmly.\n\nEnd of Month 2:\n- trace fetch -> state -> render,\n- diagnose common frontend failures,\n- patch medium-risk UI logic safely.\n\nEnd of Month 3:\n- run self-directed maintenance cycles,\n- deploy with checklist discipline,\n- rollback quickly from simple mistakes,\n- escalate high-risk uncertainty early.\n\n## 14. When To Stop and Ask For Help\n\nEscalate immediately for:\n- auth/session/security changes,\n- database/schema migration risk,\n- backup/restore operations,\n- EV formula changes affecting recommendations,\n- anything involving secrets,\n- unexplained data corruption.\n\n## 15. Personal Learning Log\n\nKeep a private running log with:\n- what you learned,\n- what broke and why,\n- which files now feel safe,\n- which files still feel risky,\n- questions for future workshop sessions.\n";

const PRACTICE_LAB_MARKDOWN = "# Practice Lab: Hands-On ScarabEV Training\n\nThis book is where understanding becomes repeatable skill.\nEvery lab is designed to avoid dead ends and panic edits.\n\n## 1. Lab Method (Fail-Proof)\n\nFor every lab, complete these blocks:\n- Goal\n- Skill trained\n- Solo attempt prompt\n- Hint ladder (L1, L2, L3)\n- Full walkthrough mode\n- What success looks like\n- Common mistakes\n- First 3 checks if it fails\n- Rollback steps\n- Why this lab matters\n- Replay variation\n\n## 2. Practice Lab Pack (20 Progressive Labs)\n\n### Labs 1-5: Confidence building\n\nLab 1: FAQ copy update\n- Goal: ship a safe copy edit.\n- Skill trained: low-risk source mapping.\n- Solo attempt prompt: change one FAQ answer without touching selectors.\n- Hint ladder: L1 find FAQ source, L2 edit one entry only, L3 verify render output.\n- Full walkthrough mode: edit, reload, verify text and console.\n- Success: only copy changes.\n- Common mistakes: accidental id/class edits.\n- First 3 checks: source key, render usage, console.\n- Rollback: revert FAQ diff only.\n- Why this matters: establishes safe-edit baseline.\n- Replay variation: update a tooltip copy.\n\nLab 2: button label cleanup\n- Goal: update visible button text safely.\n- Skill trained: label vs selector discipline.\n- Solo attempt prompt: rename text, preserve functional selector hooks.\n- Hint ladder: L1 locate label source, L2 verify no selector edit, L3 test button behavior.\n- Full walkthrough mode: edit text, run click path, verify behavior.\n- Success: behavior unchanged.\n- Common mistakes: changing id instead of text.\n- First 3 checks: selector id, handler binding, output.\n- Rollback: restore previous label line.\n- Why this matters: prevents UI-only edits from breaking logic.\n- Replay variation: repeat on a second control.\n\nLab 3: helper text under a card\n- Goal: improve readability without logic changes.\n- Skill trained: render text insertion.\n- Solo attempt prompt: add one explanatory sentence under existing stat card.\n- Hint ladder: L1 locate render block, L2 insert copy only, L3 verify dark/light readability.\n- Full walkthrough mode: add line, reload, inspect in both themes.\n- Success: new help text appears cleanly.\n- Common mistakes: touching calculation lines.\n- First 3 checks: template block, closing tags, theme contrast.\n- Rollback: remove added line.\n- Why this matters: teaches non-destructive rendering edits.\n- Replay variation: improve one empty-state message.\n\nLab 4: spacing correction\n- Goal: resolve one dense section visually.\n- Skill trained: scoped CSS adjustments.\n- Solo attempt prompt: increase readability spacing in one docs region only.\n- Hint ladder: L1 identify target class, L2 adjust one spacing rule, L3 verify no global layout drift.\n- Full walkthrough mode: edit one margin/padding value, inspect desktop and mobile.\n- Success: local improvement, no regressions.\n- Common mistakes: global selector overreach.\n- First 3 checks: selector specificity, affected surfaces, mobile view.\n- Rollback: revert style rule.\n- Why this matters: practical UI polish confidence.\n- Replay variation: tune one table cell spacing value.\n\nLab 5: click event trace\n- Goal: map click to visible effect.\n- Skill trained: event-state-render tracing.\n- Solo attempt prompt: explain exactly what one click does internally.\n- Hint ladder: L1 find listener, L2 track state mutation, L3 locate render call.\n- Full walkthrough mode: step through listener, helper call, state write, UI output.\n- Success: can explain flow in plain English.\n- Common mistakes: jumping directly to backend.\n- First 3 checks: listener fire, state key, render function.\n- Rollback: remove temporary debug logs.\n- Why this matters: this pattern covers most UI bugs.\n- Replay variation: repeat with a different control.\n\n### Labs 6-10: UI logic ownership\n\nLab 6: add sort option label\nLab 7: wire sort option behavior\nLab 8: update table column meaning safely\nLab 9: add regex warning copy\nLab 10: trace market fetch to rendered value\n\nFor labs 6-10 apply the same fail-proof structure above, with emphasis on branch logic clarity and before/after verification notes.\n\n### Labs 11-15: integrated data flow\n\nLab 11: docs copy update with TOC/search validation.\nLab 12: add small state-backed helper field.\nLab 13: recover from intentionally broken selector.\nLab 14: add stat card from existing state value.\nLab 15: trace weighted EV from request to display.\n\n### Labs 16-20: operations readiness\n\nLab 16: backend status message alignment.\nLab 17: fix intentionally broken fetch path.\nLab 18: EV sample validation run (read-first).\nLab 19: small market worker observability copy update.\nLab 20: mini release drill with checklist + rollback plan.\n\n## 3. Feature Trace Labs (Deep)\n\n### Threshold slider lab\n- Goal: prove how slider movement changes recommendation output.\n- Skill trained: conversion + render continuity.\n- Solo attempt prompt: map slider input to threshold value used in decisions.\n- Hint ladder: L1 listener, L2 conversion helper, L3 rerender point.\n- Full walkthrough mode: trace and log each stage once, then remove logs.\n- Success: threshold change is visible and explainable.\n- Common mistakes: editing EV formulas first.\n- First 3 checks: handler bind, conversion output, render call.\n- Rollback: revert temporary edits.\n- Why this matters: common live issue path.\n- Replay variation: test tiny vs large slider moves.\n\n### Weighted EV load path lab\n- Goal: explain exactly when weighted EV should appear.\n- Skill trained: multi-source readiness reasoning.\n- Solo attempt prompt: identify required data dependencies and gates.\n- Hint ladder: L1 inspect requests, L2 inspect state readiness flags, L3 inspect weighted branch.\n- Full walkthrough mode: request -> parse -> state -> branch -> render map.\n- Success: owner can justify why weighted is shown or hidden.\n- Common mistakes: forcing mode without data readiness.\n- First 3 checks: aggregate payload, market payload, readiness keys.\n- Rollback: remove forced toggles.\n- Why this matters: trust-critical recommendation path.\n- Replay variation: simulate one missing dependency.\n\n### Regex copy flow lab\n- Goal: isolate copy failures to exact stage.\n- Skill trained: action path diagnostics.\n- Solo attempt prompt: identify where copy flow fails when button appears inert.\n- Hint ladder: L1 handler fire, L2 output target value, L3 clipboard promise.\n- Full walkthrough mode: trace click -> target lookup -> write -> status.\n- Success: copied text matches displayed regex.\n- Common mistakes: adding duplicate listeners.\n- First 3 checks: selector, listener, permission.\n- Rollback: remove duplicate wiring experiments.\n- Why this matters: teaches reliable UI action debugging.\n- Replay variation: run same flow in alternate tab state.\n\n### CSV import lab\n- Goal: validate parse and mapping correctness.\n- Skill trained: structured input debugging.\n- Solo attempt prompt: locate first point where wrong totals appear.\n- Hint ladder: L1 headers, L2 parsed row shape, L3 numeric coercion.\n- Full walkthrough mode: import known sample and compare expected row outputs.\n- Success: totals match known sample.\n- Common mistakes: broad string replacement patches.\n- First 3 checks: header mapping, row parse, numeric conversion.\n- Rollback: restore parser baseline.\n- Why this matters: estimator trust depends on this.\n- Replay variation: test shuffled column order.\n\n### Tab switching lab\n- Goal: ensure docs tab and content sync correctly.\n- Skill trained: UI state + localStorage coordination.\n- Solo attempt prompt: prove selected tab, TOC, and content remain aligned.\n- Hint ladder: L1 selected id, L2 active class, L3 persisted key.\n- Full walkthrough mode: switch tabs, reload, verify restored selection.\n- Success: active book and TOC are always in sync.\n- Common mistakes: editing unrelated nav code.\n- First 3 checks: selected id, class toggles, storage key.\n- Rollback: revert tab-specific wiring changes.\n- Why this matters: core admin documentation usability.\n- Replay variation: clear storage and retest defaults.\n\n## 4. Translate Code To English Drills\n\nThese drills are about fluency, not memorization.\n\nDrill sequence:\n1. function with parameters,\n2. map/filter/render block,\n3. event listener path,\n4. fetch with guard branch,\n5. state mutation and rerender,\n6. conditional rendering,\n7. module import/export boundary.\n\nFor each drill, write:\n- one technical explanation,\n- one plain-English translation,\n- one tiny safe edit challenge,\n- one reflection: what would break if this is misunderstood?\n\n## 5. Real Debugging Scenarios\n\nScenario set:\n- weighted EV blank,\n- stale prices,\n- regex overflow,\n- copy button inert,\n- empty table after update,\n- slider updates UI incorrectly,\n- docs tab not switching,\n- backend 403 after login,\n- chart missing,\n- CSV parse drift.\n\nEach scenario must be solved with evidence-first order, never by random edits.\n\n## 6. Hands-On Missions\n\nMission tiers:\n- Easy: content and layout edits.\n- Medium: sorting/filtering/render blocks.\n- Hard: cross-file tracing and safe backend-aware fixes.\n\nEvery mission submission should include:\n- diff summary,\n- test evidence,\n- rollback note,\n- lesson learned.\n\n## 7. 60-Day Apprenticeship Mode\n\nDaily micro-task cadence:\n- Day 1-20: safe visible edits + tracing.\n- Day 21-40: debugging reps + render logic changes.\n- Day 41-60: integrated maintenance drills with deploy checklist discipline.\n\nWeekly review ritual:\n- strongest skill this week,\n- weakest pattern this week,\n- one bug you can now explain clearly,\n- one red-zone item to avoid until ready.\n\n## 8. Answer Keys and Solutions Pack\n\nAnswer keys should teach reasoning, not just final lines.\n\nFor each key include:\n- why the expected path is correct,\n- why tempting wrong paths are risky,\n- what signal confirms success,\n- how to roll back safely if result drifts.\n\n## 9. Real Bugs Catalog (Readable Table)\n\n| # | Bug symptom | Meaning | First check |\n|---|---|---|---|\n| 1 | Undefined value in row | parse key mismatch | payload path |\n| 2 | Button appears dead | listener/selector mismatch | handler bind |\n| 3 | Sort does nothing | branch not wired | sort switch |\n| 4 | Slider changes but table static | render path missing | state + rerender |\n| 5 | Weighted EV blank | readiness gate unmet | aggregate + market dependencies |\n| 6 | Regex copy fails | clipboard or target mismatch | click path |\n| 7 | Docs tab desync | active state mismatch | selected id + TOC sync |\n| 8 | 403 on mutation | CSRF/session/role issue | request context |\n| 9 | Stale data warning persists | worker freshness issue | worker meta + logs |\n| 10 | Runtime fails but build passes | shape assumption mismatch | first console error |\n\n## 10. Confidence Rules\n\n- Never stack unknown edits when stressed.\n- Never patch red-zone systems casually.\n- Always preserve rollback optionality.\n- Always document what you learned after each lab.\n";

const MENTORED_WORKSHOPS_MARKDOWN = "# Mentored Workshops: Guided Ownership Sessions\n\nThese workshops are private, step-by-step teaching sessions designed for solo execution.\nEach workshop follows the same structure so you can learn without dead ends.\n\n## Workshop Format\n\n- Goal\n- Files\n- Exact search terms\n- What this code does\n- Exact edit\n- Why it works\n- Test steps\n- Failure checks\n- Rollback\n- Concept learned\n\n## Workshop 1: Update FAQ guidance safely\n- Goal: improve user-facing FAQ clarity with zero logic risk.\n- Files: js/config.js, js/app.js.\n- Exact search terms: FAQ_SECTIONS, renderFAQ.\n- What this code does: stores and renders FAQ entries.\n- Exact edit: revise one FAQ answer sentence for clarity.\n- Why it works: render path reads FAQ config directly.\n- Test steps: reload app, open FAQ, verify updated copy only.\n- Failure checks: undefined FAQ item, broken HTML in entry.\n- Rollback: revert edited FAQ object entry.\n- Concept learned: low-risk content ownership.\n\n## Workshop 2: Improve a tooltip explanation\n- Goal: teach intent without changing behavior.\n- Files: js/app.js.\n- Exact search terms: tooltip, title=, helper text.\n- What this code does: defines helper context around controls.\n- Exact edit: replace one vague tooltip with action-oriented text.\n- Why it works: tooltip string is rendered directly.\n- Test steps: hover target, verify new message.\n- Failure checks: selector mismatch, stale cached markup.\n- Rollback: restore original tooltip line.\n- Concept learned: UX clarity with zero business-risk edits.\n\n## Workshop 3: Trace one button click end-to-end\n- Goal: build event-state-render literacy.\n- Files: js/app.js, js/state.js.\n- Exact search terms: addEventListener, renderVendorTable.\n- What this code does: wires button events to UI updates.\n- Exact edit: temporary log at handler start and render entry.\n- Why it works: logs expose runtime path order.\n- Test steps: click button, inspect console sequence.\n- Failure checks: log not firing, wrong control selected.\n- Rollback: remove temporary logs.\n- Concept learned: evidence-first behavior tracing.\n\n## Workshop 4: Add helper note under a stat card\n- Goal: increase interpretation confidence for readers.\n- Files: js/app.js.\n- Exact search terms: stat card markup, analysis card.\n- What this code does: builds card HTML from state values.\n- Exact edit: add one explanatory line below card value.\n- Why it works: template output is rerendered each cycle.\n- Test steps: refresh card region, verify copy placement.\n- Failure checks: malformed template string.\n- Rollback: remove inserted line.\n- Concept learned: safe render-template edits.\n\n## Workshop 5: Slider path deep read\n- Goal: explain threshold slider behavior precisely.\n- Files: js/app.js, js/scarabEngine.js.\n- Exact search terms: initSlider, threshold, sliderValueToThreshold.\n- What this code does: converts slider movement into decision threshold.\n- Exact edit: add one debug status line showing converted threshold.\n- Why it works: conversion function is central value source.\n- Test steps: move slider and observe status output change.\n- Failure checks: status not updating, handler not firing.\n- Rollback: remove debug status line.\n- Concept learned: conversion and state propagation.\n\n## Workshop 6: Sort option review and safe tweak\n- Goal: understand and safely adjust sorting behavior.\n- Files: js/app.js.\n- Exact search terms: setNinjaSort, sort, comparator.\n- What this code does: controls list order presentation.\n- Exact edit: adjust default sort label text only, then optionally reorder one branch.\n- Why it works: sort mode maps to comparator branches.\n- Test steps: switch sort modes and compare row ordering.\n- Failure checks: mode value mismatch, branch unreachable.\n- Rollback: revert comparator branch change.\n- Concept learned: controlled branch editing.\n\n## Workshop 7: Empty-state messaging quality pass\n- Goal: make failure states actionable for future debugging.\n- Files: js/app.js.\n- Exact search terms: no data, empty state, unavailable.\n- What this code does: communicates non-success states.\n- Exact edit: rewrite one empty-state message with next step guidance.\n- Why it works: users get actionable context instead of ambiguity.\n- Test steps: trigger empty-state condition and inspect output.\n- Failure checks: condition no longer matched.\n- Rollback: restore previous message block.\n- Concept learned: resilience UX.\n\n## Workshop 8: Market payload trace map\n- Goal: map response fields to UI values.\n- Files: js/market.js, js/app.js.\n- Exact search terms: parseWorkerResponse, chaosValue, divine rate.\n- What this code does: normalizes worker payload for rendering.\n- Exact edit: add temporary mapping comments for one field path.\n- Why it works: comments document parse contract.\n- Test steps: inspect parsed object and rendered value alignment.\n- Failure checks: wrong key path assumption.\n- Rollback: remove temporary comments.\n- Concept learned: parse contract literacy.\n\n## Workshop 9: Weighted EV readiness workshop\n- Goal: understand why weighted EV appears or hides.\n- Files: js/app.js, js/scarabEngine.js.\n- Exact search terms: computeWeightBasedRate, weightMeta, readiness.\n- What this code does: gates weighted math by data readiness.\n- Exact edit: add temporary debug badge showing readiness state.\n- Why it works: readiness values drive weighted branch activation.\n- Test steps: inspect badge with data present and absent.\n- Failure checks: badge logic reading wrong keys.\n- Rollback: remove debug badge rendering.\n- Concept learned: gate-based feature activation.\n\n## Workshop 10: Regex output quality workshop\n- Goal: understand regex build pipeline and constraints.\n- Files: js/regexEngine.js, js/tokenSource.js.\n- Exact search terms: buildRegex, tokensByName, overflow.\n- What this code does: builds and validates regex output.\n- Exact edit: improve one warning message for overflow/fallback.\n- Why it works: warning path already exists, copy update clarifies action.\n- Test steps: trigger long output condition and verify warning.\n- Failure checks: warning branch not reached.\n- Rollback: restore prior warning text.\n- Concept learned: safe guardrail editing.\n\n## Workshop 11: CSV parser orientation\n- Goal: read import path without fear.\n- Files: js/app.js.\n- Exact search terms: parseWealthyCSV, importWealthyCSV.\n- What this code does: parses rows into estimator inputs.\n- Exact edit: add temporary console count of parsed rows.\n- Why it works: count verifies parse success quickly.\n- Test steps: import known sample and verify count.\n- Failure checks: delimiter/header mismatch.\n- Rollback: remove temporary logging.\n- Concept learned: structured input debugging.\n\n## Workshop 12: Card layout readability fix\n- Goal: improve scanability of one crowded card.\n- Files: index.html style blocks or docs/admin styles where appropriate.\n- Exact search terms: card, gap, line-height.\n- What this code does: controls spacing and visual hierarchy.\n- Exact edit: increase spacing for one card region.\n- Why it works: typography spacing reduces cognitive load.\n- Test steps: compare before/after screenshot mentally or visually.\n- Failure checks: unintended global selector reach.\n- Rollback: revert spacing rule.\n- Concept learned: precision CSS ownership.\n\n## Workshop 13: Docs book persistence check\n- Goal: verify selected documentation book survives reload.\n- Files: backend/admin-ui-src/scripts/documentation.js.\n- Exact search terms: DOC_BOOK_STORAGE_KEY, localStorage.\n- What this code does: saves and restores selected docs tab.\n- Exact edit: add temporary status line showing persisted book id.\n- Why it works: exposes storage read/write state.\n- Test steps: switch book, reload, verify restored selection.\n- Failure checks: key mismatch or write failure.\n- Rollback: remove temporary status line.\n- Concept learned: persistence debugging.\n\n## Workshop 14: TOC behavior inspection\n- Goal: understand section navigation state updates.\n- Files: backend/admin-ui-src/scripts/documentation.js.\n- Exact search terms: docs-toc-item, syncActiveFromScroll.\n- What this code does: updates active TOC item by scroll position.\n- Exact edit: instrument active section id in status text.\n- Why it works: makes sync behavior observable.\n- Test steps: scroll content and watch active id update.\n- Failure checks: section id mismatch.\n- Rollback: remove instrumentation.\n- Concept learned: scroll-state synchronization.\n\n## Workshop 15: Knowledge Checks setup flow\n- Goal: understand setup -> active quiz transition.\n- Files: backend/admin-ui-src/scripts/documentation.js.\n- Exact search terms: docsQuizRenderSetup, kcStartBtn, docsQuizStart.\n- What this code does: builds setup screen and launches attempts.\n- Exact edit: improve one setup helper label for clarity.\n- Why it works: label change reduces operator confusion.\n- Test steps: start quiz and verify transition still works.\n- Failure checks: button id mismatch.\n- Rollback: revert label string.\n- Concept learned: UI flow tracing in admin module.\n\n## Workshop 16: Quiz feedback readability\n- Goal: improve feedback comprehension without logic changes.\n- Files: backend/src/admin/styles.ts.\n- Exact search terms: kc-choice, status ok, status warn.\n- What this code does: styles answer options and feedback states.\n- Exact edit: adjust spacing or emphasis for feedback block.\n- Why it works: clearer hierarchy improves review speed.\n- Test steps: submit correct and wrong answers.\n- Failure checks: style selector too broad.\n- Rollback: revert style rule.\n- Concept learned: instructional UI polish discipline.\n\n## Workshop 17: Results report interpretation workshop\n- Goal: use report card data for next-study decisions.\n- Files: backend/admin-ui-src/scripts/documentation.js.\n- Exact search terms: strongestCategories, weakestCategories, recommendedReview.\n- What this code does: summarizes attempt strengths and gaps.\n- Exact edit: improve one review recommendation string.\n- Why it works: clearer recommendation drives better next action.\n- Test steps: complete quiz and inspect report output.\n- Failure checks: missing array values edge case.\n- Rollback: restore previous recommendation text.\n- Concept learned: actionable analytics communication.\n\n## Workshop 18: History integrity workshop\n- Goal: confirm history entries persist and render correctly.\n- Files: backend/admin-ui-src/scripts/documentation.js.\n- Exact search terms: QUIZ_STORAGE_KEYS.history, docsQuizRenderHistory.\n- What this code does: stores and lists quiz attempts.\n- Exact edit: add temporary badge with history length.\n- Why it works: quickly verifies persistence pipeline.\n- Test steps: take two quizzes, reload, verify count.\n- Failure checks: malformed history JSON.\n- Rollback: remove temporary badge.\n- Concept learned: client-side persistence validation.\n\n## Workshop 19: Practice Lab readability workshop\n- Goal: improve one dense section's readability hierarchy.\n- Files: backend/admin-ui-src/scripts/documentation.js, backend/src/admin/styles.ts.\n- Exact search terms: Practice Lab heading, docs-kv-item.\n- What this code does: renders and styles lab instruction blocks.\n- Exact edit: split one dense paragraph into guided steps.\n- Why it works: chunking improves retention and execution.\n- Test steps: inspect updated section in dark and light modes.\n- Failure checks: heading level mismatch affecting TOC.\n- Rollback: restore original paragraph block.\n- Concept learned: educational information architecture.\n\n## Workshop 20: Fear index calibration workshop\n- Goal: make risk boundaries operational, not abstract.\n- Files: Owner Curriculum content in documentation source.\n- Exact search terms: Fear Index, GREEN, YELLOW, RED.\n- What this code does: sets safe learning boundaries.\n- Exact edit: add one concrete escalation trigger example.\n- Why it works: practical triggers reduce risky improvisation.\n- Test steps: read section and verify clarity against real scenario.\n- Failure checks: overly vague language.\n- Rollback: restore previous wording.\n- Concept learned: risk-aware decision framing.\n\n## Workshop 21: Backend route literacy drill\n- Goal: read route contracts without editing behavior.\n- Files: backend/src/routes files and Project Bible references.\n- Exact search terms: method, path, auth, csrf.\n- What this code does: defines protected and public API behavior.\n- Exact edit: none required; produce route map notes.\n- Why it works: route contract literacy prevents blind fixes.\n- Test steps: map one mutation route and one read route.\n- Failure checks: missing method/path pairing.\n- Rollback: no code changes.\n- Concept learned: contract-first backend awareness.\n\n## Workshop 22: Worker freshness reasoning\n- Goal: interpret stale/fresh signals correctly.\n- Files: workers/market-worker/worker.js, js/market.js.\n- Exact search terms: stale, ageSeconds, cache metadata.\n- What this code does: controls freshness semantics for market data.\n- Exact edit: add temporary comments in local notes only.\n- Why it works: clarifies trust model and avoids bypass fixes.\n- Test steps: compare stale vs fresh response handling.\n- Failure checks: misreading metadata path.\n- Rollback: remove temporary notes.\n- Concept learned: worker-first data reliability model.\n\n## Workshop 23: Rollback rehearsal workshop\n- Goal: practice rollback as a normal maintenance action.\n- Files: current feature branch diff.\n- Exact search terms: git diff, last known good.\n- What this code does: version control safety net.\n- Exact edit: intentionally apply tiny reversible change, then rollback.\n- Why it works: trains calm recovery muscle.\n- Test steps: apply, verify, rollback, verify restore.\n- Failure checks: rollback scope too broad.\n- Rollback: explicit targeted revert.\n- Concept learned: operational resilience.\n\n## Workshop 24: Incident calmness simulation\n- Goal: run structured response under pressure.\n- Files: issue notes, console/network logs, relevant source files.\n- Exact search terms: first error, failing request.\n- What this code does: creates disciplined diagnosis flow.\n- Exact edit: none until evidence is captured.\n- Why it works: prevents panic multi-layer edits.\n- Test steps: capture symptom, first evidence, one hypothesis, one test.\n- Failure checks: jumping to unverified fixes.\n- Rollback: revert any speculative edits.\n- Concept learned: high-quality debugging process.\n\n## Workshop 25: Solo maintenance capstone\n- Goal: complete one safe change from request to validation and notes.\n- Files: one UI file plus any required companion file.\n- Exact search terms: feature-specific to chosen task.\n- What this code does: end-to-end maintenance cycle.\n- Exact edit: one scoped owner-approved improvement.\n- Why it works: integrates tracing, editing, testing, and rollback planning.\n- Test steps: run behavior checks, type/build checks, document outcomes.\n- Failure checks: unclear ownership, missing test evidence.\n- Rollback: revert change if any uncertainty remains.\n- Concept learned: independent, safe, repeatable ownership.\n";

const KNOWLEDGE_CHECKS_INTRO_MARKDOWN = "# Knowledge Checks\\n\\nPrivate internal quiz system for certification-style self-study inside admin docs.";
const DOC_BOOKS = {
  'project-bible': {
    id: 'project-bible',
    label: 'Project Bible',
    markdown: BIBLE_MARKDOWN,
    prefix: 'bible'
  },
  'owner-curriculum': {
    id: 'owner-curriculum',
    label: 'Owner Curriculum',
    markdown: OWNER_CURRICULUM_MARKDOWN,
    prefix: 'curriculum'
  },
  'practice-lab': {
    id: 'practice-lab',
    label: 'Practice Lab',
    markdown: PRACTICE_LAB_MARKDOWN,
    prefix: 'practice'
  },
  'knowledge-checks': {
    id: 'knowledge-checks',
    label: 'Knowledge Checks',
    markdown: KNOWLEDGE_CHECKS_INTRO_MARKDOWN,
    prefix: 'knowledge',
    customRenderer: 'knowledge-checks'
  },
  'mentored-workshops': {
    id: 'mentored-workshops',
    label: 'Mentored Workshops',
    markdown: MENTORED_WORKSHOPS_MARKDOWN,
    prefix: 'workshops'
  }
};
const DOC_BOOK_ORDER = ['project-bible', 'owner-curriculum', 'practice-lab', 'knowledge-checks', 'mentored-workshops'];
const DOC_BOOK_STORAGE_KEY = 'scarabev.admin.docs.book';
const QUIZ_STORAGE_KEYS = {
  history: 'scarabev.admin.quiz.history',
  examHistory: 'scarabev.admin.quiz.examHistory',
  settings: 'scarabev.admin.quiz.settings',
  missed: 'scarabev.admin.quiz.missed',
  progression: 'scarabev.admin.quiz.progression',
  distractorMemory: 'scarabev.admin.quiz.distractorMemory'
};
const QUIZ_PASS_PERCENT = 75;

const DAILY_QUIZ_MODE_DEFS = [
  { id: 'quick-drill', label: 'Quick Drill', count: 10 },
  { id: 'standard-quiz', label: 'Standard Quiz', count: 25 },
  { id: 'deep-exam', label: 'Deep Exam', count: 50 },
  { id: 'weak-areas', label: 'Weak Areas', count: 25 },
  { id: 'scenario-debugging', label: 'Scenario Debugging', count: 25 },
  { id: 'code-reading', label: 'Code Reading', count: 25 },
  { id: 'safety-operations', label: 'Safety & Operations', count: 25 },
  { id: 'custom-practice', label: 'Custom Practice', count: 25 }
];
const FINAL_EXAM_LEVEL_DEFS = [
  { id: 'exam-apprentice', label: 'Apprentice', count: 35, passPercent: 72 },
  { id: 'exam-maintainer', label: 'Maintainer', count: 40, passPercent: 76 },
  { id: 'exam-owner', label: 'Owner', count: 45, passPercent: 80 },
  { id: 'exam-crisis', label: 'Crisis Mode', count: 50, passPercent: 84 }
];
const QUIZ_MODE_DEFS = DAILY_QUIZ_MODE_DEFS.concat(FINAL_EXAM_LEVEL_DEFS);
const QUIZ_SKILL_DIMENSIONS = [
  { id:'frontendReasoning', label:'Frontend reasoning score' },
  { id:'debugging', label:'Debugging score' },
  { id:'codeReading', label:'Code reading score' },
  { id:'safeChange', label:'Safe change judgment score' },
  { id:'backendWorker', label:'Backend / worker awareness score' },
  { id:'riskManagement', label:'Risk management score' }
];

const QUIZ_CATEGORY_DEFS = [
  { id:'js-foundations', label:'JavaScript foundations', ownerFile:'js/app.js', avoidFile:'backend/src/index.ts', files:['js/app.js','js/state.js','js/config.js'], inspectFirst:'Check function parameters and value shape before editing logic.', rootCause:'A mismatch between expected input shape and provided value in a helper path.', safeAction:'Trace the function contract (input -> output) before changing behavior.', concept:'Function contracts define what input shape is safe.', meaning:'A function contract prevents accidental behavior drift.', wrong:'You can safely change parameter meaning without checking callers.', snippet:"function toChaos(v){ return Number(v||0).toFixed(1); }", snippetPrompt:'What is the safest reading of this function?', snippetAnswer:'It coerces empty input to 0 and returns formatted text.', curriculum:'Owner Curriculum section 5 and section 6', practice:'Practice Lab section 26' },
  { id:'frontend-basics', label:'HTML/CSS/frontend basics', ownerFile:'index.html', avoidFile:'workers/market-worker/worker.js', files:['index.html','js/app.js'], inspectFirst:'Verify selector ids/classes used by JS before changing markup names.', rootCause:'An id/class rename broke event binding or render target lookup.', safeAction:'Edit visible text first, not structural selector names.', concept:'Selector stability matters for JS wiring.', meaning:'Markup ids/classes are often functional hooks, not just styling.', wrong:'Changing ids is safe if the UI still looks correct.', snippet:"<button id='refreshBtn'>Refresh</button>", snippetPrompt:'What can break if id changes but JS selector does not?', snippetAnswer:'The event handler may never bind, so button does nothing.', curriculum:'Owner Curriculum section 6', practice:'Practice Lab section 10' },
  { id:'state-render-event', label:'State / render / event flow', ownerFile:'js/app.js', avoidFile:'js/config.js', files:['js/app.js','js/state.js'], inspectFirst:'Check whether event handler runs, state changes, then render runs.', rootCause:'State changed without rerender or rerender reads wrong key.', safeAction:'Trace event -> state -> render before touching formulas.', concept:'UI updates depend on event-state-render continuity.', meaning:'Missing one link causes stale or broken UI output.', wrong:'UI always updates automatically after state mutation.', snippet:"state.threshold = next; renderVendorTable();", snippetPrompt:'Why pair state update with render call?', snippetAnswer:'Without render call, UI can remain stale despite new state.', curriculum:'Owner Curriculum sections 5 and 8', practice:'Practice Lab sections 24 and 27' },
  { id:'async-fetch-api', label:'async / fetch / API flow', ownerFile:'js/app.js', avoidFile:'index.html', files:['js/app.js','js/market.js'], inspectFirst:'Inspect Network response status and payload shape before UI edits.', rootCause:'Non-OK response or unexpected payload shape not handled correctly.', safeAction:'Add/verify non-OK guard and parse shape checks.', concept:'Async flow must guard failures before parsing.', meaning:'A failed request can look like UI bug if not handled clearly.', wrong:'If fetch runs, response shape is always safe to use.', snippet:"const res=await fetch(url); if(!res.ok) throw new Error(res.status);", snippetPrompt:'Why is the guard important?', snippetAnswer:'It fails early with clear cause instead of cascading parse errors.', curriculum:'Owner Curriculum section 5', practice:'Practice Lab sections 24 and 26' },
  { id:'frontend-ownership', label:'ScarabEV frontend ownership', ownerFile:'js/app.js', avoidFile:'js/scarabEngine.js', files:['js/app.js','js/config.js'], inspectFirst:'Confirm requested change belongs to rendering/events layer.', rootCause:'Change was made in wrong ownership file causing hidden side effects.', safeAction:'Route change to ownership file before writing code.', concept:'Ownership routing reduces regression risk.', meaning:'Right file placement is a safety control, not bureaucracy.', wrong:'Any file edit is fine if tests pass once.', snippet:"// UI text and render wiring live in js/app.js", snippetPrompt:'What maintenance rule is expressed here?', snippetAnswer:'UI behavior should stay in the UI ownership module.', curriculum:'Owner Curriculum section 6', practice:'Practice Lab section 10' },
  { id:'ev-formulas', label:'EV / formula understanding', ownerFile:'js/scarabEngine.js', avoidFile:'js/app.js', files:['js/scarabEngine.js','js/app.js'], inspectFirst:'Validate input data and mode readiness before formula edits.', rootCause:'Formula blamed for data-path issue (wrong payload/readiness).', safeAction:'Run read-only formula validation with known sample first.', concept:'Formula edits are high-risk and should be last step.', meaning:'Many wrong numbers come from data path, not formula math.', wrong:'Edit formula first whenever output looks odd.', snippet:"const weighted = sum / 3;", snippetPrompt:'What must be verified before changing this line?', snippetAnswer:'Input data validity, readiness gates, and expected sample outputs.', curriculum:'Owner Curriculum section 9', practice:'Practice Lab sections 24 and 29' },
  { id:'regex-token', label:'regex / token system', ownerFile:'js/regexEngine.js', avoidFile:'js/scarabEngine.js', files:['js/regexEngine.js','js/tokenSource.js','backend/src/tokens'], inspectFirst:'Check token source status and builder inputs before regex rewrites.', rootCause:'Invalid/missing token set or fallback path mismatch.', safeAction:'Verify token source and output length/fallback behavior first.', concept:'Regex output quality depends on token source quality.', meaning:'Broken inputs create broken regex even with correct builder code.', wrong:'Regex failures are always parser algorithm bugs.', snippet:"const out = buildRegex(prefix, vendorNames, keepNames);", snippetPrompt:'Which dependency is most critical here?', snippetAnswer:'Correct tokenized names and source availability.', curriculum:'Owner Curriculum section 6', practice:'Practice Lab sections 24 and 27' },
  { id:'csv-parsing', label:'CSV import / parsing', ownerFile:'js/app.js', avoidFile:'backend/src/routes', files:['js/app.js'], inspectFirst:'Check header mapping and coercion rules on known sample file.', rootCause:'Column mapping drift or numeric coercion issue.', safeAction:'Use known-good sample and compare parsed rows before UI patching.', concept:'Import reliability depends on structured parsing discipline.', meaning:'Wrong mapping silently poisons downstream totals.', wrong:'Global text replacement is a safe parser fix.', snippet:"const qty = Number(row.Quantity || 0);", snippetPrompt:'What failure should you guard against?', snippetAnswer:'Non-numeric or missing values causing NaN/incorrect totals.', curriculum:'Owner Curriculum section 8', practice:'Practice Lab section 27' },
  { id:'debugging-process', label:'debugging process', ownerFile:'js/app.js', avoidFile:'global refactor', files:['js/app.js','backend/src/routes'], inspectFirst:'Capture first error and first failing request before editing.', rootCause:'Panic edits across layers without evidence.', safeAction:'Evidence -> hypothesis -> tiny test -> verify.', concept:'Debugging is evidence sequencing, not guesswork.', meaning:'First error is usually highest-value clue.', wrong:'Start by rewriting the largest related file.', snippet:"console.error('first failure', err);", snippetPrompt:'Why log first failure location?', snippetAnswer:'It prevents chasing downstream cascade errors.', curriculum:'Owner Curriculum section 12', practice:'Practice Lab section 27' },
  { id:'backend-admin', label:'backend/admin basics', ownerFile:'backend/src/routes/*', avoidFile:'js/config.js', files:['backend/src/index.ts','backend/src/routes'], inspectFirst:'Verify route method/path and auth requirement on failing endpoint.', rootCause:'Path/method mismatch or missing guard header.', safeAction:'Check route mapping and expected request contract first.', concept:'Backend route contracts define safe mutation behavior.', meaning:'A correct frontend call can still fail if route contract mismatched.', wrong:'Frontend retries fix backend contract mismatch.', snippet:"if(method==='POST' && path==='/admin/ops/retry'){...}", snippetPrompt:'What is first diagnostic for 404 here?', snippetAnswer:'Method/path mismatch against expected route.', curriculum:'Owner Curriculum section 9', practice:'Practice Lab section 10' },
  { id:'worker-cache', label:'worker/cache/snapshot basics', ownerFile:'workers/market-worker/worker.js', avoidFile:'js/app.js', files:['workers/market-worker/worker.js','js/market.js'], inspectFirst:'Inspect cache age/stale metadata and worker response status.', rootCause:'Stale cache policy or failed refresh path.', safeAction:'Fix worker freshness path, do not bypass source model.', concept:'Worker-first cache policy is architectural guardrail.', meaning:'Bypassing worker can hide deeper reliability issues.', wrong:'Add direct upstream source in frontend as quick fix.', snippet:"return json({ data, meta:{stale,ageSeconds} });", snippetPrompt:'Which field explains stale warning?', snippetAnswer:'meta.stale and related age metadata.', curriculum:'Owner Curriculum section 9', practice:'Practice Lab section 24' },
  { id:'auth-session-csrf', label:'auth/session/CSRF safety', ownerFile:'backend/src/security/*', avoidFile:'js/app.js', files:['backend/src/security','backend/src/routes/auth'], inspectFirst:'Check csrf header/cookie pair and session validity.', rootCause:'Missing csrf token or expired session.', safeAction:'Re-authenticate and send proper mutation headers; never weaken checks.', concept:'Security controls must be fixed by request correctness, not relaxed guards.', meaning:'403 often means request context issue, not feature bug.', wrong:'Disable CSRF validation to unblock mutation.', snippet:"if(!csrfMatches(req)) return forbidden();", snippetPrompt:'What is safe response to repeated 403?', snippetAnswer:'Inspect csrf/session context, not remove guard.', curriculum:'Owner Curriculum section 16', practice:'Practice Lab section 27' },
  { id:'deploy-rollback', label:'deployment/rollback', ownerFile:'backend/wrangler*.toml', avoidFile:'formula edits in incident', files:['backend/wrangler.production.toml','backend/src'], inspectFirst:'Check diff scope and rollback plan before deploy.', rootCause:'Unscoped deploy with no rollback path.', safeAction:'Staging-first validation and explicit rollback steps.', concept:'Rollback is safety instrument, not failure.', meaning:'Fast rollback reduces blast radius during uncertainty.', wrong:'Keep patching in production without reverting.', snippet:"npm.cmd run typecheck && npm.cmd run test:build", snippetPrompt:'Why run these before deploy?', snippetAnswer:'They catch structural/type regressions before runtime impact.', curriculum:'Owner Curriculum section 9', practice:'Practice Lab section 23' },
  { id:'file-ownership', label:'file ownership', ownerFile:'module-specific ownership map', avoidFile:'unrelated files', files:['AGENTS.md','js/app.js','js/scarabEngine.js','workers/market-worker/worker.js'], inspectFirst:'Confirm ownership map before editing behavior.', rootCause:'Logic changed in wrong layer causing hidden drift.', safeAction:'Route by ownership, then edit smallest scope.', concept:'Ownership map is change-risk control.', meaning:'Correct location accelerates debugging and rollback.', wrong:'Cross-layer edits are fine if feature passes once.', snippet:"// EV math -> js/scarabEngine.js", snippetPrompt:'What does this routing rule protect?', snippetAnswer:'It keeps business logic centralized and testable.', curriculum:'Owner Curriculum section 6', practice:'Practice Lab section 10' },
  { id:'ai-handoff', label:'AI handoff / guardrails', ownerFile:'documentation + AGENTS.md', avoidFile:'blind patches', files:['AGENTS.md','backend/admin-ui-src/scripts/documentation.js'], inspectFirst:'Check whether requested change crosses risk boundaries.', rootCause:'AI followed vague request without ownership/risk constraints.', safeAction:'Ask for smallest safe scope and validation proof.', concept:'AI assistance is strongest with explicit constraints.', meaning:'Guardrails prevent plausible but unsafe edits.', wrong:'Accept generated patch without file-level reasoning.', snippet:"ALWAYS: make surgical patches and report validations", snippetPrompt:'What behavior is expected from this rule?', snippetAnswer:'Small scoped edits with explicit verification.', curriculum:'Owner Curriculum section 14', practice:'Practice Lab section 29' },
  { id:'practice-review', label:'Practice Lab review', ownerFile:'Practice-Lab.md', avoidFile:'skip retry loop', files:['Practice-Lab.md','Owner-Curriculum.md'], inspectFirst:'Review missed concepts and retry failed categories.', rootCause:'Practicing completion without reflection/replay.', safeAction:'Run replay variation and document root cause in plain English.', concept:'Deliberate replay builds durable independence.', meaning:'One success is weaker than repeatable success.', wrong:'Move on after first pass regardless of misses.', snippet:"if(blocked>30min){ switchToWalkthrough(); }", snippetPrompt:'What training behavior does this enforce?', snippetAnswer:'Structured self-recovery instead of panic edits.', curriculum:'Owner Curriculum section 17', practice:'Practice Lab sections 18 and 23' }
];

const QUIZ_TYPE_ROTATION = ['multiple-choice','code-reading','safest-next-action','scenario-debugging','matching','file-ownership','inspect-first','root-cause'];
const QUIZ_DIFFICULTIES = ['beginner','intermediate','advanced'];
const SAFETY_MODE_CATEGORY_IDS = new Set(['auth-session-csrf','deploy-rollback','worker-cache','backend-admin','ev-formulas','file-ownership']);
const SCENARIO_MODE_TYPES = new Set(['scenario-debugging','inspect-first','root-cause','safest-next-action']);
const CODE_MODE_TYPES = new Set(['code-reading','matching','multiple-choice']);

const KNOWLEDGE_CHECKS_STATE = {
  initialized: false,
  bank: [],
  examBank: [],
  history: [],
  examHistory: [],
  settings: {
    mode: 'quick-drill',
    dailyMode: 'quick-drill',
    examMode: 'exam-apprentice',
    customCategories: [],
    customDifficulties: ['beginner','intermediate'],
    customCount: 25
  },
  progression: {
    highestExamLevelPassed: '',
    weakCategoryTrend: {},
    lastRecommendations: []
  },
  distractorMemory: {
    seen: {},
    recent: []
  },
  missed: {},
  attempt: null,
  lastResult: null,
  screen: 'setup',
  context: null
};

function docsQuizEsc(v){ return docsEscHtml(v); }
function docsQuizClamp(n,min,max){ const x=Number(n); if(!Number.isFinite(x))return min; return Math.max(min,Math.min(max,x)); }
function docsQuizShuffle(arr){
  const out = arr.slice();
  for(let i=out.length-1;i>0;i-=1){
    const j=Math.floor(Math.random()*(i+1));
    const t=out[i]; out[i]=out[j]; out[j]=t;
  }
  return out;
}
function docsQuizRead(key,fallback){
  try{
    const raw=localStorage.getItem(key);
    if(!raw)return fallback;
    const parsed=JSON.parse(raw);
    return parsed==null?fallback:parsed;
  }catch(_){ return fallback; }
}
function docsQuizWrite(key,value){
  try{ localStorage.setItem(key,JSON.stringify(value)); }catch(_){}
}
function docsQuizModeById(id){
  return QUIZ_MODE_DEFS.find((m)=>m.id===id)||QUIZ_MODE_DEFS[0];
}
function docsQuizIsExamMode(modeId){
  return FINAL_EXAM_LEVEL_DEFS.some((m)=>m.id===modeId);
}
function docsQuizDailyModeById(id){
  return DAILY_QUIZ_MODE_DEFS.find((m)=>m.id===id)||DAILY_QUIZ_MODE_DEFS[0];
}
function docsQuizExamLevelById(id){
  return FINAL_EXAM_LEVEL_DEFS.find((m)=>m.id===id)||FINAL_EXAM_LEVEL_DEFS[0];
}
function docsQuizCategoryDefByLabel(label){
  return QUIZ_CATEGORY_DEFS.find((c)=>c.label===label) || null;
}
function docsQuizChoiceSig(category, type, text){
  const c = String(category||'unknown').toLowerCase().replace(/[^a-z0-9]+/g,'-');
  const t = String(type||'question').toLowerCase().replace(/[^a-z0-9]+/g,'-');
  const x = String(text||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(/\s+/).slice(0,8).join('-');
  return c+'|'+t+'|'+x;
}
function docsQuizWeakDistractorText(text){
  const v = String(text||'').toLowerCase();
  if(!v)return false;
  return /refactor|skip evidence|styling checks|visual check passes|architecture first|disable guards|unknown platform instability/.test(v);
}
function docsQuizDistractorVariants(q){
  const cat = docsQuizCategoryDefByLabel(q.category) || {};
  const owner = String(cat.ownerFile || (q.relatedFiles&&q.relatedFiles[0]) || 'owner module');
  const avoid = String(cat.avoidFile || (q.relatedFiles&&q.relatedFiles[q.relatedFiles.length-1]) || 'unrelated module');
  const helper = String((q.relatedFiles||[])[1] || owner);
  const symptom = q.type==='code-reading' ? 'snippet interpretation' : (q.type==='scenario-debugging' ? 'runtime failure' : 'behavior regression');
  return [
    {
      text: 'Start in '+avoid+' because the symptom is visible there, then move to '+owner+' only if needed.',
      whyTempting: 'Tempting because visible symptoms feel local, but first ownership is often elsewhere.',
      whenUseful: 'Useful after owner-layer checks if evidence points to this dependency.'
    },
    {
      text: 'Apply two quick fixes at once in '+owner+' and '+helper+' to recover faster, then isolate cause later.',
      whyTempting: 'Feels efficient under pressure, but bundled fixes blur causality and hide root cause.',
      whenUseful: 'Only during explicit emergency rollback bundles with documented scope.'
    },
    {
      text: 'Treat this as '+symptom+' only; skip network/runtime evidence and proceed with a local UI patch.',
      whyTempting: 'UI patches are fast, but this skips the request/state evidence needed for durable fixes.',
      whenUseful: 'After console and network checks confirm the issue is strictly presentation-layer.'
    },
    {
      text: 'Assume data contract is stable and optimize render flow first in '+owner+'.',
      whyTempting: 'Optimization feels productive, but unverified contracts can make optimizations hide defects.',
      whenUseful: 'Later, after contract and payload checks pass repeatedly.'
    },
    {
      text: 'Jump straight to '+avoid+' because similar incidents were fixed there previously.',
      whyTempting: 'Pattern memory can help, but reusing old fixes without fresh evidence often misroutes effort.',
      whenUseful: 'As a second hypothesis after reproducing current evidence.'
    }
  ];
}
function docsQuizChooseDistractorVariants(q, count){
  const mem = KNOWLEDGE_CHECKS_STATE.distractorMemory || { seen:{}, recent:[] };
  const recent = new Set((mem.recent||[]).slice(-120));
  return docsQuizDistractorVariants(q).map((d)=>{
    const sig = docsQuizChoiceSig(q.category, q.type, d.text);
    const seenCount = Number((mem.seen||{})[sig]||0);
    const recentPenalty = recent.has(sig) ? 5 : 0;
    return Object.assign({ sig, score: (Math.random()*1.5) - seenCount - recentPenalty }, d);
  }).sort((a,b)=>b.score-a.score).slice(0,Math.max(1,count||2));
}
function docsQuizRecordDistractorExposure(q){
  if(!q||!Array.isArray(q.preparedChoices))return;
  const mem = KNOWLEDGE_CHECKS_STATE.distractorMemory || { seen:{}, recent:[] };
  const seen = Object.assign({}, mem.seen||{});
  const recent = Array.isArray(mem.recent) ? mem.recent.slice() : [];
  q.preparedChoices.forEach((choice)=>{
    const sig = docsQuizChoiceSig(q.category, q.type, choice.text);
    seen[sig] = Number(seen[sig]||0)+1;
    recent.push(sig);
  });
  KNOWLEDGE_CHECKS_STATE.distractorMemory = {
    seen,
    recent: recent.slice(-240)
  };
  docsQuizWrite(QUIZ_STORAGE_KEYS.distractorMemory, KNOWLEDGE_CHECKS_STATE.distractorMemory);
}

function docsQuizBuildQuestion(cat, idx){
  const difficulty = idx < 6 ? 'beginner' : (idx < 14 ? 'intermediate' : 'advanced');
  const type = QUIZ_TYPE_ROTATION[idx % QUIZ_TYPE_ROTATION.length];
  const stemPrefix = difficulty==='advanced'
    ? 'Advanced Scenario'
    : (difficulty==='intermediate' ? 'Scenario' : 'Starter Scenario');
  const filesHint = (cat.files||[]).slice(0,2).join(', ') || 'related module files';
  const scenarioHint = 'Category: '+cat.label+'. Start in: '+filesHint+'.';
  const ownerChoice = String(cat.ownerFile||'module owner file');
  const avoidChoice = String(cat.avoidFile||'unrelated file');
  const altOwnerA = ((cat.files||[]).find((f)=>String(f)!==ownerChoice) || 'index.html');
  const altOwnerB = ((cat.files||[]).find((f)=>String(f)!==ownerChoice && String(f)!==altOwnerA) || avoidChoice || 'js/config.js');
  const base = {
    id: cat.id+'-'+String(idx+1).padStart(2,'0')+'-'+type,
    category: cat.label,
    difficulty,
    type,
    relatedFiles: cat.files.slice(),
    relatedSections: [cat.curriculum, cat.practice],
    recommendedFollowUp: cat.practice,
    plainEnglish: 'In plain English: treat this as a sequence problem. Confirm the right file, inspect evidence first, then make the smallest safe change.',
    misconception: 'Common beginner misunderstanding: jumping to broad edits before validating ownership, state path, and request evidence.',
    followUpPrompt: 'Write a 2-3 sentence plain-English explanation of this concept and identify one file to review next.'
  };
  const makeChoice = (text, whyBest, whyTempting, whenUseful)=>({ text, whyBest, whyTempting, whenUseful });
  const uniqueChoices = (arr)=>{
    const seen=new Set();
    return arr.filter((c)=>{
      const key=String(c&&c.text||'').trim().toLowerCase();
      if(!key||seen.has(key))return false;
      seen.add(key);
      return true;
    }).slice(0,4);
  };

  if(type==='multiple-choice'){
    const choiceSpecs = uniqueChoices([
      makeChoice(
        cat.meaning+' This aligns with ScarabEV ownership guardrails.',
        'It reflects correct ownership boundaries, evidence-first sequencing, and safe change discipline.',
        '',
        ''
      ),
      makeChoice(
        cat.wrong,
        '',
        'It often sounds true because it is partially related, but it misses the key risk boundary or sequence.',
        'Can be relevant after first diagnostics, not as the primary rule.'
      ),
      makeChoice(
        'Make cross-layer edits first, then narrow scope only if users report breakage.',
        '',
        'Feels fast, but it creates noisy diffs and hides root cause.',
        'Only during controlled planned refactors with full test coverage and explicit scope.'
      ),
      makeChoice(
        'If one quick visual check passes, skip deeper verification and move on.',
        '',
        'Tempting under time pressure, but it misses latent regressions.',
        'A quick visual check is useful as a first smoke check, never the only check.'
      )
    ]);
    return Object.assign(base,{
      prompt: stemPrefix+': '+scenarioHint+' Which statement is the safest and most correct for this maintenance task?',
      choiceSpecs,
      correctAnswer: 0,
      explanation: cat.meaning+' The correct approach is scoped, evidence-based, and verifiable. Broad edits and shallow checks create regressions that are harder to unwind.'
    });
  }
  if(type==='code-reading'){
    const choiceSpecs = uniqueChoices([
      makeChoice(
        cat.snippetAnswer,
        'This answer respects what the code actually does instead of inferring unrelated guarantees.',
        '',
        ''
      ),
      makeChoice(
        'It proves backend/auth behavior is valid even without request inspection.',
        '',
        'Common leap: reading one snippet as proof of whole-system correctness.',
        'Only after confirming request/response contracts and guard outcomes.'
      ),
      makeChoice(
        'It guarantees render output is correct even if state keys are mismatched.',
        '',
        'Tempting because rendering is visible, but mismatched state keys still break UI logic.',
        'Relevant as a downstream check after state-shape verification.'
      ),
      makeChoice(
        'It means deployment checks can be skipped when this line exists.',
        '',
        'Shortcut reasoning: local code confidence does not replace build/runtime validation.',
        'Never appropriate; checks remain required.'
      )
    ]);
    return Object.assign(base,{
      prompt: stemPrefix+': '+cat.snippetPrompt+' Choose the best plain-English interpretation of this snippet.',
      snippet: cat.snippet,
      choiceSpecs,
      correctAnswer: 0,
      explanation: cat.snippetAnswer+' Read code by identifying input shape, transformation/guard behavior, and user-visible side effects.'
    });
  }
  if(type==='safest-next-action'){
    const choiceSpecs = uniqueChoices([
      makeChoice(
        cat.safeAction,
        'Best first move: high-signal evidence with minimal risk and clear rollback path.',
        '',
        ''
      ),
      makeChoice(
        'Patch production immediately and investigate root cause only if errors continue.',
        '',
        'Feels pragmatic during urgency, but often cements wrong assumptions.',
        'Reasonable only for a narrowly scoped rollback/hotfix after initial evidence confirms direction.'
      ),
      makeChoice(
        'Temporarily disable safety guards to speed up debugging.',
        '',
        'Tempting shortcut when blocked, but it destroys trust boundaries and creates new risk.',
        'Almost never acceptable in production paths.'
      ),
      makeChoice(
        'Start with a broad refactor so the bug has fewer places to hide.',
        '',
        'Appeals to cleanup instinct, but blocks fast root-cause isolation.',
        'Useful later as a separate hardening task after the bug is fixed and verified.'
      )
    ]);
    return Object.assign(base,{
      prompt: stemPrefix+': You see a bug in '+cat.label+'. What is the safest NEXT action before you edit behavior?',
      choiceSpecs,
      correctAnswer: 0,
      explanation: cat.safeAction+' Sequencing matters: evidence first, scoped change second, verification third.'
    });
  }
  if(type==='scenario-debugging'){
    const choiceSpecs = uniqueChoices([
      makeChoice(
        'Capture first error and first failing request, form one hypothesis, run one tiny test.',
        'This maximizes diagnostic signal while preserving change safety.',
        '',
        ''
      ),
      makeChoice(
        'Apply several likely fixes at once to maximize your chance of quick recovery.',
        '',
        'Tempting under pressure, but multi-fix bundles make causality unclear.',
        'Can be acceptable only in carefully planned rollback bundles, not initial diagnosis.'
      ),
      makeChoice(
        'Skip evidence and start with the largest related file to save time.',
        '',
        'Feels decisive, but high-volume edits without evidence usually prolong incidents.',
        'Large-file review is useful later for hardening once root cause is known.'
      ),
      makeChoice(
        'Touch formula/security code first because those areas are usually the cause.',
        '',
        'High-risk-layer bias: these areas are critical but should be touched only with evidence.',
        'Appropriate only when logs/requests clearly indicate those boundaries.'
      )
    ]);
    return Object.assign(base,{
      prompt: stemPrefix+': A regression appears in '+cat.label+'. Which debug approach gives the highest signal with lowest risk?',
      choiceSpecs,
      correctAnswer: 0,
      explanation: 'Evidence-first debugging avoids guesswork, preserves rollback clarity, and prevents accidental blast-radius expansion.'
    });
  }
  if(type==='matching'){
    const term = 'state';
    const pairA = term+' -> current runtime app memory used by render logic';
    const pairB = term+' -> permanent backend storage table in D1';
    const pairC = term+' -> CSS selector naming convention';
    const pairD = term+' -> deployment environment file';
    const choiceSpecs = [
      makeChoice(
        pairA,
        'Correct: in ScarabEV, state is runtime memory read by render paths.',
        '',
        ''
      ),
      makeChoice(
        pairB,
        '',
        'Tempting because D1 is also “data,” but this question is specifically about frontend runtime state.',
        'Useful distinction when tracing frontend vs backend responsibility.'
      ),
      makeChoice(
        pairC,
        '',
        'Tempting because selectors affect UI behavior, but they are not runtime state values.',
        'Relevant during DOM wiring checks, not state definition checks.'
      ),
      makeChoice(
        pairD,
        '',
        'Tempting because environments change behavior, but they are config context, not state.',
        'Useful when debugging endpoint/environment resolution.'
      )
    ];
    return Object.assign(base,{
      prompt: stemPrefix+': Matching drill. Term = "'+term+'". Choose the ONE option that correctly defines this term in ScarabEV runtime behavior.',
      choiceSpecs,
      correctAnswer: 0,
      matchingPairs: [
        { term: 'state', meaning: 'current runtime app memory used by render logic' },
        { term: 'render', meaning: 'update visible UI from state' },
        { term: 'fetch', meaning: 'request remote data' },
        { term: 'CSRF', meaning: 'mutation request forgery protection check' }
      ],
      explanation: 'Correct matching supports fast debugging language: if state is wrong, render output will also be wrong.'
    });
  }
  if(type==='file-ownership'){
    const choiceSpecs = uniqueChoices([
      makeChoice(
        ownerChoice,
        'Best first owner based on module responsibility boundaries.',
        '',
        ''
      ),
      makeChoice(
        avoidChoice,
        '',
        'Tempting when symptoms appear nearby, but this starts in the wrong layer.',
        'May become relevant after owner-layer evidence indicates upstream/downstream dependency.'
      ),
      makeChoice(
        altOwnerA,
        '',
        'Plausible because it is related, but not the primary ownership path for this behavior.',
        'Useful for secondary verification after owner-layer trace.'
      ),
      makeChoice(
        altOwnerB,
        '',
        'Looks nearby in codebase, but weak as first ownership choice for this scenario.',
        'Potentially useful for integration checks later.'
      )
    ]);
    return Object.assign(base,{
      prompt: stemPrefix+': You need to change behavior in '+cat.label+'. Which file should you inspect/edit first?',
      choiceSpecs,
      correctAnswer: 0,
      explanation: 'Ownership routing prevents cross-layer drift. Start in '+ownerChoice+' and keep edits scoped before expanding.'
    });
  }
  if(type==='inspect-first'){
    const choiceSpecs = uniqueChoices([
      makeChoice(
        cat.inspectFirst,
        'Best first inspection: highest signal for verifying assumptions and choosing correct layer.',
        '',
        ''
      ),
      makeChoice(
        'Refactor the module first so future debugging is easier.',
        '',
        'Attractive long-term idea, but wrong timing for first diagnosis.',
        'Good follow-up after issue resolution and stabilization.'
      ),
      makeChoice(
        'Change data-source architecture before checking current evidence.',
        '',
        'Tempting when current path feels painful, but architecture changes hide immediate root cause.',
        'Possibly valid later as roadmap work if repeated evidence supports it.'
      ),
      makeChoice(
        'Skip console/network evidence and start with visual styling checks.',
        '',
        'Feels easy and quick, but often misses the failing contract or runtime error.',
        'Styling checks are appropriate later if logic/data paths are already healthy.'
      )
    ]);
    return Object.assign(base,{
      prompt: stemPrefix+': A bug appears in '+cat.label+'. What should you inspect FIRST before writing a fix?',
      choiceSpecs,
      correctAnswer: 0,
      explanation: cat.inspectFirst+' First-check quality determines whether you solve the real problem or chase symptoms.'
    });
  }
  const choiceSpecs = uniqueChoices([
    makeChoice(
      cat.rootCause,
      'Most plausible first root-cause hypothesis given this category and symptom pattern.',
      '',
      ''
    ),
    makeChoice(
      'A purely visual theme contrast issue in unrelated docs styles.',
      '',
      'Plausible if symptom is visual, but weak for logic/data flow failures in this category.',
      'Valid later if evidence isolates the issue to presentation-only behavior.'
    ),
    makeChoice(
      'Random network latency alone, regardless of request shape or status.',
      '',
      'Tempting external blame, but ignores contract/status/payload evidence.',
      'Latency becomes relevant after request correctness and payload handling are confirmed.'
    ),
    makeChoice(
      'General user confusion with no path/contract mismatch in code.',
      '',
      'Can happen, but unsafe as default explanation before verifying runtime evidence.',
      'Useful later for UX improvements once technical path is validated.'
    )
  ]);
  return Object.assign(base,{
    prompt: stemPrefix+': Given this symptom in '+cat.label+', what is the MOST likely root cause to verify first?',
    choiceSpecs,
    correctAnswer: 0,
    explanation: cat.rootCause+' Treat this as a hypothesis and verify with concrete evidence before editing logic.'
  });
}

function docsQuizBuildExamQuestion(cat, level, idx){
  const tier = String(level.label||'Apprentice');
  const difficulty = tier==='Apprentice'
    ? (idx%2===0?'intermediate':'advanced')
    : 'advanced';
  const examTypes = ['advanced-mc','scenario-debugging','first-3-actions','rank-priority','multi-select','code-reading-deep','code-editing-challenge','deploy-rollback-judgment','file-ownership-map','root-cause-reasoning'];
  const type = examTypes[idx % examTypes.length];
  const ownerChoice = String(cat.ownerFile||'module owner file');
  const avoidChoice = String(cat.avoidFile||'unrelated file');
  const helperFile = ((cat.files||[]).find((f)=>String(f)!==ownerChoice) || 'js/app.js');
  const secondHelper = ((cat.files||[]).find((f)=>String(f)!==ownerChoice && String(f)!==helperFile) || 'js/state.js');
  const base = {
    id: 'exam-'+level.id+'-'+cat.id+'-'+String(idx+1).padStart(2,'0')+'-'+type,
    category: cat.label,
    difficulty,
    type,
    track: 'final-exam',
    examLevel: level.label,
    relatedFiles: (cat.files||[]).slice(),
    relatedSections: [cat.curriculum, cat.practice],
    recommendedFollowUp: cat.practice,
    plainEnglish: 'In plain English: this exam checks whether you can choose the safest first move, not just recognize keywords.',
    misconception: 'Common beginner misunderstanding: picking a technically possible action that is still wrong because the order, layer, or risk profile is off.',
    followUpPrompt: 'Rewrite your decision path in 3 lines: first check, second check, first safe edit.'
  };
  const makeChoice = (text, whyBest, whyTempting, whenUseful)=>({ text, whyBest, whyTempting, whenUseful });
  if(type==='advanced-mc'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): A recommendation output is wrong after a UI tweak. Which action is best first?',
      choiceSpecs: [
        makeChoice('Trace the exact event -> state -> render path in '+ownerChoice+' and verify inputs before touching formulas.', 'This is safest: confirm where behavior diverged before changing high-risk math.', '', ''),
        makeChoice('Patch '+cat.avoidFile+' immediately because symptoms appear there.', '', 'Tempting because the symptom is visible, but this is often the wrong ownership layer first.', 'Use later only if evidence points there after initial trace.'),
        makeChoice('Run a production deploy with a quick guess and monitor feedback.', '', 'Feels fast, but increases blast radius without evidence.', 'Only after staging validation and rollback plan are ready.'),
        makeChoice('Refactor related modules first to make debugging easier.', '', 'Tempting cleanup instinct, but it destroys signal during diagnosis.', 'Refactor after root cause is fixed and verified.')
      ],
      correctAnswer: 0,
      explanation: 'Exam-grade reasoning prioritizes evidence order and risk reduction. Correct ownership + trace beats fast guesses.',
      snippet: "function onSlider(v){ state.threshold=v; renderVendorTable(); }"
    });
  }
  if(type==='scenario-debugging'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Weighted EV is blank for one dataset. What is the most useful first check?',
      choiceSpecs: [
        makeChoice('Inspect readiness gates and source payload shape in '+helperFile+' before formula edits.', 'Blank output often means readiness/data path gates, not formula failure.', '', ''),
        makeChoice('Lower display precision and rounding rules first.', '', 'Rounding issues are plausible but rarely the first cause of blank values.', 'Useful later once data path is confirmed valid.'),
        makeChoice('Disable guards so value always renders.', '', 'Tempting visual fix, but it hides real faults and creates unsafe behavior.', 'Never as first action; only in isolated debug branches.'),
        makeChoice('Move calculation into UI render block for quick control.', '', 'Tempting shortcut, but violates ownership and multiplies bugs.', 'Only as deliberate architecture change with tests, not incident response.')
      ],
      correctAnswer: 0,
      explanation: 'Blank values are usually gated. Validate gate conditions and payload shape before touching math.',
      snippet: "if(!weightMeta.ready){ return null; }"
    });
  }
  if(type==='first-3-actions'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Choose the BEST first 3 actions in order for a failing mutation request (403).',
      choiceSpecs: [
        makeChoice('1) Confirm session valid 2) confirm CSRF header/cookie pairing 3) inspect route auth requirement in backend route.', 'Best order: request context first, then contract details.', '', ''),
        makeChoice('1) Disable CSRF check 2) retest quickly 3) re-enable later.', '', 'Tempting for speed, but unsafe and teaches the wrong habit.', 'Never as a normal workflow.'),
        makeChoice('1) Rewrite frontend form 2) change table rendering 3) clear browser cache.', '', 'Mostly wrong layer and wrong sequence for 403.', 'Cache clear can help later if token state is stale.'),
        makeChoice('1) Deploy to production to compare 2) inspect logs after 3) rollback if needed.', '', 'High-risk sequence with avoidable blast radius.', 'Only after staging and contract checks fail.')
      ],
      correctAnswer: 0,
      explanation: 'Ownership-ready debugging means verify auth/session request context first, not bypass guards.',
      snippet: "if(!csrfMatches(req)) return forbidden();"
    });
  }
  if(type==='rank-priority'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Which ranked investigation priority is strongest for stale market prices?',
      choiceSpecs: [
        makeChoice('Worker freshness metadata -> worker refresh/cron path -> UI parse/render assumptions.', 'Correct risk-aware order isolates source reliability before UI tweaks.', '', ''),
        makeChoice('UI color badges -> table styling -> FAQ text.', '', 'Visible UI changes are tempting but not causal for stale data.', 'Useful for UX polish only after data source is healthy.'),
        makeChoice('Formula constants -> auth cookies -> docs wording.', '', 'Wrong subsystem sequence for freshness incidents.', 'Could be relevant in unrelated incidents.'),
        makeChoice('Directly bypass worker and fetch upstream in frontend.', '', 'Tempting shortcut under pressure; violates worker-first architecture.', 'Only with explicit architecture approval.')
      ],
      correctAnswer: 0,
      explanation: 'Priority ranking quality is a core ownership skill: source reliability first, then downstream render.',
      snippet: "return json({ data, meta:{ stale, ageSeconds } });"
    });
  }
  if(type==='multi-select'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Multi-select synthesis. Which bundled answer contains ONLY safe first checks?',
      choiceSpecs: [
        makeChoice('Inspect console first error, inspect Network failing request, trace owning event handler in '+ownerChoice+'.', 'This bundle keeps scope tight and evidence-rich.', '', ''),
        makeChoice('Disable protections, force render defaults, then inspect logs.', '', 'Tempting because it creates visible movement, but it destroys reliable diagnosis signal.', 'Forcing defaults is only useful in isolated local repros after baselines are captured.'),
        makeChoice('Rename selectors, refactor state structure, then retest.', '', 'Large coupled edits too early; high regression risk.', 'Refactors belong after stable diagnosis.'),
        makeChoice('Patch worker cache TTL and backend routes simultaneously.', '', 'Changes multiple layers before proving root cause.', 'Layer-crossing edits can be useful only when evidence implicates both layers.')
      ],
      correctAnswer: 0,
      explanation: 'This tests judgment under ambiguity: choose the answer set that is safe, ordered, and evidence-first.',
      snippet: "console.error('first failure', err);"
    });
  }
  if(type==='code-reading-deep'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Code interpretation. What does this branch really guarantee?',
      choiceSpecs: [
        makeChoice('It short-circuits on missing data and prevents downstream render from pretending values are valid.', 'Correct: guard clauses protect correctness and error clarity.', '', ''),
        makeChoice('It guarantees backend data is correct forever after one good fetch.', '', 'Tempting over-generalization from a local guard.', 'You can trust this only for the current execution branch.'),
        makeChoice('It automatically retries all failed calls.', '', 'Common assumption, but no retry logic is shown.', 'Retries require explicit code paths.'),
        makeChoice('It sanitizes auth and session state globally.', '', 'Wrong concern domain for this snippet.', 'Auth/session checks belong in dedicated security layers.')
      ],
      correctAnswer: 0,
      explanation: 'Deep code reading means distinguishing local guarantees from global assumptions.',
      snippet: "if(!rows || !rows.length){ setEmptyState(); return; }\nrenderTable(rows.filter(Boolean));"
    });
  }
  if(type==='code-editing-challenge'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Which patch is safest for a button click that no longer triggers?',
      choiceSpecs: [
        makeChoice('Verify selector id match, confirm listener registration path, then apply smallest selector/event binding fix.', 'Best fix strategy: prove wiring break, then patch minimally.', '', ''),
        makeChoice('Duplicate listener on document and window to ensure click always catches.', '', 'Tempting brute force, but creates duplicate side effects.', 'Can be used temporarily in diagnostics, not final behavior.'),
        makeChoice('Move button handling to backend route.', '', 'Wrong ownership layer for client click binding.', 'Backend changes only when request contract is broken.'),
        makeChoice('Rewrite the tab system completely.', '', 'Over-scoped response to a local wiring fault.', 'Only with explicit redesign goals.')
      ],
      correctAnswer: 0,
      explanation: 'Editing challenge: pick the patch path that keeps behavior stable and diagnosable.',
      snippet: "document.querySelector('#refreshBtn')?.addEventListener('click', onRefresh);"
    });
  }
  if(type==='deploy-rollback-judgment'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): A staging deploy passes typecheck but behavior is unstable. Best release judgment?',
      choiceSpecs: [
        makeChoice('Hold production deploy, isolate issue with targeted tests, keep rollback plan ready.', 'Safest ownership call: uncertainty means contain blast radius.', '', ''),
        makeChoice('Deploy anyway to gather faster production feedback.', '', 'Tempting speed bias, but risky with known instability.', 'Can be appropriate only for low-risk changes with explicit acceptance of risk.'),
        makeChoice('Hotfix directly in production without staging retry.', '', 'High-risk workflow that skips normal safety controls.', 'Emergency-only and still should preserve rollback steps.'),
        makeChoice('Disable checks so deployment can proceed cleanly.', '', 'Destroys confidence signals and increases incident risk.', 'Never recommended.')
      ],
      correctAnswer: 0,
      explanation: 'Risk management score depends on choosing containment over velocity when confidence is low.',
      snippet: "npm.cmd run typecheck && npm.cmd run test:build"
    });
  }
  if(type==='file-ownership-map'){
    return Object.assign(base,{
      prompt: 'Final Exam ('+tier+'): Ownership mapping. Which file should you inspect first for this UI behavior issue?',
      choiceSpecs: [
        makeChoice(ownerChoice, 'Correct owner file selection prevents wrong-layer edits and wasted debugging.', '', ''),
        makeChoice(avoidChoice, '', 'Tempting because it looks adjacent, but ownership mismatch is a common failure path.', 'May be relevant later if data source evidence points there.'),
        makeChoice(helperFile, '', 'Related but often not first owner location for this symptom.', 'Useful second check after owner file trace.'),
        makeChoice(secondHelper, '', 'Plausible support file, but not best first owner for this issue.', 'Useful later for dependent value validation.')
      ],
      correctAnswer: 0,
      explanation: 'Ownership mapping is exam-critical: first file choice strongly affects speed and safety.'
    });
  }
  return Object.assign(base,{
    prompt: 'Final Exam ('+tier+'): What is the most likely root cause to test first for this symptom path?',
    choiceSpecs: [
      makeChoice(cat.rootCause, 'This root-cause hypothesis aligns with ownership boundaries and observable evidence.', '', ''),
      makeChoice('A broad visual style conflict unrelated to data/event flow.', '', 'Tempting because visual symptoms are obvious, but often downstream only.', 'Useful after core data/event path is proven healthy.'),
      makeChoice('Random user behavior with no reproducible runtime evidence.', '', 'Possible in rare cases, but weak first hypothesis without logs/requests.', 'Use after reproducible technical causes are ruled out.'),
      makeChoice('Unknown platform instability; skip targeted diagnosis.', '', 'Tempting under stress, but avoids learnable root-cause work.', 'Only after concrete platform incidents are confirmed.')
    ],
    correctAnswer: 0,
    explanation: 'Root-cause reasoning is scored by evidence quality and first-check efficiency.'
  });
}

function docsQuizBuildBank(){
  const out = [];
  QUIZ_CATEGORY_DEFS.forEach((cat)=>{
    for(let i=0;i<18;i+=1){
      out.push(docsQuizBuildQuestion(cat, i));
    }
  });
  return out;
}
function docsQuizBuildExamBank(){
  const out = [];
  FINAL_EXAM_LEVEL_DEFS.forEach((level)=>{
    QUIZ_CATEGORY_DEFS.forEach((cat)=>{
      for(let i=0;i<10;i+=1){
        out.push(docsQuizBuildExamQuestion(cat, level, i));
      }
    });
  });
  return out;
}

function docsQuizInitState(){
  if(KNOWLEDGE_CHECKS_STATE.initialized)return;
  KNOWLEDGE_CHECKS_STATE.bank = docsQuizBuildBank();
  KNOWLEDGE_CHECKS_STATE.examBank = docsQuizBuildExamBank();
  KNOWLEDGE_CHECKS_STATE.history = docsQuizRead(QUIZ_STORAGE_KEYS.history, []);
  KNOWLEDGE_CHECKS_STATE.examHistory = docsQuizRead(QUIZ_STORAGE_KEYS.examHistory, []);
  KNOWLEDGE_CHECKS_STATE.settings = Object.assign({}, KNOWLEDGE_CHECKS_STATE.settings, docsQuizRead(QUIZ_STORAGE_KEYS.settings, {}));
  if(!KNOWLEDGE_CHECKS_STATE.settings.dailyMode){
    KNOWLEDGE_CHECKS_STATE.settings.dailyMode = docsQuizIsExamMode(KNOWLEDGE_CHECKS_STATE.settings.mode) ? 'quick-drill' : (KNOWLEDGE_CHECKS_STATE.settings.mode||'quick-drill');
  }
  if(!KNOWLEDGE_CHECKS_STATE.settings.examMode){
    KNOWLEDGE_CHECKS_STATE.settings.examMode = 'exam-apprentice';
  }
  KNOWLEDGE_CHECKS_STATE.progression = Object.assign({}, KNOWLEDGE_CHECKS_STATE.progression, docsQuizRead(QUIZ_STORAGE_KEYS.progression, {}));
  KNOWLEDGE_CHECKS_STATE.distractorMemory = Object.assign({}, KNOWLEDGE_CHECKS_STATE.distractorMemory, docsQuizRead(QUIZ_STORAGE_KEYS.distractorMemory, {}));
  KNOWLEDGE_CHECKS_STATE.missed = docsQuizRead(QUIZ_STORAGE_KEYS.missed, {});
  KNOWLEDGE_CHECKS_STATE.initialized = true;
}

function docsQuizGetBreakdown(questions, responses, field){
  const map = {};
  questions.forEach((q)=>{
    const k = q[field];
    if(!map[k])map[k]={correct:0,total:0,percent:0};
    map[k].total += 1;
    const r = responses[q.id];
    if(r&&r.correct)map[k].correct += 1;
  });
  Object.keys(map).forEach((k)=>{
    const row = map[k];
    row.percent = row.total ? Math.round((row.correct/row.total)*100) : 0;
  });
  return map;
}

function docsQuizFindStrongWeak(categoryBreakdown){
  const rows = Object.keys(categoryBreakdown).map((k)=>({ category:k, percent:categoryBreakdown[k].percent, total:categoryBreakdown[k].total }));
  rows.sort((a,b)=>b.percent-a.percent || b.total-a.total);
  return {
    strongest: rows.slice(0,3).map((r)=>r.category),
    weakest: rows.slice(-3).reverse().map((r)=>r.category)
  };
}

function docsQuizAllAttempts(){
  const out = (KNOWLEDGE_CHECKS_STATE.history||[]).concat(KNOWLEDGE_CHECKS_STATE.examHistory||[]);
  out.sort((a,b)=>new Date(a.timestamp).getTime()-new Date(b.timestamp).getTime());
  return out;
}

function docsQuizWeakCategoriesFromHistory(){
  const hist = docsQuizAllAttempts();
  if(!hist.length)return [];
  const agg = {};
  hist.slice(-30).forEach((a)=>{
    const b = a.categoryBreakdown||{};
    Object.keys(b).forEach((k)=>{
      if(!agg[k])agg[k]={correct:0,total:0};
      agg[k].correct += Number(b[k].correct||0);
      agg[k].total += Number(b[k].total||0);
    });
  });
  return Object.keys(agg)
    .map((k)=>({k,p:agg[k].total?Math.round((agg[k].correct/agg[k].total)*100):0}))
    .sort((a,b)=>a.p-b.p)
    .slice(0,5)
    .map((x)=>x.k);
}

function docsQuizRecentQuestionIds(){
  const out = new Set();
  docsQuizAllAttempts().slice(-4).forEach((a)=>{
    (a.questionIds||[]).forEach((id)=>out.add(id));
  });
  return out;
}

function docsQuizFilterPool(modeId){
  const isExam = docsQuizIsExamMode(modeId);
  const bank = isExam ? KNOWLEDGE_CHECKS_STATE.examBank : KNOWLEDGE_CHECKS_STATE.bank;
  const settings = KNOWLEDGE_CHECKS_STATE.settings;
  if(isExam){
    const level = docsQuizExamLevelById(modeId);
    return bank.filter((q)=>q.examLevel===level.label);
  }
  if(modeId==='scenario-debugging'){
    return bank.filter((q)=>SCENARIO_MODE_TYPES.has(q.type));
  }
  if(modeId==='code-reading'){
    return bank.filter((q)=>CODE_MODE_TYPES.has(q.type));
  }
  if(modeId==='safety-operations'){
    return bank.filter((q)=>{
      const catId = QUIZ_CATEGORY_DEFS.find((c)=>c.label===q.category);
      return catId && SAFETY_MODE_CATEGORY_IDS.has(catId.id);
    });
  }
  if(modeId==='custom-practice'){
    const categoryFilter = (settings.customCategories||[]);
    const diffFilter = (settings.customDifficulties||[]);
    return bank.filter((q)=>{
      const catOk = !categoryFilter.length || categoryFilter.includes(q.category);
      const diffOk = !diffFilter.length || diffFilter.includes(q.difficulty);
      return catOk && diffOk;
    });
  }
  if(modeId==='weak-areas'){
    const weak = new Set(docsQuizWeakCategoriesFromHistory());
    if(!weak.size)return bank;
    const filtered = bank.filter((q)=>weak.has(q.category));
    return filtered.length ? filtered : bank;
  }
  return bank;
}

function docsQuizQuestionCountForMode(modeId){
  if(docsQuizIsExamMode(modeId)){
    return docsQuizExamLevelById(modeId).count;
  }
  if(modeId==='custom-practice'){
    return docsQuizClamp(KNOWLEDGE_CHECKS_STATE.settings.customCount, 5, 60);
  }
  return docsQuizDailyModeById(modeId).count;
}

function docsQuizPrepareQuestion(q){
  let rawChoices = Array.isArray(q.choiceSpecs) && q.choiceSpecs.length
    ? q.choiceSpecs.map((spec,idx)=>({
        text: String(spec.text||''),
        whyBest: String(spec.whyBest||''),
        whyTempting: String(spec.whyTempting||''),
        whenUseful: String(spec.whenUseful||''),
        isCorrect: idx===q.correctAnswer
      }))
    : (q.choices||[]).map((txt,idx)=>({ text:txt, whyBest:'', whyTempting:'', whenUseful:'', isCorrect:idx===q.correctAnswer }));
  const fallbackPool = docsQuizChooseDistractorVariants(q, 3);
  let fallbackIdx = 0;
  rawChoices = rawChoices.map((choice)=>{
    if(choice.isCorrect)return choice;
    if(!docsQuizWeakDistractorText(choice.text))return choice;
    const repl = fallbackPool[fallbackIdx] || fallbackPool[fallbackPool.length-1];
    fallbackIdx += 1;
    if(!repl)return choice;
    return {
      text: repl.text,
      whyBest: choice.whyBest,
      whyTempting: repl.whyTempting || choice.whyTempting,
      whenUseful: repl.whenUseful || choice.whenUseful,
      isCorrect: false
    };
  });
  const uniqueByText = [];
  const seenText = new Set();
  rawChoices.forEach((choice)=>{
    const k = String(choice.text||'').trim().toLowerCase();
    if(!k || seenText.has(k))return;
    seenText.add(k);
    uniqueByText.push(choice);
  });
  rawChoices = uniqueByText;
  while(rawChoices.length < 4 && fallbackIdx < fallbackPool.length){
    const repl = fallbackPool[fallbackIdx];
    fallbackIdx += 1;
    const k = String(repl&&repl.text||'').trim().toLowerCase();
    if(!k || seenText.has(k))continue;
    seenText.add(k);
    rawChoices.push({
      text: repl.text,
      whyBest: '',
      whyTempting: repl.whyTempting||'Tempting but weaker first move.',
      whenUseful: repl.whenUseful||'Useful after first evidence checks.',
      isCorrect: false
    });
  }
  const choices = rawChoices.map((c,idx)=>Object.assign({ id:'opt-'+(idx+1)+'-'+Math.random().toString(36).slice(2,6) }, c));
  const mem = KNOWLEDGE_CHECKS_STATE.distractorMemory || { seen:{}, recent:[] };
  const recent = new Set((mem.recent||[]).slice(-120));
  const shuffled = choices.slice().sort((a,b)=>{
    const sigA = docsQuizChoiceSig(q.category, q.type, a.text);
    const sigB = docsQuizChoiceSig(q.category, q.type, b.text);
    const seenA = Number((mem.seen||{})[sigA]||0) + (recent.has(sigA)?3:0);
    const seenB = Number((mem.seen||{})[sigB]||0) + (recent.has(sigB)?3:0);
    const scoreA = (a.isCorrect?0.35:0) + (Math.random()*1.2) - seenA;
    const scoreB = (b.isCorrect?0.35:0) + (Math.random()*1.2) - seenB;
    return scoreB-scoreA;
  });
  const correct = shuffled.find((c)=>c.isCorrect);
  return Object.assign({}, q, {
    preparedChoices: shuffled.map((c)=>({ id:c.id, text:c.text, whyBest:c.whyBest, whyTempting:c.whyTempting, whenUseful:c.whenUseful, isCorrect:c.isCorrect })),
    correctChoiceId: correct ? correct.id : '',
    correctChoiceText: (correct&&correct.text) || ((q.choices||[])[q.correctAnswer]) || '',
    correctChoiceDetails: correct ? { whyBest:correct.whyBest||'', whyTempting:correct.whyTempting||'', whenUseful:correct.whenUseful||'' } : { whyBest:'', whyTempting:'', whenUseful:'' }
  });
}

function docsQuizSelectQuestions(modeId, explicitIds){
  const targetCount = explicitIds&&explicitIds.length ? explicitIds.length : docsQuizQuestionCountForMode(modeId);
  const recent = docsQuizRecentQuestionIds();
  const missedMap = KNOWLEDGE_CHECKS_STATE.missed || {};
  const weak = new Set(docsQuizWeakCategoriesFromHistory());
  const isExam = docsQuizIsExamMode(modeId);
  const sourceBank = isExam ? KNOWLEDGE_CHECKS_STATE.examBank : KNOWLEDGE_CHECKS_STATE.bank;

  let pool = docsQuizFilterPool(modeId);
  if(explicitIds&&explicitIds.length){
    const wanted = new Set(explicitIds);
    pool = sourceBank.filter((q)=>wanted.has(q.id));
  }
  if(!pool.length)pool = sourceBank.slice();

  const ranked = docsQuizShuffle(pool).map((q)=>{
    let w = 0;
    if(missedMap[q.id])w += (isExam?4:6) + Number(missedMap[q.id]||0);
    if(weak.has(q.category))w += 4;
    if(!recent.has(q.id))w += 3;
    w += Math.random()*2;
    return { q, w };
  }).sort((a,b)=>b.w-a.w);

  const chosen = [];
  const used = new Set();
  for(let i=0;i<ranked.length && chosen.length<targetCount;i+=1){
    const q = ranked[i].q;
    if(used.has(q.id))continue;
    used.add(q.id);
    chosen.push(docsQuizPrepareQuestion(q));
  }
  return chosen;
}

function docsQuizStart(modeId, explicitIds){
  const resolvedMode = docsQuizModeById(modeId).id;
  const isExam = docsQuizIsExamMode(resolvedMode);
  const questions = docsQuizSelectQuestions(resolvedMode, explicitIds);
  KNOWLEDGE_CHECKS_STATE.attempt = {
    id: 'attempt-'+Date.now(),
    mode: resolvedMode,
    track: isExam ? 'final-exam' : 'daily-quiz',
    examLevel: isExam ? docsQuizExamLevelById(resolvedMode).label : '',
    questions,
    index: 0,
    responses: {},
    submitted: false,
    selectedChoiceId: '',
    startedAt: new Date().toISOString()
  };
  KNOWLEDGE_CHECKS_STATE.screen = 'active';
  docsQuizRender();
}

function docsQuizCurrentQuestion(){
  const a = KNOWLEDGE_CHECKS_STATE.attempt;
  if(!a||!a.questions.length)return null;
  return a.questions[a.index] || null;
}

function docsQuizSubmitCurrent(){
  const a = KNOWLEDGE_CHECKS_STATE.attempt;
  const q = docsQuizCurrentQuestion();
  if(!a||!q||!a.selectedChoiceId || a.submitted)return;
  const selected = q.preparedChoices.find((c)=>c.id===a.selectedChoiceId);
  const correct = a.selectedChoiceId===q.correctChoiceId;
  a.responses[q.id] = {
    selectedChoiceId: a.selectedChoiceId,
    selectedText: selected ? selected.text : '',
    selectedWhyTempting: selected ? selected.whyTempting : '',
    selectedWhenUseful: selected ? selected.whenUseful : '',
    correct,
    correctText: q.correctChoiceText
  };
  docsQuizRecordDistractorExposure(q);
  a.submitted = true;
  docsQuizRender();
}

function docsQuizNext(){
  const a = KNOWLEDGE_CHECKS_STATE.attempt;
  if(!a)return;
  if(!a.submitted)return;
  if(a.index >= a.questions.length-1){
    docsQuizFinish();
    return;
  }
  a.index += 1;
  a.submitted = false;
  a.selectedChoiceId = '';
  docsQuizRender();
}

function docsQuizDimensionIdsForQuestion(q){
  const rows = QUIZ_CATEGORY_DEFS.find((c)=>c.label===q.category);
  const id = rows ? rows.id : '';
  const map = {
    'js-foundations':['codeReading','frontendReasoning'],
    'frontend-basics':['frontendReasoning','safeChange'],
    'state-render-event':['frontendReasoning','debugging'],
    'async-fetch-api':['debugging','backendWorker'],
    'frontend-ownership':['safeChange','riskManagement'],
    'ev-formulas':['safeChange','riskManagement'],
    'regex-token':['codeReading','debugging'],
    'csv-parsing':['debugging','codeReading'],
    'debugging-process':['debugging','riskManagement'],
    'backend-admin':['backendWorker','safeChange'],
    'worker-cache':['backendWorker','riskManagement'],
    'auth-session-csrf':['riskManagement','backendWorker'],
    'deploy-rollback':['riskManagement','safeChange'],
    'file-ownership':['safeChange','frontendReasoning'],
    'ai-handoff':['riskManagement','safeChange'],
    'practice-review':['codeReading','debugging']
  };
  return map[id] || ['frontendReasoning'];
}

function docsQuizSkillBreakdown(questions, responses){
  const out = {};
  QUIZ_SKILL_DIMENSIONS.forEach((d)=>{ out[d.id]={label:d.label,correct:0,total:0,percent:0}; });
  questions.forEach((q)=>{
    const dims = docsQuizDimensionIdsForQuestion(q);
    dims.forEach((id)=>{
      if(!out[id])return;
      out[id].total += 1;
      const r = responses[q.id];
      if(r&&r.correct) out[id].correct += 1;
    });
  });
  Object.keys(out).forEach((k)=>{
    const row = out[k];
    row.percent = row.total ? Math.round((row.correct/row.total)*100) : 0;
  });
  return out;
}

function docsQuizStatusLabel(mode, percent){
  if(docsQuizIsExamMode(mode)){
    const level = docsQuizExamLevelById(mode);
    if(percent >= Math.max(level.passPercent+8, 90)) return 'Ownership Ready';
    if(percent >= level.passPercent) return 'Pass';
    return 'Review Needed';
  }
  return percent>=QUIZ_PASS_PERCENT ? 'Pass' : 'Review Needed';
}

function docsQuizFinish(){
  const a = KNOWLEDGE_CHECKS_STATE.attempt;
  if(!a)return;
  const isExam = docsQuizIsExamMode(a.mode);
  const questions = a.questions;
  const responses = a.responses;
  const answered = Object.keys(responses).length;
  let score = 0;
  const missedIds = [];
  questions.forEach((q)=>{
    const r = responses[q.id];
    if(r&&r.correct)score += 1;
    else missedIds.push(q.id);
  });
  const percent = questions.length ? Math.round((score/questions.length)*100) : 0;
  const categoryBreakdown = docsQuizGetBreakdown(questions, responses, 'category');
  const difficultyBreakdown = docsQuizGetBreakdown(questions, responses, 'difficulty');
  const skillBreakdown = docsQuizSkillBreakdown(questions, responses);
  const sw = docsQuizFindStrongWeak(categoryBreakdown);
  const recommendedReview = sw.weakest.map((cat)=>'Review '+cat+' in Owner Curriculum and Practice Lab.');
  const misconceptionSpots = questions
    .filter((q)=>missedIds.includes(q.id))
    .map((q)=>q.misconception)
    .filter((v,i,arr)=>v&&arr.indexOf(v)===i)
    .slice(0,6);
  const recommendedPractice = questions
    .filter((q)=>missedIds.includes(q.id))
    .map((q)=>q.recommendedFollowUp)
    .filter((v,i,arr)=>v&&arr.indexOf(v)===i)
    .slice(0,6);
  const trackHistory = isExam ? (KNOWLEDGE_CHECKS_STATE.examHistory||[]) : (KNOWLEDGE_CHECKS_STATE.history||[]);
  const prev = trackHistory.slice(-1)[0];
  const trend = prev ? (percent - Number(prev.percentage||0)) : null;
  const statusLabel = docsQuizStatusLabel(a.mode, percent);
  const passThreshold = isExam ? docsQuizExamLevelById(a.mode).passPercent : QUIZ_PASS_PERCENT;

  const result = {
    id: a.id,
    timestamp: new Date().toISOString(),
    mode: a.mode,
    track: a.track||'daily-quiz',
    examLevel: a.examLevel||'',
    score,
    totalQuestions: questions.length,
    percentage: percent,
    pass: percent>=passThreshold,
    statusLabel,
    passThreshold,
    answered,
    categoryBreakdown,
    difficultyBreakdown,
    skillBreakdown,
    strongestCategories: sw.strongest,
    weakestCategories: sw.weakest,
    missedQuestionIds: missedIds,
    questionIds: questions.map((q)=>q.id),
    recommendedReview,
    recommendedPractice,
    misconceptionSpots,
    trend
  };

  if(isExam){
    const examHistory = (KNOWLEDGE_CHECKS_STATE.examHistory||[]).concat([result]).slice(-120);
    KNOWLEDGE_CHECKS_STATE.examHistory = examHistory;
    docsQuizWrite(QUIZ_STORAGE_KEYS.examHistory, examHistory);
  }else{
    const history = (KNOWLEDGE_CHECKS_STATE.history||[]).concat([result]).slice(-120);
    KNOWLEDGE_CHECKS_STATE.history = history;
    docsQuizWrite(QUIZ_STORAGE_KEYS.history, history);
  }

  const missedMap = Object.assign({}, KNOWLEDGE_CHECKS_STATE.missed||{});
  missedIds.forEach((id)=>{ missedMap[id] = Number(missedMap[id]||0)+1; });
  questions.filter((q)=>!missedIds.includes(q.id)).forEach((q)=>{
    if(missedMap[q.id]) missedMap[q.id] = Math.max(0, Number(missedMap[q.id])-1);
  });
  KNOWLEDGE_CHECKS_STATE.missed = missedMap;
  docsQuizWrite(QUIZ_STORAGE_KEYS.missed, missedMap);
  const progression = Object.assign({}, KNOWLEDGE_CHECKS_STATE.progression||{});
  if(isExam && result.pass){
    const currentIdx = FINAL_EXAM_LEVEL_DEFS.findIndex((x)=>x.id===a.mode);
    const priorIdx = FINAL_EXAM_LEVEL_DEFS.findIndex((x)=>x.id===progression.highestExamLevelPassed);
    if(currentIdx>priorIdx) progression.highestExamLevelPassed = a.mode;
  }
  const weakTrend = Object.assign({}, progression.weakCategoryTrend||{});
  (result.weakestCategories||[]).forEach((cat)=>{
    weakTrend[cat] = Number(weakTrend[cat]||0)+1;
  });
  progression.weakCategoryTrend = weakTrend;
  progression.lastRecommendations = recommendedPractice.slice(0,4);
  KNOWLEDGE_CHECKS_STATE.progression = progression;
  docsQuizWrite(QUIZ_STORAGE_KEYS.progression, progression);

  KNOWLEDGE_CHECKS_STATE.lastResult = result;
  KNOWLEDGE_CHECKS_STATE.screen = 'results';
  docsQuizRender();
}

function docsQuizSummaryRows(map){
  return Object.keys(map||{}).map((k)=>{
    const row = map[k]||{};
    return '<tr><td>'+docsQuizEsc(k)+'</td><td>'+docsQuizEsc(String(row.correct||0)+'/'+String(row.total||0))+'</td><td>'+docsQuizEsc(String(row.percent||0)+'%')+'</td></tr>';
  }).join('');
}

function docsQuizMissedCards(result){
  const attempt = KNOWLEDGE_CHECKS_STATE.attempt;
  if(!result || !attempt)return '';
  const byId = {};
  attempt.questions.forEach((q)=>{ byId[q.id]=q; });
  return (result.missedQuestionIds||[]).map((id,idx)=>{
    const q = byId[id];
    if(!q)return '';
    const r = (attempt.responses||{})[id] || {};
    return '<article class="docs-section">'
      +'<h4>Missed '+(idx+1)+': '+docsQuizEsc(q.category)+' ('+docsQuizEsc(q.difficulty)+')</h4>'
      +'<p><b>Question:</b> '+docsQuizEsc(q.prompt)+'</p>'
      +(q.snippet?'<pre class="docs-code"><code>'+docsQuizEsc(q.snippet)+'</code></pre>':'')
      +'<p><b>Your answer:</b> '+docsQuizEsc(r.selectedText||'No answer')+'</p>'
      +'<p><b>Correct answer:</b> '+docsQuizEsc(q.correctChoiceText||'')+'</p>'
      +'<p><b>Explanation:</b> '+docsQuizEsc(q.explanation||'')+'</p>'
      +'<p><b>Plain-English explanation:</b> '+docsQuizEsc(q.plainEnglish||'Think in sequence: inspect, verify, then patch minimally.')+'</p>'
      +'<p><b>Common misunderstanding tested:</b> '+docsQuizEsc(q.misconception||'Jumping to a plausible but wrong first action.')+'</p>'
      +'<p><b>Why your answer was tempting:</b> '+docsQuizEsc(r.selectedWhyTempting||'It sounds efficient, but it usually skips first-evidence discipline.')+'</p>'
      +'<p><b>When your answer could be useful later:</b> '+docsQuizEsc(r.selectedWhenUseful||'Later in the workflow, after initial diagnosis and ownership checks.')+'</p>'
      +'<p><b>Why the correct answer is better now:</b> '+docsQuizEsc((q.correctChoiceDetails&&q.correctChoiceDetails.whyBest)||'It maximizes signal while minimizing risk and rework.')+'</p>'
      +'<p><b>Related files:</b> '+docsQuizEsc((q.relatedFiles||[]).join(', '))+'</p>'
      +'<p><b>Related topics:</b> '+docsQuizEsc((q.relatedSections||[]).join(' | '))+'</p>'
      +'<p><b>Mini follow-up prompt:</b> '+docsQuizEsc(q.followUpPrompt||'Explain this concept in plain English and identify one file to inspect next.')+'</p>'
      +'</article>';
  }).join('');
}

function docsQuizRenderSetup(content){
  const settings = KNOWLEDGE_CHECKS_STATE.settings;
  const dailyMode = docsQuizDailyModeById(settings.dailyMode||settings.mode);
  const examMode = docsQuizExamLevelById(settings.examMode);
  const bank = KNOWLEDGE_CHECKS_STATE.bank;
  const examBank = KNOWLEDGE_CHECKS_STATE.examBank;
  const byDiff = docsQuizGetBreakdown(bank, {}, 'difficulty');
  const examByDiff = docsQuizGetBreakdown(examBank, {}, 'difficulty');
  const highestExam = docsQuizExamLevelById((KNOWLEDGE_CHECKS_STATE.progression||{}).highestExamLevelPassed||'exam-apprentice');
  const categoryChecks = QUIZ_CATEGORY_DEFS.map((c)=>{
    const checked = (settings.customCategories||[]).includes(c.label) ? ' checked' : '';
    return '<label class="check"><input type="checkbox" class="kc-custom-category" value="'+docsQuizEsc(c.label)+'"'+checked+' />'+docsQuizEsc(c.label)+'</label>';
  }).join('');
  const diffChecks = QUIZ_DIFFICULTIES.map((d)=>{
    const checked = (settings.customDifficulties||[]).includes(d) ? ' checked' : '';
    return '<label class="check"><input type="checkbox" class="kc-custom-diff" value="'+docsQuizEsc(d)+'"'+checked+' />'+docsQuizEsc(d)+'</label>';
  }).join('');

  content.innerHTML = '<article id="kc-setup" class="docs-section">'
    +'<h3>Knowledge Checks: Learning + Assessment System</h3>'
    +'<p>Daily Quizzes train consistency. Final Exams test deeper ownership judgment. Every answer teaches, even when correct.</p>'
    +'<div class="grid2">'
      +'<div class="card"><div class="h">Daily Quizzes</div>'
        +'<div><label class="sub">Daily mode</label><select id="kcDailyModeSelect">'+DAILY_QUIZ_MODE_DEFS.map((m)=>'<option value="'+m.id+'"'+(m.id===dailyMode.id?' selected':'')+'>'+docsQuizEsc(m.label)+'</option>').join('')+'</select></div>'
        +'<div class="sub mono">Daily bank: '+docsQuizEsc(String(bank.length))+' questions | target: '+docsQuizEsc(String(dailyMode.id==='custom-practice'?docsQuizClamp(settings.customCount,5,60):dailyMode.count))+'</div>'
        +'<div class="toolbar"><button id="kcStartDailyBtn" class="btn" type="button">Start Daily Quiz</button><button id="kcGoDailyBtn" class="btn ghost" type="button">Daily Setup</button></div>'
      +'</div>'
      +'<div class="card"><div class="h">Final Exams</div>'
        +'<div><label class="sub">Exam level</label><select id="kcExamModeSelect">'+FINAL_EXAM_LEVEL_DEFS.map((m)=>'<option value="'+m.id+'"'+(m.id===examMode.id?' selected':'')+'>'+docsQuizEsc(m.label)+' ('+docsQuizEsc(String(m.count))+' q)</option>').join('')+'</select></div>'
        +'<div class="sub mono">Exam pool: '+docsQuizEsc(String(examBank.length))+' scenarios | highest passed: '+docsQuizEsc(highestExam.label)+'</div>'
        +'<div class="toolbar"><button id="kcStartExamBtn" class="btn" type="button">Start Final Exam</button><button id="kcGoFinalBtn" class="btn ghost" type="button">Final Exam Setup</button></div>'
      +'</div>'
    +'</div>'
    +'<div class="grid2">'
      +'<div class="card"><div class="h">Review & Relearn</div><p class="sub">Use history trends, weak-area summaries, and missed-question retries to convert mistakes into understanding.</p><div class="toolbar"><button id="kcViewHistoryBtn" class="btn ghost" type="button">Review History</button><button id="kcViewWeakBtn" class="btn ghost" type="button">Weak Areas</button></div></div>'
      +'<div class="card"><div class="h">Practice Recommendations</div><p class="sub">Get targeted Owner Curriculum / Practice Lab / Workshops suggestions from your latest weak categories.</p><div class="toolbar"><button id="kcViewPracticeBtn" class="btn ghost" type="button">Practice Recommendations</button></div></div>'
    +'</div>'
    +'<div id="kc-daily-setup-block" class="card"><div class="h">Daily Custom Practice Filters</div><div class="sub">Used only when daily mode = Custom Practice.</div>'
      +'<div class="grid2">'
        +'<div><label class="sub">Questions</label><input id="kcCustomCount" type="number" min="5" max="60" value="'+docsQuizEsc(String(settings.customCount||25))+'" /></div>'
        +'<div>'+diffChecks+'</div>'
      +'</div>'
      +'<div class="sub">Categories</div><div class="grid2"><div>'+categoryChecks+'</div><div></div></div>'
    +'</div>'
    +'<div class="grid2">'
      +'<div class="card"><div class="h">Daily Difficulty Distribution</div><table><thead><tr><th>Difficulty</th><th>Count</th></tr></thead><tbody>'
      +QUIZ_DIFFICULTIES.map((d)=>'<tr><td>'+docsQuizEsc(d)+'</td><td>'+docsQuizEsc(String((byDiff[d]&&byDiff[d].total)||0))+'</td></tr>').join('')
      +'</tbody></table></div>'
      +'<div class="card"><div class="h">Final Exam Difficulty Distribution</div><table><thead><tr><th>Difficulty</th><th>Count</th></tr></thead><tbody>'
      +QUIZ_DIFFICULTIES.map((d)=>'<tr><td>'+docsQuizEsc(d)+'</td><td>'+docsQuizEsc(String((examByDiff[d]&&examByDiff[d].total)||0))+'</td></tr>').join('')
      +'</tbody></table></div>'
    +'</div>'
    +'</article>';
}

function docsQuizRenderActive(content){
  const a = KNOWLEDGE_CHECKS_STATE.attempt;
  const q = docsQuizCurrentQuestion();
  const runLabel = a && a.track==='final-exam' ? ('Final Exam - '+docsQuizEsc(a.examLevel||'Level')) : 'Daily Quiz';
  if(!a||!q){
    content.innerHTML = '<article id="kc-active-empty" class="docs-section">'
      +'<h3>Active Session</h3>'
      +'<p>No active quiz is running yet.</p>'
      +'<div class="toolbar"><button id="kcStartDailyBtn" class="btn" type="button">Start Daily Quiz</button><button id="kcBackSetupBtn" class="btn ghost" type="button">Back to Setup</button></div>'
      +'</article>';
    return;
  }
  const answered = Object.values(a.responses||{}).filter((r)=>r&&r.correct).length;
  const progress = a.questions.length ? Math.round((a.index / a.questions.length) * 100) : 0;
  const choiceRows = (q.preparedChoices||[]).map((c)=>{
    const checked = a.selectedChoiceId===c.id ? ' checked' : '';
    return '<label class="check kc-choice"><input type="radio" name="kcChoice" value="'+docsQuizEsc(c.id)+'"'+checked+' '+(a.submitted?'disabled':'')+' /><span>'+docsQuizEsc(c.text)+'</span></label>';
  }).join('');
  const r = (a.responses||{})[q.id];
  const feedback = a.submitted ? (()=>{
    const correctWhy = (q.correctChoiceDetails&&q.correctChoiceDetails.whyBest) ? q.correctChoiceDetails.whyBest : 'It matches the safest evidence-first and ownership-aware path for this scenario.';
    const altNotes = (q.preparedChoices||[]).filter((c)=>!c.isCorrect).slice(0,3).map((c)=>(
      '<li>&bull; '+docsQuizEsc(c.text)+' &mdash; '+docsQuizEsc(c.whyTempting||'Plausible, but weaker as the first move here.')+'</li>'
    )).join('');
    const statusClass = (r&&r.correct)?'ok':'warn';
    const statusWord = (r&&r.correct)?'Correct':'Incorrect';
    const icon = (r&&r.correct)?'&#10003;':'!';
    if(r&&r.correct){
      return '<div class="status '+statusClass+' kc-feedback">'
        +'<div class="kc-feedback-head"><span class="kc-feedback-icon" aria-hidden="true">'+icon+'</span><strong>'+statusWord+'</strong></div>'
        +'<div class="kc-feedback-block"><div class="kc-feedback-label">Why this answer is correct:</div><p>'+docsQuizEsc(correctWhy)+'</p></div>'
        +(altNotes?'<div class="kc-feedback-block"><div class="kc-feedback-label">Why other options are weaker:</div><ul>'+altNotes+'</ul></div>':'')
        +'<div class="kc-feedback-block"><div class="kc-feedback-label">What this teaches:</div><p>'+docsQuizEsc(q.plainEnglish||'Think in sequence: inspect evidence, then choose the smallest safe action.')+'</p></div>'
        +'<div class="kc-feedback-row"><span class="kc-feedback-label">Related file/topic:</span><span class="mono">'+docsQuizEsc((q.relatedFiles||[]).join(', ')||'n/a')+' | '+docsQuizEsc((q.relatedSections||[]).join(' | ')||'n/a')+'</span></div>'
        +'</div>';
    }
    return '<div class="status '+statusClass+' kc-feedback">'
      +'<div class="kc-feedback-head"><span class="kc-feedback-icon" aria-hidden="true">'+icon+'</span><strong>'+statusWord+'</strong></div>'
      +'<div class="kc-feedback-row"><span class="kc-feedback-label">Correct answer:</span><span>'+docsQuizEsc(r&&r.correctText||q.correctChoiceText||'')+'</span></div>'
      +'<div class="kc-feedback-block"><div class="kc-feedback-label">Why the correct answer is stronger:</div><p>'+docsQuizEsc(correctWhy)+'</p></div>'
      +'<div class="kc-feedback-block"><div class="kc-feedback-label">Why your selected answer was tempting but weaker:</div><p>'+docsQuizEsc((r&&r.selectedWhyTempting)||'This choice was plausible, but it skipped the highest-signal first check.')+'</p>'+(r&&r.selectedWhenUseful?'<p class="sub">When it can be useful later: '+docsQuizEsc(r.selectedWhenUseful)+'</p>':'')+'</div>'
      +'<div class="kc-feedback-block"><div class="kc-feedback-label">What concept was missed:</div><p>'+docsQuizEsc(q.misconception||'Common mistake: a plausible option chosen at the wrong time.')+'</p></div>'
      +'<div class="kc-feedback-row"><span class="kc-feedback-label">Review recommendation:</span><span class="mono">'+docsQuizEsc(q.recommendedFollowUp||'Revisit the owner file and run one evidence-first repro pass before patching.')+'</span></div>'
      +'</div>';
  })() : '';

  content.innerHTML = '<article id="kc-active" class="docs-section">'
    +'<div class="toolbar"><div class="sub mono kc-scoreline">'+runLabel+' | Question '+docsQuizEsc(String(a.index+1))+' / '+docsQuizEsc(String(a.questions.length))+' | Score so far: '+docsQuizEsc(String(answered))+'</div><button id="kcQuitBtn" class="btn ghost" type="button">Quit to Setup</button></div>'
    +'<div class="kc-progress" aria-hidden="true"><div class="kc-progress-fill" style="width:'+docsQuizEsc(String(progress))+'%"></div></div>'
    +'<div class="toolbar"><span class="badge">'+docsQuizEsc(q.category)+'</span><span class="badge">'+docsQuizEsc(q.difficulty)+'</span><span class="badge">'+docsQuizEsc(q.type)+'</span></div>'
    +'<p><b>'+docsQuizEsc(q.prompt)+'</b></p>'
    +(q.snippet?'<pre class="docs-code"><code>'+docsQuizEsc(q.snippet)+'</code></pre>':'')
    +'<div>'+choiceRows+'</div>'
    +'<div class="toolbar kc-action-row"><button id="kcSubmitBtn" class="btn" type="button" '+(a.submitted?'disabled':'')+'>Submit Answer</button><button id="kcNextBtn" class="btn ghost" type="button" '+(a.submitted?'':'disabled')+'>'+(a.index>=a.questions.length-1?(a.track==='final-exam'?'Finish Exam':'Finish Quiz'):'Next Question')+'</button></div>'
    +feedback
    +'</article>';
}

function docsQuizRenderResults(content){
  const result = KNOWLEDGE_CHECKS_STATE.lastResult;
  if(!result){
    content.innerHTML = '<article id="kc-results-empty" class="docs-section">'
      +'<h3>Quiz Report Card</h3>'
      +'<p>No completed quiz attempt yet. Finish a quiz to generate your report card.</p>'
      +'<div class="toolbar"><button id="kcStartBtn" class="btn" type="button">Start Quiz</button><button id="kcBackSetupBtn" class="btn ghost" type="button">Back to Setup</button></div>'
      +'</article>';
    return;
  }
  const passTxt = String(result.statusLabel||'Review Needed').toUpperCase();
  const isExam = result.track==='final-exam';
  const trendTxt = result.trend==null ? 'No prior attempt comparison yet.' : ('Trend vs previous attempt: '+(result.trend>=0?'+':'')+result.trend+'%');
  const missedCount = (result.missedQuestionIds||[]).length;
  const groupedMissed = {};
  (result.missedQuestionIds||[]).forEach((id)=>{
    const q = (KNOWLEDGE_CHECKS_STATE.attempt&&KNOWLEDGE_CHECKS_STATE.attempt.questions||[]).find((x)=>x.id===id);
    if(!q)return;
    groupedMissed[q.category] = (groupedMissed[q.category]||0)+1;
  });
  const groupedRows = Object.keys(groupedMissed).map((k)=>'<tr><td>'+docsQuizEsc(k)+'</td><td>'+docsQuizEsc(String(groupedMissed[k]))+'</td></tr>').join('');

  content.innerHTML = '<article id="kc-results" class="docs-section">'
    +'<h3>'+(isExam?'Final Exam Report Card':'Daily Quiz Report Card')+'</h3>'
    +'<p><b>Score:</b> '+docsQuizEsc(String(result.score))+' / '+docsQuizEsc(String(result.totalQuestions))+' ('+docsQuizEsc(String(result.percentage))+'%)</p>'
    +(isExam?'<p><b>Exam level:</b> '+docsQuizEsc(result.examLevel||'')+'</p>':'')
    +'<p><b>Status:</b> <span class="badge">'+docsQuizEsc(passTxt)+'</span> <span class="sub mono">(Pass threshold: '+docsQuizEsc(String(result.passThreshold||QUIZ_PASS_PERCENT))+'%)</span></p>'
    +'<p class="sub mono">'+docsQuizEsc(trendTxt)+'</p>'
    +'<div class="grid2">'
      +'<div class="card"><div class="h">Category Breakdown</div><table><thead><tr><th>Category</th><th>Correct</th><th>%</th></tr></thead><tbody>'+docsQuizSummaryRows(result.categoryBreakdown)+'</tbody></table></div>'
      +'<div class="card"><div class="h">Difficulty Breakdown</div><table><thead><tr><th>Difficulty</th><th>Correct</th><th>%</th></tr></thead><tbody>'+docsQuizSummaryRows(result.difficultyBreakdown)+'</tbody></table></div>'
    +'</div>'
    +'<div class="card"><div class="h">'+(isExam?'Ownership Readiness Scores':'Learning Scores')+'</div><table><thead><tr><th>Skill area</th><th>Correct</th><th>%</th></tr></thead><tbody>'
      +QUIZ_SKILL_DIMENSIONS.map((d)=>{
        const row = (result.skillBreakdown||{})[d.id]||{correct:0,total:0,percent:0};
        return '<tr><td>'+docsQuizEsc(d.label)+'</td><td>'+docsQuizEsc(String(row.correct)+'/'+String(row.total))+'</td><td>'+docsQuizEsc(String(row.percent)+'%')+'</td></tr>';
      }).join('')
    +'</tbody></table></div>'
    +'<p><b>Strongest:</b> '+(result.strongestCategories||[]).map((c)=>'<span class="badge ok">'+docsQuizEsc(c)+'</span>').join(' ')+'</p>'
    +'<p><b>Weakest:</b> '+(result.weakestCategories||[]).map((c)=>'<span class="badge warn">'+docsQuizEsc(c)+'</span>').join(' ')+'</p>'
    +'<p><b>Recommended review:</b> '+docsQuizEsc((result.recommendedReview||[]).join(' | ')||'n/a')+'</p>'
    +'<p><b>Recommended Practice Lab sections:</b> '+docsQuizEsc((result.recommendedPractice||[]).join(' | ')||'n/a')+'</p>'
    +'<p><b>Misunderstandings to revisit:</b> '+docsQuizEsc((result.misconceptionSpots||[]).join(' | ')||'n/a')+'</p>'
    +'<div class="toolbar"><button id="kcRetryMissedBtn" class="btn" type="button" '+(missedCount?'':'disabled')+'>Retry Missed ('+docsQuizEsc(String(missedCount))+')</button><button id="kcReviewBtn" class="btn ghost" type="button" '+(missedCount?'':'disabled')+'>Review & Relearn</button><button id="kcNewQuizBtn" class="btn ghost" type="button">New Quiz</button><button id="kcResultsHistoryBtn" class="btn ghost" type="button">History</button></div>'
    +(missedCount?'<div class="card"><div class="h">Missed Concept Groups</div><table><thead><tr><th>Category</th><th>Missed Count</th></tr></thead><tbody>'+groupedRows+'</tbody></table></div>':'')
    +'</article>';
}

function docsQuizRenderReview(content){
  const result = KNOWLEDGE_CHECKS_STATE.lastResult;
  if(!result){
    content.innerHTML = '<article id="kc-review-empty" class="docs-section">'
      +'<h3>Review & Relearn</h3>'
      +'<p>No completed quiz attempt yet. Finish a quiz to unlock review mode.</p>'
      +'<div class="toolbar"><button id="kcStartBtn" class="btn" type="button">Start Quiz</button><button id="kcBackSetupBtn" class="btn ghost" type="button">Back to Setup</button></div>'
      +'</article>';
    return;
  }
  if(!(result.missedQuestionIds||[]).length){
    content.innerHTML = '<article id="kc-review-none-missed" class="docs-section">'
      +'<h3>Review & Relearn</h3>'
      +'<p>Nice work. Your latest attempt has no missed questions to review.</p>'
      +'<div class="toolbar"><button id="kcNewQuizBtn" class="btn" type="button">Start New Quiz</button><button id="kcBackToResultsBtn" class="btn ghost" type="button">Back to Results</button></div>'
      +'</article>';
    return;
  }
  const cards = docsQuizMissedCards(result);
  content.innerHTML = '<article id="kc-review" class="docs-section"><h3>Review & Relearn</h3><p>Mini lesson mode for missed questions. Use this to rebuild understanding immediately.</p><div class="toolbar"><button id="kcBackToResultsBtn" class="btn ghost" type="button">Back to Results</button><button id="kcRetryMissedFromReviewBtn" class="btn" type="button">Retry Missed</button></div></article>'+cards;
}

function docsQuizRenderHistory(content){
  const history = docsQuizAllAttempts();
  const rows = history.slice().reverse().map((h)=>{
    const strong = (h.strongestCategories||[])[0]||'-';
    const weak = (h.weakestCategories||[])[0]||'-';
    const track = h.track==='final-exam' ? ('Final Exam'+(h.examLevel?(' - '+h.examLevel):'')) : 'Daily Quiz';
    return '<tr><td>'+docsQuizEsc(new Date(h.timestamp).toLocaleString())+'</td><td>'+docsQuizEsc(track)+'</td><td>'+docsQuizEsc(String(h.score)+'/'+String(h.totalQuestions))+'</td><td>'+docsQuizEsc(String(h.percentage)+'%')+'</td><td>'+docsQuizEsc(strong)+'</td><td>'+docsQuizEsc(weak)+'</td></tr>';
  }).join('');
  const avg = history.length ? Math.round(history.reduce((s,h)=>s+Number(h.percentage||0),0)/history.length) : 0;
  const latest = history.slice(-1)[0];
  const trend = history.length>1 ? (Number(latest.percentage||0)-Number(history[history.length-2].percentage||0)) : null;
  content.innerHTML = '<article id="kc-history" class="docs-section"><h3>Review History</h3><p class="sub mono">Attempts: '+docsQuizEsc(String(history.length))+' | Average score: '+docsQuizEsc(String(avg))+'%'+(trend==null?'':' | Latest trend: '+(trend>=0?'+':'')+trend+'%')+'</p><div class="toolbar"><button id="kcBackSetupBtn" class="btn ghost" type="button">Back to Setup</button><button id="kcViewWeakBtn" class="btn ghost" type="button">Weak Areas</button><button id="kcClearHistoryBtn" class="btn danger" type="button">Clear History</button></div><table><thead><tr><th>Date/Time</th><th>Track</th><th>Score</th><th>%</th><th>Strongest</th><th>Weakest</th></tr></thead><tbody>'+rows+'</tbody></table></article>';
}

function docsQuizRenderWeakAreas(content){
  const weakCats = docsQuizWeakCategoriesFromHistory();
  const progression = KNOWLEDGE_CHECKS_STATE.progression||{};
  const trendMap = progression.weakCategoryTrend||{};
  const rows = weakCats.map((cat)=>'<tr><td>'+docsQuizEsc(cat)+'</td><td>'+docsQuizEsc(String(trendMap[cat]||0))+'</td><td>'+docsQuizEsc('Review '+cat+' in Practice Lab + Owner Curriculum')+'</td></tr>').join('');
  content.innerHTML = '<article id="kc-weak" class="docs-section">'
    +'<h3>Weak Areas</h3>'
    +'<p>This section prioritizes categories that repeatedly underperform across Daily Quizzes and Final Exams.</p>'
    +'<table><thead><tr><th>Category</th><th>Times flagged weak</th><th>Recommended next step</th></tr></thead><tbody>'+(rows||'<tr><td colspan="3">No weak trends yet. Complete more attempts.</td></tr>')+'</tbody></table>'
    +'<div class="toolbar"><button id="kcBackSetupBtn" class="btn ghost" type="button">Back to Setup</button><button id="kcViewPracticeBtn" class="btn" type="button">Practice Recommendations</button></div>'
    +'</article>';
}

function docsQuizRenderPracticeRecommendations(content){
  const progression = KNOWLEDGE_CHECKS_STATE.progression||{};
  const latest = KNOWLEDGE_CHECKS_STATE.lastResult;
  const weakCats = docsQuizWeakCategoriesFromHistory();
  const recs = (latest&&latest.recommendedPractice||[]).concat(progression.lastRecommendations||[]).filter((v,i,arr)=>v&&arr.indexOf(v)===i).slice(0,10);
  const weakItems = weakCats.map((cat)=>'<li><b>'+docsQuizEsc(cat)+':</b> Re-run one related Practice Lab mission, then retake a focused Daily Quiz.</li>').join('');
  const recItems = recs.map((r)=>'<li>'+docsQuizEsc(r)+'</li>').join('');
  content.innerHTML = '<article id="kc-practice-recs" class="docs-section">'
    +'<h3>Practice Recommendations</h3>'
    +'<p>Use this section when scores stall. Relearn weak concepts, then verify improvement with a replay quiz/exam.</p>'
    +'<h4>Category-driven guidance</h4><ul>'+(weakItems||'<li>No weak categories yet. Keep practicing to build trend data.</li>')+'</ul>'
    +'<h4>Suggested next sections</h4><ul>'+(recItems||'<li>Complete one Daily Quiz and one Final Exam to generate recommendations.</li>')+'</ul>'
    +'<div class="toolbar"><button id="kcBackSetupBtn" class="btn ghost" type="button">Back to Setup</button><button id="kcStartDailyBtn" class="btn" type="button">Start Daily Quiz</button><button id="kcStartExamBtn" class="btn ghost" type="button">Start Final Exam</button></div>'
    +'</article>';
}

function docsQuizRenderDaily(content){
  docsQuizRenderSetup(content);
  const block = content.querySelector('#kc-daily-setup-block');
  if(block) block.scrollIntoView({ block:'start' });
}

function docsQuizRenderFinal(content){
  docsQuizRenderSetup(content);
}

function docsQuizRender(){
  const ctx = KNOWLEDGE_CHECKS_STATE.context;
  if(!ctx||!ctx.content)return;
  const content = ctx.content;
  const navMap = new Set(['setup','daily','final','active','results','history','weak','practice']);
  const nav = navMap.has(KNOWLEDGE_CHECKS_STATE.screen) ? KNOWLEDGE_CHECKS_STATE.screen : 'setup';
  if(ctx.toc){
    ctx.toc.querySelectorAll('.docs-toc-item[data-kc-nav]').forEach((btn)=>{
      btn.classList.toggle('active', btn.getAttribute('data-kc-nav')===nav);
    });
  }
  if(KNOWLEDGE_CHECKS_STATE.screen==='active') docsQuizRenderActive(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='results') docsQuizRenderResults(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='history') docsQuizRenderHistory(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='review') docsQuizRenderReview(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='daily') docsQuizRenderDaily(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='final') docsQuizRenderFinal(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='weak') docsQuizRenderWeakAreas(content);
  else if(KNOWLEDGE_CHECKS_STATE.screen==='practice') docsQuizRenderPracticeRecommendations(content);
  else docsQuizRenderSetup(content);
  docsEnhanceRenderedDocs(content);
}

function docsQuizBindEvents(){
  const ctx = KNOWLEDGE_CHECKS_STATE.context;
  if(!ctx||!ctx.content)return;
  const content = ctx.content;
  content.onclick = (ev)=>{
    const t = ev.target;
    if(!t)return;
    if(t.id==='kcStartBtn' || t.id==='kcStartDailyBtn'){ docsQuizStart(KNOWLEDGE_CHECKS_STATE.settings.dailyMode||'quick-drill'); return; }
    if(t.id==='kcStartExamBtn'){ docsQuizStart(KNOWLEDGE_CHECKS_STATE.settings.examMode||'exam-apprentice'); return; }
    if(t.id==='kcGoDailyBtn'){ KNOWLEDGE_CHECKS_STATE.screen='daily'; docsQuizRender(); return; }
    if(t.id==='kcGoFinalBtn'){ KNOWLEDGE_CHECKS_STATE.screen='final'; docsQuizRender(); return; }
    if(t.id==='kcViewHistoryBtn' || t.id==='kcResultsHistoryBtn'){ KNOWLEDGE_CHECKS_STATE.screen='history'; docsQuizRender(); return; }
    if(t.id==='kcViewWeakBtn'){ KNOWLEDGE_CHECKS_STATE.screen='weak'; docsQuizRender(); return; }
    if(t.id==='kcViewPracticeBtn'){ KNOWLEDGE_CHECKS_STATE.screen='practice'; docsQuizRender(); return; }
    if(t.id==='kcBackSetupBtn'){ KNOWLEDGE_CHECKS_STATE.screen='setup'; docsQuizRender(); return; }
    if(t.id==='kcSubmitBtn'){ docsQuizSubmitCurrent(); return; }
    if(t.id==='kcNextBtn'){ docsQuizNext(); return; }
    if(t.id==='kcQuitBtn'){ KNOWLEDGE_CHECKS_STATE.screen='setup'; KNOWLEDGE_CHECKS_STATE.attempt=null; docsQuizRender(); return; }
    if(t.id==='kcNewQuizBtn'){ KNOWLEDGE_CHECKS_STATE.screen='setup'; KNOWLEDGE_CHECKS_STATE.attempt=null; docsQuizRender(); return; }
    if(t.id==='kcReviewBtn'){ KNOWLEDGE_CHECKS_STATE.screen='review'; docsQuizRender(); return; }
    if(t.id==='kcBackToResultsBtn'){ KNOWLEDGE_CHECKS_STATE.screen='results'; docsQuizRender(); return; }
    if(t.id==='kcRetryMissedBtn' || t.id==='kcRetryMissedFromReviewBtn'){
      const result = KNOWLEDGE_CHECKS_STATE.lastResult;
      const ids = result&&result.missedQuestionIds ? result.missedQuestionIds.slice() : [];
      docsQuizStart('custom-practice', ids);
      return;
    }
    if(t.id==='kcClearHistoryBtn'){
      KNOWLEDGE_CHECKS_STATE.history = [];
      KNOWLEDGE_CHECKS_STATE.examHistory = [];
      KNOWLEDGE_CHECKS_STATE.distractorMemory = { seen:{}, recent:[] };
      docsQuizWrite(QUIZ_STORAGE_KEYS.history, []);
      docsQuizWrite(QUIZ_STORAGE_KEYS.examHistory, []);
      docsQuizWrite(QUIZ_STORAGE_KEYS.distractorMemory, KNOWLEDGE_CHECKS_STATE.distractorMemory);
      docsQuizRender();
      return;
    }
  };
  content.onchange = (ev)=>{
    const t = ev.target;
    if(!t)return;
    if(t.id==='kcModeSelect' || t.id==='kcDailyModeSelect'){
      KNOWLEDGE_CHECKS_STATE.settings.mode = t.value;
      KNOWLEDGE_CHECKS_STATE.settings.dailyMode = t.value;
      docsQuizWrite(QUIZ_STORAGE_KEYS.settings, KNOWLEDGE_CHECKS_STATE.settings);
      docsQuizRender();
      return;
    }
    if(t.id==='kcExamModeSelect'){
      KNOWLEDGE_CHECKS_STATE.settings.examMode = t.value;
      docsQuizWrite(QUIZ_STORAGE_KEYS.settings, KNOWLEDGE_CHECKS_STATE.settings);
      docsQuizRender();
      return;
    }
    if(t.id==='kcCustomCount'){
      KNOWLEDGE_CHECKS_STATE.settings.customCount = docsQuizClamp(t.value,5,60);
      docsQuizWrite(QUIZ_STORAGE_KEYS.settings, KNOWLEDGE_CHECKS_STATE.settings);
      return;
    }
    if(t.classList&&t.classList.contains('kc-custom-category')){
      const set = new Set(KNOWLEDGE_CHECKS_STATE.settings.customCategories||[]);
      if(t.checked)set.add(t.value); else set.delete(t.value);
      KNOWLEDGE_CHECKS_STATE.settings.customCategories = Array.from(set);
      docsQuizWrite(QUIZ_STORAGE_KEYS.settings, KNOWLEDGE_CHECKS_STATE.settings);
      return;
    }
    if(t.classList&&t.classList.contains('kc-custom-diff')){
      const set = new Set(KNOWLEDGE_CHECKS_STATE.settings.customDifficulties||[]);
      if(t.checked)set.add(t.value); else set.delete(t.value);
      KNOWLEDGE_CHECKS_STATE.settings.customDifficulties = Array.from(set);
      docsQuizWrite(QUIZ_STORAGE_KEYS.settings, KNOWLEDGE_CHECKS_STATE.settings);
      return;
    }
    if(t.name==='kcChoice'){
      const a = KNOWLEDGE_CHECKS_STATE.attempt;
      if(!a||a.submitted)return;
      a.selectedChoiceId = t.value;
    }
  };
}

function docsQuizRenderBookInDocs(toc, content, searchInput, searchMeta){
  docsQuizInitState();
  KNOWLEDGE_CHECKS_STATE.context = { toc, content, searchInput, searchMeta };
  KNOWLEDGE_CHECKS_STATE.screen = KNOWLEDGE_CHECKS_STATE.screen || 'setup';
  if(searchInput){
    searchInput.value = '';
    searchInput.disabled = true;
    searchInput.placeholder = 'Knowledge Checks uses mode/category filters instead of text search.';
  }
  const clearBtn = $('docsSearchClearBtn');
  if(clearBtn) clearBtn.disabled = true;
  if(searchMeta){
    searchMeta.textContent = 'Knowledge Checks daily bank: '+KNOWLEDGE_CHECKS_STATE.bank.length+' questions | final exam bank: '+KNOWLEDGE_CHECKS_STATE.examBank.length+' scenarios. Replayable modes + history are enabled.';
  }
  toc.innerHTML = '<button type="button" class="docs-toc-item" data-kc-nav="setup">Setup</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="daily">Daily Quiz</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="final">Final Exams</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="active">Active Session</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="results">Report Card</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="history">Review History</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="weak">Weak Areas</button>'
    +'<button type="button" class="docs-toc-item" data-kc-nav="practice">Practice Recommendations</button>';
  docsQuizRender();
  docsQuizBindEvents();
}

function docsEnhanceRenderedDocs(content){
  if(!content)return;
  const labelSet=new Set([
    'goal',
    'skill trained',
    'solo attempt prompt',
    'hint ladder',
    'full walkthrough mode',
    'full walkthrough',
    'what success looks like',
    'success criteria',
    'common mistakes',
    'if it failed, first 3 checks',
    'first 3 checks',
    'rollback steps',
    'rollback',
    'why this matters',
    'replay variation'
  ]);
  const parsePlainLabel=(line)=>{
    const m=String(line||'').match(/^\s*([A-Za-z][A-Za-z0-9\s\/-]{1,60}):\s+(.+)$/);
    if(!m)return null;
    const label=String(m[1]||'').trim();
    const body=String(m[2]||'').trim();
    if(!body)return null;
    if(!labelSet.has(label.toLowerCase()))return null;
    return {label:label+':', body};
  };
  const sections=[...content.querySelectorAll('.docs-section')];
  sections.forEach((section)=>{
    section.classList.add('docs-section-read');
    section.querySelectorAll('li').forEach((li)=>{
      const first=li.firstElementChild;
      if(!first)return;
      const tag=String(first.tagName||'').toLowerCase();
      if(tag!=='b'&&tag!=='strong')return;
      const label=String(first.textContent||'').trim();
      if(!/:$/.test(label))return;
      if(li.classList.contains('docs-kv-item'))return;
      li.classList.add('docs-kv-item');
      first.classList.add('docs-kv-label');
      const body=document.createElement('span');
      body.className='docs-kv-body';
      let n=first.nextSibling;
      while(n){
        const next=n.nextSibling;
        body.appendChild(n);
        n=next;
        }
        li.appendChild(body);
      });
    section.querySelectorAll('li:not(.docs-kv-item)').forEach((li)=>{
      const parsed=parsePlainLabel(li.textContent||'');
      if(!parsed)return;
      li.textContent='';
      li.classList.add('docs-kv-item');
      const label=document.createElement('strong');
      label.className='docs-kv-label';
      label.textContent=parsed.label;
      const body=document.createElement('span');
      body.className='docs-kv-body';
      body.textContent=parsed.body;
      li.appendChild(label);
      li.appendChild(body);
    });
    section.querySelectorAll('p').forEach((p)=>{
      if(p.classList.contains('docs-lead-label'))return;
      if(p.children&&p.children.length)return;
      const text=String(p.textContent||'').trim();
      if(!text)return;
      if(text.length>96)return;
      if(!/^[A-Za-z][A-Za-z0-9\s,\/-]{1,95}:$/.test(text))return;
      p.classList.add('docs-lead-label');
    });
  });
}

function docsResolveBook(bookId){
  const key = String(bookId || '').trim().toLowerCase();
  return DOC_BOOKS[key] ? key : DOC_BOOK_ORDER[0];
}

function docsReadPreferredBook(){
  try{
    return docsResolveBook(localStorage.getItem(DOC_BOOK_STORAGE_KEY));
  }catch(_){
    return DOC_BOOK_ORDER[0];
  }
}

function docsWritePreferredBook(bookId){
  try{ localStorage.setItem(DOC_BOOK_STORAGE_KEY, docsResolveBook(bookId)); }catch(_){}
}
function docsSectionsForBook(book){
  if(!book)return [];
  if(Array.isArray(book.sections))return book.sections;
  book.sections = buildSectionsFromMarkdown(String(book.markdown||''), String(book.prefix||book.id||'doc'));
  return book.sections;
}

function docsEnsureBuilt(){
  const root=$('panel-docs');
  if(!root)return;
  if(root.dataset.docsReady==='1')return;

  const toc=$('docsToc');
  const content=$('docsContent');
  const searchInput=$('docsSearchInput');
  const searchMeta=$('docsSearchMeta');
  if(!toc||!content)return;

  const docTabs=[...root.querySelectorAll('.docs-doctab[data-doc-book]')];
  let activeBookId=docsReadPreferredBook();
  const defaultSearchPlaceholder = searchInput ? searchInput.getAttribute('placeholder') : '';

  const getActiveBook=()=>DOC_BOOKS[activeBookId]||DOC_BOOKS[DOC_BOOK_ORDER[0]];

  const setActiveDocTab=()=>{
    docTabs.forEach((btn)=>{
      const active=btn.getAttribute('data-doc-book')===activeBookId;
      btn.classList.toggle('active',active);
      btn.setAttribute('aria-selected',active?'true':'false');
    });
  };

  const setActiveLink=(id)=>{
    toc.querySelectorAll('.docs-toc-item').forEach((btn)=>btn.classList.toggle('active',btn.getAttribute('data-doc-link')===id));
  };

  const jumpTo=(id)=>{
    const target=content.querySelector('#'+CSS.escape(id));
    if(!target)return;
    target.scrollIntoView({behavior:'smooth',block:'start'});
    setActiveLink(id);
  };

  const syncActiveFromScroll=()=>{
    const visibleSections=[...content.querySelectorAll('.docs-section:not(.hidden)')];
    if(!visibleSections.length)return;
    const containerRect=content.getBoundingClientRect();
    const anchorY=containerRect.top+18;
    let bestId=visibleSections[0].id;
    let bestDistance=Number.POSITIVE_INFINITY;
    visibleSections.forEach((section)=>{
      const rect=section.getBoundingClientRect();
      if(rect.bottom<anchorY)return;
      const dist=Math.abs(rect.top-anchorY);
      if(dist<bestDistance){
        bestDistance=dist;
        bestId=section.id;
      }
    });
    setActiveLink(bestId);
  };
  content.addEventListener('scroll',syncActiveFromScroll,{passive:true});

  const applyFilter=()=>{
    const activeBook=getActiveBook();
    if(activeBook.customRenderer)return;
    const sections=docsSectionsForBook(activeBook);
    const q=String(searchInput&&searchInput.value||'').trim().toLowerCase();
    let visible=0;
    sections.forEach((section)=>{
      const match=!q||section.searchText.includes(q);
      const card=content.querySelector('[data-doc-section="'+section.id+'"]');
      const link=toc.querySelector('[data-doc-link="'+section.id+'"]');
      if(card)card.classList.toggle('hidden',!match);
      if(link)link.classList.toggle('hidden',!match);
      if(match)visible+=1;
    });
    if(searchMeta){
      searchMeta.textContent=q
        ?('Showing '+visible+' of '+sections.length+' sections in '+activeBook.label+' for "'+q+'".')
        :('Showing all '+sections.length+' sections in '+activeBook.label+'.');
    }
    if(!q&&sections.length)jumpTo(sections[0].id);
    requestAnimationFrame(syncActiveFromScroll);
  };

  const renderActiveBook=()=>{
    const activeBook=getActiveBook();
    setActiveDocTab();
    if(activeBook.customRenderer==='knowledge-checks'){
      docsQuizRenderBookInDocs(toc, content, searchInput, searchMeta);
      return;
    }
    if(searchInput){
      searchInput.disabled = false;
      searchInput.placeholder = defaultSearchPlaceholder || 'Search docs...';
    }
    const clearBtn=$('docsSearchClearBtn');
    if(clearBtn)clearBtn.disabled=false;

    const sections=docsSectionsForBook(activeBook);
    toc.innerHTML=sections.map((section)=>(
      '<button type="button" class="docs-toc-item" data-doc-link="'+docsEscHtml(section.id)+'">'+docsEscHtml(section.title)+'</button>'
    )).join('');
    content.innerHTML=sections.map((section)=>(
      '<article id="'+docsEscHtml(section.id)+'" class="docs-section" data-doc-section="'+docsEscHtml(section.id)+'">'
      +'<h3>'+docsEscHtml(section.title)+'</h3>'
      +section.html
      +'</article>'
    )).join('');
    docsEnhanceRenderedDocs(content);
    applyFilter();
    requestAnimationFrame(syncActiveFromScroll);
  };

  toc.addEventListener('click',(ev)=>{
    const kcBtn = ev.target&&ev.target.closest?ev.target.closest('.docs-toc-item[data-kc-nav]'):null;
    if(kcBtn && getActiveBook().customRenderer==='knowledge-checks'){
      const nav = kcBtn.getAttribute('data-kc-nav');
      KNOWLEDGE_CHECKS_STATE.screen = nav;
      docsQuizRender();
      return;
    }
    const btn=ev.target&&ev.target.closest?ev.target.closest('.docs-toc-item[data-doc-link]'):null;
    if(!btn)return;
    jumpTo(btn.getAttribute('data-doc-link'));
  });

  if(searchInput){
    searchInput.addEventListener('input',applyFilter);
  }
  const clearBtn=$('docsSearchClearBtn');
  if(clearBtn&&searchInput){
    clearBtn.addEventListener('click',()=>{
      searchInput.value='';
      applyFilter();
      searchInput.focus();
    });
  }

  docTabs.forEach((btn)=>{
    btn.addEventListener('click',()=>{
      const nextBook=docsResolveBook(btn.getAttribute('data-doc-book'));
      if(nextBook===activeBookId)return;
      activeBookId=nextBook;
      docsWritePreferredBook(activeBookId);
      renderActiveBook();
    });
  });

  root.dataset.docsReady='1';
  renderActiveBook();
}

function openDocumentationPanel(){
  docsEnsureBuilt();
}


`;
